interface:
  display_name: "UI Screen Uplift"
  short_description: "既存UIを構成再設計と状態別収束で看板品質へ引き上げる"
  default_prompt: "Use $ui-screen-uplift to improve this existing game screen from full-screen structure through dynamic-state convergence and evidence-backed freeze."

policy:
  allow_implicit_invocation: true
