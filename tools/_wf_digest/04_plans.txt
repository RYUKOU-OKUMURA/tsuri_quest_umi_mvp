### 水中ファイト画面 (underwater fight — signature/看板 screen): src/ui/fishing_screen.gd + src/ui/components/underwater_view.gd + src/ui/components/gauge_bar.gd
VISION: A vibrant bright-sea underwater fight where a hand-pixelled fish (recognizable sea-bream proportions, animated swim/idle/struggle frames) writhes at the center of a glowing water column lit by soft cyan god-rays, rising bubbles, and drifting caustics — all rendered through a low-internal-resolution NEAREST pipeline so every pixel is chunky and intentional. The HUD chrome around it is genuine JRPG window-skin: ornate gold-on-wood 9-slice frames with beveled corners and a bitmap/pixel font for all numbers and labels, replacing the current flat StyleBoxFlat. Every fight event lands with weight — squash-and-stretch on bites, trauma-based screen shake on big strikes, a trailing ghost bar and pulse flash when tension spikes into the red, and a distinctly framed catch/escape flourish — so the 看板 screen reads as a polished GBA/SNES fishing-JRPG rather than a readable prototype.

GAPS:
  [HIGH] No pixel-art pipeline: the entire scene is anti-aliased vector (20-strip lerp water, 26-band ellipse fish, StyleBoxFlat, GradientTexture2D). project.godot already sets default_texture_filter=0 and gl_compat, but snap-to-pixel keys are missing (causing shimmer) and nothing is authored at low internal resolution. This is the single biggest art-direction delta.
      FIX: Add the two snap keys to project.godot [rendering] (snap_2d_transforms_to_pixel=true, snap_2d_vertices_to_pixel=false). Wrap UnderwaterView's painted content in a SubViewport at 480x270 (clean 2.67x→ round to integer 2x at 960x540, or use 320x180 for clean 4x) upscaled via a TextureRect with STRETCH_SCALE + NEAREST, keeping the existing code-drawn gauge/label HUD in a separate high-res CanvasLayer so text stays crisp. Snap all remaining _draw coords with snappedf(). Keep currently-clean vector art (gradients) LINEAR via per-node texture_filter override so the retrofit does not blur it.
      FILES: project.godot, src/ui/components/underwater_view.gd, src/ui/fishing_screen.gd
      TECH: Enable global snap-to-pixel in project.godot, Low-internal-resolution SubViewport + nearest-upscaling TextureRect, Per-node texture_filter override (mixed HD + pixel art), Integer scale factor with canvas_items stretch + keep aspect
  [HIGH] The target fish is a generic 26-band banded ellipse (procedural cel vector). It does not read as a specific species, has no animation frames, and boss/non-boss differ only by a scale multiplier + faint aura. This is the #1 pixel-art target on the signature screen.
      FIX: Replace _draw_target_fish() with an AnimatedSprite2D child of UnderwaterView, pixel-snapped each frame to simulator.visual_position*view_size. Author sea-bream/kurodai sprites at ~96x64 with tags swim/idle/struggle/caught, flip via scale.x=simulator.visual_direction. Drive the frame and a 1px outline shader (CanvasItemMaterial) from simulator.action_name/state. Keep the tension aura as an additive ColorRect/CPUParticles2D ring. Use a per-fish palette tint (fish_data.color) by modulating the sprite, not by redrawing geometry.
      FILES: src/ui/components/underwater_view.gd
      TECH: Author fish/backgrounds/icons in Aseprite and import via Aseprite Wizard plugin, Fish readability system: silhouette-first authoring + hue/value contrast + sprite outline shader, Scale rules & pixel-perfect placement at 1280x720
  [MED] Water background is 20 hard-edged lerped draw_rect strips + 5 flat-alpha god-ray polygons + an 8-rect fake vignette, recomputed every frame. It skews cold/muted (#1a8fd0→#062847) vs the bright-sea target and shows 8-bit banding with no dithering.
      FIX: Replace the 20-strip loop and 5 ray polygons with ONE procedural canvas_item gradient+god-ray shader on a Full-Rect Control (uniforms top/mid/bot color, ray_intensity, ray_angle, speed), warmed toward a brighter tropical cyan. Add an additive caustics shader layer (BLEND_MODE_ADD) and a dither term to kill banding. Replace the 8-rect vignette with one topmost combined bloom+vignette+grade screen-read pass (single hint_screen_texture sample) requiring a BackBufferCopy. This removes ~30 draw calls/frame and instantly looks cinematic.
      FILES: src/ui/components/underwater_view.gd
      TECH: Per-pixel gradient + god-ray shader to replace the 20-strip _draw_water_background, Animated caustics + shimmer shader on a second fullscreen Control (additive), Screen-space vignette + color-grade shader as the topmost fullscreen pass, Procedural noise grain / dithering shader to kill banding in the water gradient
  [HIGH] JRPG window-skin is absent: all chrome (header, water panel, info panel, HUD, fight badge, distance meter) is flat StyleBoxFlat (bg + 1 border + radius + shadow). No 9-slice ornate frames, bevels, gold corner ornaments, or inner trim — reads as modern flat UI, not 川のぬし lineage. The fish-name badge in underwater_view.gd allocates 3 StyleBoxFlat.new() every frame inside _draw.
      FIX: Migrate ui_theme.gd _style() to a new _tex_stylebox() using StyleBoxTexture 9-slice window-skins (parchment/dark/blue/dialog variants, 24px corners), reusing existing set_stylebox slots and extending the DarkPanel/BluePanel variation pattern to JRPGPanel/JRPGHeader/JRPGDialog. Bake the drop shadow INTO each skin PNG's transparent border + expand_margin_* (StyleBoxTexture has no shadow property). Add TextureRect corner ornaments via a reusable OrnatePanel helper. Cache the badge/track/fill StyleBoxFlat in _ready to stop per-frame allocation. Set sb.texture_filtering=LINEAR_WITH_MIPMAPS on ornate textures since project default is NEAREST.
      FILES: src/ui/ui_theme.gd, src/ui/screen_base.gd, src/ui/components/underwater_view.gd
      TECH: Replace StyleBoxFlat parchment with 9-slice StyleBoxTexture window-skin, Layer a StyleBoxFlat shadow UNDER the StyleBoxTexture (since StyleBoxTexture has no shadow), Decorative corner ornaments, gold accents, ribbons, dividers via TextureRect children + _draw(), Centralize panel grammar via Theme type variations + content_margins, Reduce _draw cost: cache StyleBox/Color, stop queue_redraw when static, drop the 26-band fish gradient
  [HIGH] Typography is a smooth anti-aliased SystemFont (Hiragino Maru Gothic) with generate_mipmaps, used everywhere via get_theme_default_font() and ui_theme build_font(). No .ttf/.otf exists. The most visible 'not retro' tell on HUD numbers, depth, distance, and fish name.
      FIX: Ship a license-cleared JP-capable pixel/bitmap FontFile in res://assets/fonts/. Replace build_font() to load it as FontFile with rendering_antialiasing=MSDF (or NONE for raw pixel), generate_mipmaps=true, msdf_pixel_range=8, and CRITICALLY allow_system_fallback=false (else macOS silently substitutes Hiragino). Keep the already-wired font_outline_color/outline_size theme entries. Pragmatic fallback if no full-JIS pixel font: render a clean small sans (M+/Noto Sans JP) with filtering OFF + integer snap to fake crispness — but make this a conscious logged decision. Note the MSDF outline regression (#109757); fall back to NONE+baked outline if needed.
      FILES: src/ui/ui_theme.gd
      TECH: Pixel/bitmap fonts via FontFile with outline + drop shadow
  [HIGH] No fight juice: motion is continuous procedural sway only. No hit-flash, no screen shake, no hit-stop, no squash, no floating callouts (the mockup's 'ビット!' exclamation is absent), no success/escape flourish. The result overlay is a flat 72% black ColorRect + plain panel.
      FIX: Add a shared Juicer (autoload) exposing add_trauma(amount) on a dedicated ShakeLayer CanvasLayer (FastNoiseLite, cap ~10px at 720p). On fish state changes (charge/jump/hook/escape): squash-and-stretch the fish sprite (pivot_offset=size*0.5, TRANS_BACK/ELASTIC), a 40-90ms hit-stop on a PROCESS_MODE_ALWAYS driver, a white ColorRect flash, and a floating Label popup (釣った!/逃げられた!) rising+fading. Make gauge_bar.gd juice its own bar: exponential lerp toward target (1-exp(-k*delta)), a slow ghost trailing bar, self_modulate pulse on damage, and a pulsing critical-state glow loop when value<0.25. Frame the catch/escape overlay with the JRPG dialog skin + a one-shot CPUParticles2D sparkle burst.
      FILES: src/ui/components/gauge_bar.gd, src/ui/components/underwater_view.gd, src/ui/fishing_screen.gd, src/autoload/juicer.gd (new), project.godot (register autoload)
      TECH: Squash & stretch on impact (fight hits, fish bite, button press), Trauma-based screen shake (CanvasLayer.offset + FastNoiseLite), Hit-stop / freeze frame via process_mode + brief tween pause, Floating damage / number popups (Label spawn + rise + fade), Gauge fill lerp + pulse (tension/stamina/energy bars — already in gauge_bar.gd), Level-up burst (radial particle burst + scale punch + flash + sound sync)
  [MED] Bubbles, background fish, bait, and seabed (rocks/seaweed/pebbles) are all smooth _draw primitives (draw_arc rings, circle+tail polys, draw_circle offset rocks). Atmosphere lacks richness; static water is the biggest mood tell.
      FIX: Replace _draw_bubbles() 18-arc loop with a CPUParticles2D rising-bubble system (NEVER GPUParticles2D on macOS gl_compat — transform-feedback is buggy) using a soft-circle texture, CanvasItemMaterial BLEND_MODE_ADD for glow. Convert background fish to tiny AnimatedSprite2D/TextureRect pixel sprites drifting via the existing fmod loop math. Convert rocks/seaweed to tiled Sprite2D pixel art. Keep plankton motes as a second low-density additive CPUParticles2D layer for mid-water richness. Pixel-snap all sprite positions each physics frame.
      FILES: src/ui/components/underwater_view.gd
      TECH: CPUParticles2D for bubbles/sparkles/steam/debris (NOT GPUParticles2D on macOS gl_compat), Author fish/backgrounds/icons in Aseprite and import via Aseprite Wizard plugin, Scale rules & pixel-perfect placement at 1280x720
  [MED] Palette skews cold/muted and is ad-hoc per element, not a governed indexed set; sand #9ab68c reads green rather than the bright-sea warm beige. Gauge color semantics (tension red/green) are runtime hot-swapped but not tokenized.
      FIX: Define one governed ~16-24 color palette file (sea cyans, warm beige sand, wood, parchment, gold, gauge red/green/amber) and source all hex from it in ui_theme.gd + underwater_view.gd + gauge_bar.gd. Warm the water top toward tropical cyan and sand toward warm beige. Keep tension=red/danger, stamina=green, energy=cyan as committed semantic tokens (genre-standard; the current green→amber→red hot-swap in fishing_screen._update_tension_bar_color is correct intent, just needs to pulse on threshold cross).
      FILES: src/ui/ui_theme.gd, src/ui/components/underwater_view.gd, src/ui/components/gauge_bar.gd, src/ui/fishing_screen.gd
      TECH: Retro palette swap via canvas_item shader (post-process), Consistent pixel-density asset/import presets

NEW ASSETS:
  - res://assets/fish/fish_seabream.ase: The star target fish — recognizable sea-bream/kurodai pixel sprite replacing the 26-band ellipse, with swim/idle/struggle/caught animation. | Canvas 96x64 px, limited ~24-color palette, tags: swim (6 frames loop), idle (2 frames), struggle (4 frames), caught (2 frames). Silhouette-first (dark-on-light read), flip via scale.x. Tintable base for fish_data.color override.
  - res://assets/fish/fish_boss_kurodai.ase: Boss variant with distinct larger proportions and characteristic boss markings. | Canvas 128x80 px, same palette family + boss accent colors, same tag set. Distinct silhouette from normal fish.
  - res://assets/fish/fish_silhouette_bg.ase: Background drift fish silhouettes replacing the circle+tail polys. | Canvas 24x14 px, 2-3 tone dark silhouette, 2-frame swim loop, NEAREST, no mipmap. Several tint variants by depth.
  - res://assets/ui/skin_panel_parchment.png: JRPG 9-slice window-skin for light panels (info panel, result dialog). | 64x64 px, 24px corner border, double-line gold-on-wood bevel + corner ornament + baked drop shadow in transparent margin. Import Lossless, mipmap ON for TILE_FIT centers.
  - res://assets/ui/skin_panel_dark.png: 9-slice window-skin for dark panels (header, HUD, message, water panel frame). | 64x64 px, 24px border, dark navy + gold inner trim + corner studs + baked shadow.
  - res://assets/ui/skin_panel_blue.png + skin_panel_dialog.png: BluePanel variation + dedicated dialog/result overlay skin with heavier ornament. | 64x64 px, 24px (dialog 28px) border, ornate gold corners, ribbon header slot.
  - res://assets/ui/corner_gem_gold.png: Decorative gem/jewel corner ornaments overhanging panels. | 16x16 px, gold gem with highlight pixel, 4 rotations or mirrored per corner. NEAREST.
  - res://assets/fx/bubble_soft.png: CPUParticles2D bubble texture for rising bubbles + sparkle bursts. | 4x4 px white circle with 1px highlight, NEAREST, no mipmap. Used additively.
  - res://assets/fx/sparkle_burst.png: One-shot catch sparkle particle texture. | 8x8 px soft star/sparkle, additive blend, NEAREST.
  - res://assets/env/seabed_rock.png + seaweed.png + sand_pebble.png: Seabed tile sprites replacing draw_circle rocks / line seaweed / pebbles. | rock 48x40, seaweed 16x40 (3-frame sway), pebbles 4-8px variants. Warm beige sand palette, NEAREST.
  - res://assets/shaders/water_gradient.gdshader + caustics.gdshader + vignette_grade.gdshader: Procedural water gradient+god-rays, additive caustics, and combined bloom+vignette+color-grade topmost pass. | canvas_item shaders, UV+TIME only for gradient/caustics (no screen read); vignette uses hint_screen_texture (single sample, low Kawase taps). Bright-sea cyan palette uniforms.
  - res://assets/fonts/jrpg_pixel_jp.ttf: JP-capable pixel/bitmap font for all HUD numbers, labels, depth/distance, fish name. | License-cleared or original, full kana+kanji coverage for 釣り/ファイト/体力/距離. Loaded as FontFile: rendering_antialiasing=MSDF (or NONE), generate_mipmaps=true, allow_system_fallback=false, msdf_pixel_range=8.
  - res://assets/palette/bright_sea_palette.gd: Governed indexed color constants (sea cyans, warm beige, wood, parchment, gold, gauge red/green/amber) sourced by all UI/view code. | GDScript const Color dictionary, ~16-24 entries. Single source of truth replacing scattered hex.
  - res://docs/08_asset_licensing_ledger.csv: Compliance gate per docs/08: provenance record proving every .ase/.png is original art, no pixel-level copying. | Columns: asset_path, type, author, created_date, source=original, license=proprietary-original, references_used (textual only), ai_assisted, review_status. EditorScript scanner for untracked files.

### 水上の釣り/キャスト画面 (surface fishing / cast) — 水上の釣り/キャスト画面
VISION: A bright, sunny pixel-art cast scene: a small player on a wooden dock overlooking a calm sparkling ocean under a stepped-blue GBA-style sky, with the fishing line arcing out and dropping into the water where a ripple ring expands and sun-sparkle particles drift. The action gauges (tension/stamina/energy), a slim top status strip (Lv/rod/buff), and a right-side target-fish zukan card float as ornate JRPG 9-slice window-skins over the world (gold-on-wood beveled frames + bitmap pixel font), NOT flat panels that replace the scene. During READY the player idles on the dock with a "cast" prompt; on CAST/WAITING the line drops with a splash; at BITE a flash + ripple + "hit!" cue fires before the camera transitions down into the underwater fight — giving the cast beat its own distinct, tactile, retro-JRPG identity rather than jumping straight underwater.

GAPS:
  [HIGH] There is NO surface-cast phase: fishing_screen.gd renders the underwater UnderwaterView from State.READY onward, so the player never sees a dock/ocean-horizon view and the cast 'jumps straight to underwater'. The CASTING state exists in the simulator (_tick_casting, 0.55s) but the screen shows nothing surface-specific.
      FIX: Add a dedicated SurfaceCastView Control (sibling/layered over UnderwaterView) that is visible during READY/CASTING/WAITING/APPROACH and hidden once BITE→FIGHT begins. It draws (or sprites) the sky horizon, sea surface band, dock + player sprite, the arcing fishing line, and a bait splash. Wire visibility in fishing_screen._on_state_changed: surface shows when state in [READY,CASTING,WAITING,APPROACH], underwater shows when state in [BITE,FIGHT,CAUGHT,ESCAPED]. Cross-fade with a create_tween on modulate.a over ~0.25s for a smooth dive transition.
      FILES: src/ui/fishing_screen.gd, src/ui/components/surface_cast_view.gd, src/ui/components/underwater_view.gd
      TECH: Low-internal-resolution SubViewport + nearest-upscaling TextureRect, Per-node texture_filter override (mixed HD + pixel art), Scale rules & pixel-perfect placement at 1280x720
  [HIGH] All visuals are smooth vector (StyleBoxFlat panels, GradientTexture2D sky, ellipse-drawn fish, SystemFont). The screen reads painterly, not retro pixel-art — the single biggest art-direction gap vs the reference mockup.
      FIX: Introduce a low-res pixel render layer for the world: wrap SurfaceCastView (and eventually UnderwaterView) in a 320x180 SubViewport upscaled via TextureRect at integer 4x with NEAREST (project.godot already has default_texture_filter=0). Add snap_2d_transforms_to_pixel=true to project.godot [rendering]. Keep the HUD chrome (gauges, buttons, status strip) in a separate high-res CanvasLayer OUTSIDE the SubViewport so text stays crisp. For the sky/sea, replace add_gradient_background with a hand-authored stepped-blue pixel tile (cast_sky_tile.png, sea_surface_tile.png) at 320x180, no mipmaps, Lossless import.
      FILES: project.godot, src/ui/screen_base.gd, src/ui/fishing_screen.gd, src/ui/components/surface_cast_view.gd
      TECH: Enable global snap-to-pixel in project.godot, Low-internal-resolution SubViewport + nearest-upscaling TextureRect, Per-texture import presets: disable mipmaps, set lossless VRAM, fix alpha, Consistent pixel-density asset/import presets
  [HIGH] UI chrome is flat StyleBoxFlat rectangles (ui_theme.gd _style: bg + 1 border + corner_radius + soft shadow). Reads as modern flat UI, not JRPG ornate window-skin — the #1 'professional JRPG' signal missing.
      FIX: Migrate ui_theme.gd _style() to a _tex_stylebox() helper using StyleBoxTexture 9-slice window-skins (skin_panel_parchment.png, skin_panel_dark.png, skin_panel_blue.png) at 24px corners. CRITICAL: since StyleBoxTexture has no shadow property, bake the drop shadow into each skin PNG's transparent border and set expand_margin_* so it renders outside the content rect (the current code relies on shadow_size everywhere and would silently lose lift otherwise). Set sb.texture_filtering = LINEAR_WITH_MIPMAPS on the ornate textures (project default is Nearest and would pixelate painterly frames) and enable texture_mipmaps on import. Preserve the existing DarkPanel/BluePanel type-variation pattern and add JRPGPanel/JRPGHeader variants with consistent content_margin_*.
      FILES: src/ui/ui_theme.gd, src/ui/screen_base.gd
      TECH: Replace StyleBoxFlat parchment with 9-slice StyleBoxTexture window-skin, Layer a StyleBoxFlat shadow UNDER the StyleBoxTexture (since StyleBoxTexture has no shadow), Decorative corner ornaments, gold accents, ribbons, dividers via TextureRect children + _draw(), Centralize panel grammar via Theme type variations + content_margins
  [HIGH] Typography is a smooth anti-aliased SystemFont (Hiragino Maru Gothic, ui_theme.gd build_font lines 80-84) with generate_mipmaps. HUD numbers, the fish-card, and the cast prompt all read as modern web text, not retro JRPG. CRITICAL: a pure ASCII 8x8 bitmap font cannot render 釣り/仕掛け/港.
      FIX: Ship a license-cleared JP-capable pixel/bitmap FontFile in res://assets/fonts/, loaded in build_font() as FontFile with rendering_antialiasing=NONE (or MSDF if the JP font supports it), generate_mipmaps=true, allow_system_fallback=false (essential on macOS or the OS silently substitutes Hiragino and destroys the pixel look). Keep the already-wired font_outline_color/outline_size theme entries. If a full JIS pixel font is out of scope, fallback is a clean small JP sans (M+ / Noto Sans JP) rendered with filter=NEAREST + integer snap — a conscious decision documented in the asset ledger.
      FILES: src/ui/ui_theme.gd
      TECH: Pixel/bitmap fonts via FontFile with outline + drop shadow
  [MED] The cast/bite moment has no juice: no splash, no ripple ring, no flash, no exclamation, no button press feedback. The action buttons use only StyleBox hover/pressed swaps. The cast currently just swaps simulator state and re-renders underwater. Static and lifeless vs the mockup.
      FIX: (1) Add a CPUParticles2D splash + an expanding ripple-ring (pixel circle outline drawn in SurfaceCastView._draw at the bait water-entry point) on CASTING, and a white flash + 'hit!' popup + ripple on BITE. (2) Add a shared button-feedback wiring in screen_base.make_button: set pivot_offset=size*0.5, connect mouse_entered/exited/button_down/button_up to create_tween scale (hover 1.05, press 0.92, release TRANS_BACK overshoot 1.0), always kill() the previous tween. (3) Add a light trauma screen-shake via a dedicated CanvasLayer.offset + FastNoiseLite on BITE/hit (cap ~8px at 720p). (4) On the gauges, add a CPUParticles2D-free self_modulate pulse on damage and a slow ghost-trailing bar.
      FILES: src/ui/screen_base.gd, src/ui/components/gauge_bar.gd, src/ui/components/surface_cast_view.gd, src/ui/fishing_screen.gd
      TECH: Squash & stretch on impact (fight hits, fish bite, button press), Button press / anticipation feedback (press-down, hover breathe, release), CPUParticles2D for bubbles/sparkles/steam/debris (NOT GPUParticles2D on macOS gl-compat), Trauma-based screen shake (CanvasLayer.offset + FastNoiseLite), Gauge fill lerp + pulse (tension/stamina/energy bars — already in gauge_bar.gd), Floating damage / number popups (Label spawn + rise + fade)
  [MED] GaugeBar allocates StyleBoxFlat.new() every _draw frame (shadow_style, track_style in gauge_bar.gd lines 64-78) and is smooth vector, not pixel-styled. UnderwaterView similarly allocates badge_style/track_style/fill_style per-frame in _draw_fight_overlay and re-queues redraw unconditionally every _process even when static.
      FIX: Cache the three StyleBoxFlat instances as member vars initialized in _ready (gauge_bar.gd) and _ready (underwater_view.gd _draw_fight_overlay badge/track/fill). Gate queue_redraw() on whether anything is actually animating (e.g. only redraw SurfaceCastView when state in CASTING/WAITING/APPROACH or _time-driven sparkle/ripple is visible; skip when READY-idle with no animation). Add integer snapping via roundf()/snappedf() on all draw coordinates so pixel art and drawn shapes share one pixel lattice. This frees the frame budget the pixel pipeline and juice need.
      FILES: src/ui/components/gauge_bar.gd, src/ui/components/underwater_view.gd, src/ui/components/surface_cast_view.gd
      TECH: Reduce _draw cost: cache StyleBox/Color, stop queue_redraw when static, drop the 26-band fish gradient, Consistent pixel-density asset/import presets
  [MED] The cast scene's ocean is flat/dead: no animated sun-sparkles, no parallax wave bands, no foam crest at the horizon — the single biggest atmosphere tell. The reference's 'bright calm sea' mood depends on water life.
      FIX: In SurfaceCastView add: (a) a CPUParticles2D sparkle layer (1-2px white dots drifting on the sea band, additive blend via CanvasItemMaterial BLEND_MODE_ADD); (b) 2-3 parallax wave-band strips scrolling horizontally via _time in _draw (pixel-snapped); (c) a foam crest line at the horizon drawn as a 1-2px near-white line with a few dithered dots. Use CanvasItemMaterial BLEND_MODE_ADD for the sparkle layer (cheapest glow on gl_compat, no HDR needed). Keep these in the low-res SubViewport so they pixelate consistently.
      FILES: src/ui/components/surface_cast_view.gd
      TECH: CanvasItemMaterial blend modes on the _draw node itself (additive/multiply/subtract), CPUParticles2D for bubbles/sparkles/steam/debris (NOT GPUParticles2D on macOS gl-compat)
  [MED] There is no top status strip (time/weather/wind/gold or Lv/rod/buff summary) distinct from the header, and no right-side target-fish zukan card with a pixel thumbnail. The current info_panel is a plain parchment box with text and a stock OptionButton.
      FIX: Restructure fishing_screen layout: add a slim top DarkPanel status strip (Lv/rod/buff, sourced from existing _trip_stats — moves content out of the header subtitle into its own strip). Convert the right info_panel into a zukan card: parchment JRPGPanel + gold border + a pixel fish thumbnail (from the new fish sprite atlas) + bitmap-font name/size/habitat. Theme the OptionButton (it already has button styleboxes) so the target selector matches the window-skin. Keep the existing GameData.get_fish data binding.
      FILES: src/ui/fishing_screen.gd, src/ui/screen_base.gd, src/ui/ui_theme.gd
      TECH: Centralize panel grammar via Theme type variations + content_margins, Icon set via TextureRect + AtlasTexture (one sheet, many icons), Hover/press/disabled feedback with texture variants + Tween
  [HIGH] No asset pipeline or original-art licensing ledger exists (assets/ = icon.svg only). The reference's art direction cannot be reached without authored sprites, and docs/08 requires original art only (no pixel-for-pixel copying).
      FIX: Stand up the asset production pipeline now: create res://assets/cast/ and res://assets/ui/ folders, configure a 'pixel_art' import preset (Filter=Nearest, Mipmaps=Off, Compression=Lossless/VRAM Uncompressed) and bulk-apply. Author all new sprites in Aseprite at fixed cell sizes (player 48x48, dock tiles 32x32, sky/sea 320x180, frame skins 64x64 @24px corners, icons 32x32). Create res://docs/08_asset_licensing_ledger.csv (asset_path, author, source='original', license='proprietary-original', references_used, ai_assisted, review_status) and an EditorScript scanner that flags any .ase/.png missing a ledger row. Every asset gets a row at creation time.
      FILES: res://assets/, res://docs/08_asset_licensing_ledger.csv, project.godot
      TECH: Author fish/backgrounds/icons in Aseprite and import via Aseprite Wizard plugin, Build a texture atlas with AtlasTexture for UI frames and icons, Per-texture import presets: disable mipmaps, set lossless VRAM, fix alpha, Original-art licensing ledger per docs/08 (no copying existing works), Fish readability system: silhouette-first authoring + hue/value contrast + sprite outline shader

NEW ASSETS:
  - cast_sky_tile.png: Bright cheerful stepped-blue sky above the horizon for the surface-cast scene, replacing the smooth GradientTexture2D. | 320x180 internal res (integer 4x upscale to 1280x720). Limited 4-6 step palette (#8ec9e8 base + 2 lighter steps + 2 dithered cloud whites). PNG, Nearest, no mipmaps, Lossless. Original art, 16-32 color cap.
  - sea_surface_tile.png: Ocean surface band below the horizon with wave bands and foam crest; tiled/scrolled horizontally for parallax. | 320x90 internal res (lower half of the 320x180 world). Palette: #3d8fc4 near-surface, #1f5e8c deeper band, #eaf6ff foam/sparkle. PNG, Nearest, no mipmaps, Lossless. Horizontal tileable.
  - dock_planks_tile.png: Wooden dock/harbor edge the player stands on, reusing the existing wood tone (#8a5428 family). | 64x32 tileable plank, 24px corner-safe. Palette: #8a5428 plank, #6e5635 shadow line, #e1bd72 gold nail highlight. PNG, Nearest, no mipmaps, Lossless. Reuse ui_theme wood button tone for cohesion.
  - player_angler_idle.png / player_angler_cast.png: Small original pixel-art angler character on the dock — idle (rod resting) and cast (rod extended, line arcing). Frames for an AnimatedSprite2D or TextureRect swap on CASTING. | 48x48 cell, 2-3 frames idle (subtle bob), 2 frames cast. Palette: ~12 colors (skin, shirt blue, hat, rod brown matching dock). PNG, Nearest, no mipmaps, Lossless. Silhouette-first authoring with 2-tone value read.
  - skin_panel_parchment.png / skin_panel_dark.png / skin_panel_blue.png: 9-slice JRPG window-skin frame textures for all HUD/panel chrome, replacing flat StyleBoxFlat. Parchment = info panels/zukan card; dark = status strip/HUD backdrop; blue = accent. | 64x64 each, 24px corner border region, DROP SHADOW BAKED into transparent outer margin (so expand_margin renders lift without StyleBoxFlat.shadow). Double-line gold inner trim (#e1bd72) + outer wood/navy line + small corner ornaments. PNG, LINEAR_WITH_MIPMAPS filtering (set per-StyleBoxTexture), mipmaps enabled, Lossless.
  - skin_button_normal.png / skin_button_hover.png / skin_button_pressed.png: 9-slice pixel button textures for the 4 existing button states (normal/hover/pressed; disabled = normal desaturated via modulate). Cast/reel/give buttons + zukan actions. | 64x32 each, 18px corner border. Pressed variant shifted 1px down (baked inset) + darker wood. Gold border on hover. PNG, LINEAR_WITH_MIPMAPS, Lossless.
  - fish_silhouette_atlas.png: Atlas of small pixel fish thumbnails for the right-side zukan card (and locked silhouettes). One sheet, sliced via AtlasTexture. | 256x64 atlas, 8 cells of 32x32. Palette per fish from fish_data.color, silhouette-first with dark outline. PNG, Nearest, no mipmaps, Lossless. Cell size 32x32 matches the icon convention.
  - sparkle_particle.png / splash_particle.png: Particle textures for sun-sparkles on the sea (sparkle, additive) and the cast/bite splash burst (splash). | 8x8 each, white/near-white core with soft alpha falloff. PNG, Nearest, no mipmaps, Lossless. Used by CPUParticles2D with BLEND_MODE_ADD.
  - jp_pixel_font.ttf: JP-capable pixel/bitmap font for HUD numbers, labels, the zukan card, and the cast prompt, replacing SystemFont Hiragino Maru Gothic. | License-cleared .ttf covering kana + kanji needed for this screen (釣り/仕掛け/港/準備/食いつき/ファイト, etc.). Rendered with rendering_antialiasing=NONE (or MSDF if supported), generate_mipmaps=true, allow_system_fallback=false. If no JIS pixel font available, fallback = M+ or Noto Sans JP small with filter=NEAREST + integer snap (documented decision in ledger).

### 調理・食事・レベルアップ画面 (cooking / meal / level-up)
VISION: A cozy indoor tavern kitchen rendered in authentic retro pixel art: a warm parchment-and-wood table scene where the player-chef sprite stands beside a fire, surrounded by ornate 9-slice JRPG window-skin panels (gold-trimmed, beveled, corner ornaments) holding a grid of pixel-art dish icons on the left, a large illustrated featured-dish plate on the right with a soft warm glow, and a dedicated framed LEVEL-UP window showing old→new level with before/after stat bars. Every character, label, and number uses a crisp JP-capable pixel bitmap font with 1px outlines. The mood is the warm 'reward beat' — deliberately NOT the bright open sea of the fishing screens — brought to life with a warm gradient + paper-grain backdrop, per-panel drop shadows, subtle steam on hot dishes, and juice (EXP-bar tween + sparkle, dish-reveal pop, level-up burst with radial sparkles + flash + label punch). The shared theme, window-skin, GaugeBar, and font upgrades make every other screen benefit simultaneously.

GAPS:
  [HIGH] UI chrome is flat StyleBoxFlat rectangles (parchment + 1 border + corner_radius + shadow), not an ornate JRPG window-skin. cooking_screen.gd uses make_panel()/make_panel(true) which resolve to ui_theme.gd _style() — no 9-slice, no bevel, no gold inner trim, no corner ornaments. This is the single biggest 'not a JRPG' tell across the whole app.
      FIX: Add a _tex_stylebox(tex, margin, tile) helper to ui_theme.gd that builds StyleBoxTexture 9-slice window-skins, and author 3 PNGs (parchment, dark, dialog) at 24px corners with beveled gold-on-wood borders + baked drop shadow in the transparent margin. Add new type variations JRPGPanel/JRPGHeader/JRPGDialog/JRPGLevelUp and a GoldButton stylebox slot. Change screen_base.make_panel(kind) to set theme_type_variation instead of the dark bool. Set sb.texture_filtering=LINEAR_WITH_MIPMAPS on ornate textures and bake shadow into the PNG so panel lift survives the StyleBoxTexture-has-no-shadow migration. Add a reusable add_corners() ornament helper to screen_base.gd.
      FILES: src/ui/ui_theme.gd, src/ui/screen_base.gd
      TECH: Replace StyleBoxFlat parchment with 9-slice StyleBoxTexture window-skin, Layer a StyleBoxFlat shadow UNDER the StyleBoxTexture (since StyleBoxTexture has no shadow), Decorative corner ornaments, gold accents, ribbons, dividers via TextureRect children + _draw(), Centralize panel grammar via Theme type variations + content_margins
  [HIGH] Typography is a smooth anti-aliased SystemFont (Hiragino Maru Gothic ProN, ui_theme.gd build_font() L80-84) with generate_mipmaps — the most obvious 'modern prototype, not retro JRPG' tell. No .ttf/.fnt exists in assets/. A JP game cannot use an ASCII 8x8 bitmap; it needs kana+kanji coverage.
      FIX: Source a license-cleared JP-capable pixel/bitmap TTF, drop into res://assets/fonts/, and rewrite build_font() to FontFile with rendering_antialiasing=MSDF, generate_mipmaps=true, msdf_pixel_range=8, msdf_size=48, allow_system_fallback=false (critical on macOS or OS silently substitutes Hiragino). Keep the already-wired font_outline_color/outline_size theme entries. If no JP pixel font is licensed, ship M+ / Noto Sans JP as FontFile with filtering OFF + integer snapping as a documented fallback. Note the MSDF outline regression (#109757) and test against LTSDF fallback.
      FILES: src/ui/ui_theme.gd
      TECH: Pixel/bitmap fonts via FontFile with outline + drop shadow
  [HIGH] No pixel-art pipeline or assets exist (assets/ holds only icon.svg). cooking_screen uses stock Godot ItemList for fish/recipe lists (unthemed — ui_theme.gd never sets an ItemList stylebox, so it falls back to default grey, breaking the window-skin illusion at the most interactive element) and stock ProgressBar for EXP (the project's nicer custom GaugeBar is unused). The screen has no dish icons, no featured-dish illustration, no chef sprite.
      FIX: (a) Theme ItemList — add panel/selected/focus/hover StyleBoxTexture slots in ui_theme.gd so list rows match the window-skin. (b) Replace _exp_bar ProgressBar with the custom GaugeBar component (gradient fill + outline), configured with EXP colors and given a pixel-mode flag. (c) Build a GridContainer of TextureRect dish icons sourced from a 32px-cell atlas (AtlasTexture slices) in place of text-only rows for at least the recipe list. (d) Add a featured-dish TextureRect + chef TextureRect in the detail panel. (e) Apply the canvas_item outline shader to the chef/dish sprites so they pop on the warm bg.
      FILES: src/ui/ui_theme.gd, src/ui/cooking_screen.gd, src/ui/components/gauge_bar.gd
      TECH: Icon set via TextureRect + AtlasTexture (one sheet, many icons), Fish readability system: silhouette-first authoring + hue/value contrast + sprite outline shader, Hover/press/disabled feedback with texture variants + Tween, Replace StyleBoxFlat parchment with 9-slice StyleBoxTexture window-skin
  [HIGH] No dedicated, framed LEVEL-UP window. Currently level-up is one appended text line in the bottom result Label ('LEVEL UP！ Lv.X になった！', cooking_screen.gd L228-231), with the boss-unlock callout at Lv.5 buried in the same text. docs/03 §5 requires a real popup with old→new level, raised stats, and boss-unlock emphasis.
      FIX: Build a reusable LevelUpPanel scene/component (CenterContainer + DarkPanel-style window-skin + gold title 'LEVEL UP!') that shows: big pixel-font level number, a 'Lv.X → Lv.Y' arrow, before/after GaugeBar pairs for raised stats, and a gold-highlighted boss-unlock callout when new level == GameData.BOSS_UNLOCK_LEVEL. Trigger it from _cook_selected when result.leveled_to is non-empty, as a modal CenterContainer over the screen. Gate it behind the same code path that today writes the text line.
      FILES: src/ui/cooking_screen.gd, src/ui/components/level_up_panel.gd
      TECH: Level-up burst (radial particle burst + scale punch + flash + sound sync), Gauge fill lerp + pulse (tension/stamina/energy bars — already in gauge_bar.gd), Centralize panel grammar via Theme type variations + content_margins
  [MED] Background is a flat ColorRect (#172235, cooking_screen.gd L16) — the flattest, darkest screen in the app, contradicting the warm cozy tavern mood. No gradient, no paper-grain texture, no light.
      FIX: Replace add_background(Color('#172235')) with a warm vertical gradient (warm beige top #2a2418 → warm dark brown bottom #14110b) via add_gradient_background (already in screen_base.gd) OR a dedicated TextureRect, then overlay a baked 64x64 NoiseTexture2D paper-grain CanvasTexture at ~0.06 alpha. Optionally add a faint warm radial light behind the featured-dish plate (additive CanvasItemMaterial glow TextureRect). Keep the same move_child(bg,0) anchoring.
      FILES: src/ui/cooking_screen.gd, src/ui/screen_base.gd
      TECH: Per-node texture_filter override (mixed HD + pixel art), Canvas-item shaders for water/lighting without 2D HDR (canvas_item Light2D-style tint), Per-pixel gradient + god-ray shader to replace the 20-strip _draw_water_background
  [MED] No animation/juice. Cooking just swaps label text in place (_cook_selected L218-242), EXP bar jumps, item selection has no transition, no dish-reveal, no sparkle. Static vs the mockup's juicy reward beat.
      FIX: Add create_tween() feedback: (a) EXP bar tween — extend GaugeBar with displayed_value exponential lerp toward target + a ghost trailing bar + self_modulate pulse on value change (framerate-independent via 1-exp(-k*delta)). (b) Dish-reveal: scale-punch the featured-dish TextureRect (TRANS_BACK 0.4→1.2→1.0) on selection change. (c) Cook action: spawn a short CPUParticles2D steam burst over the dish + a floating EXP-gain Label popup. (d) Level-up burst (see LevelUpPanel gap). (e) Wire every Button through a shared button-feedback script (hover 1.05, press 0.92, release TRANS_BACK overshoot), always kill() previous tween. (f) Add the two snap-to-pixel keys to project.godot so tweens don't shimmer.
      FILES: src/ui/cooking_screen.gd, src/ui/components/gauge_bar.gd, src/ui/screen_base.gd, project.godot
      TECH: Gauge fill lerp + pulse (tension/stamina/energy bars — already in gauge_bar.gd), Button press / anticipation feedback (press-down, hover breathe, release), Floating damage / number popups (Label spawn + rise + fade), Squash & stretch on impact (fight hits, fish bite, button press), Level-up burst (radial particle burst + scale punch + flash + sound sync), Enable global snap-to-pixel in project.godot, CPUParticles2D for bubbles/sparkles/steam/debris (NOT GPUParticles2D on macOS gl_compat)
  [MED] Palette skews dark/muddy (#172235 bg, #12283f dark panels) and gold accents are thin 1px borders rather than bold ornament. The screen reads cooler than the intended warm lived-in tavern.
      FIX: Govern a fixed warm palette via the new window-skin textures: cream parchment (#f0e6d2/#f3e8cd), warm mid-brown borders (#6e5635→#8b4513), bold gold accents (#d4af37/#e1bd72), one deep-blue contrast panel (#1e3a5a/#12283f) reserved for the level-up window only. Repeat this exact set across every panel for harmony. Warm the background toward neutral/charcoal (#2a2a2a-family) rather than blue-black.
      FILES: src/ui/ui_theme.gd, src/ui/cooking_screen.gd
      TECH: Replace StyleBoxFlat parchment with 9-slice StyleBoxTexture window-skin, Centralize panel grammar via Theme type variations + content_margins
  [LOW] No original-art licensing ledger exists. docs/08 mandates original art only (no pixel-for-pixel copying) and the project is about to author its first real sprites — compliance is trivial at creation time, painful if retrofitted.
      FIX: Create res://docs/08_asset_licensing_ledger.csv with columns (asset_path, asset_type, author, created_date, source='original', license='proprietary-original', references_used, ai_assisted, review_status). Add an EditorScript scanner that walks res://assets/ for .png/.ase and flags any missing a row. Each new asset (dish atlas, chef, plate, window-skins) gets a row at creation; art director marks review_status='approved' after confirming no third-party pixel copying. Store source .ase under res://assets_src/aseprite/.
      FILES: docs/08_asset_licensing_ledger.csv
      TECH: Original-art licensing ledger per docs/08 (no copying existing works)

NEW ASSETS:


### 全体シェル・共通テーマ・デザイントークン (title_screen, harbor_screen, main.gd, ui_theme.gd, screen_base.gd) — establishes the global JRPG look + pixel pipeline shared by ALL screens
VISION: The shell screens (title + harbor) should read as a bright, cheerful GBA/SNES-era fishing JRPG: a sunlit turquoise sea + warm parchment + gold-beveled 9-slice window-skin, with crisp pixel-art clouds/waves drifting behind ornate frames and a chunky bitmap font with a 1px dark outline on every label. Menus must feel tactile — buttons squash on press, panels lift on soft baked shadows, and screens cross-fade instead of popping — so the whole game inherits one disciplined, low-palette pixel identity from a single shared Theme rather than per-screen ad-hoc styling. The mood is そして明るく穏やか (bright and calm), NOT the current muted dark-navy prototype look.

GAPS:
  [HIGH] All panels/buttons are flat StyleBoxFlat rectangles (bg_color + 1 border + corner_radius + Gaussian shadow) via ui_theme.gd _style(). They read as modern flat dashboard chrome, not a JRPG ornate window-skin — no 9-slice bevel, no gold corner ornaments, no inner trim. This is the single biggest 'looks like a prototype' signal shared by every screen.
      FIX: In ui_theme.gd add a _tex_stylebox(tex, margin, tile) helper returning a StyleBoxTexture (9-slice), and replace _style() outputs for Panel/PanelContainer/Button/AcceptDialog with textured window-skins. Register new type variations JRPGPanel/JRPGHeader/JRPGDialog/JRPGButtonGold via set_type_variation (mirroring the existing DarkPanel/BluePanel pattern). Bake the drop shadow INTO each skin PNG's transparent border and set expand_margin_* so lift is preserved (StyleBoxTexture has NO shadow property — current code relies on StyleBoxFlat.shadow_size everywhere and will silently lose all panel lift if not handled). Set sb.texture_filtering = LINEAR_WITH_MIPMAPS on painted skins because project default_texture_filter=0 (NEAREST) would otherwise pixelate the ornate art; enable texture_mipmaps on import so TILE_FIT centers don't alias at 1280x720. Update screen_base.make_panel(dark) to take a kind string and set theme_type_variation accordingly.
      FILES: src/ui/ui_theme.gd, src/ui/screen_base.gd
      TECH: Replace StyleBoxFlat parchment with 9-slice StyleBoxTexture window-skin, Layer a StyleBoxFlat shadow UNDER the StyleBoxTexture (since StyleBoxTexture has no shadow), Decorative corner ornaments, gold accents, ribbons, dividers via TextureRect children + _draw(), Centralize panel grammar via Theme type variations + content_margins, Hover/press/disabled feedback with texture variants + Tween
  [HIGH] Typography is a smooth anti-aliased SystemFont (Hiragino Maru Gothic ProN fallback chain, generate_mipmaps=true) with no imported .ttf and no bitmap/pixel font. This is the single most visible 'not retro' tell: headers, HUD numbers, and menu labels all look like a modern web UI. Font sizes are flat ad-hoc numerics (18/28/32/58) with no designed type scale.
      FIX: Replace build_font() to load a license-cleared JP-capable pixel FontFile from res://assets/fonts/, set rendering_antialiasing=TEXT_ANTIALIASING_NONE (or MSDF if outlines mis-render, per #109757 regression), generate_mipmaps=true, allow_system_fallback=false (CRITICAL on macOS or the OS silently substitutes Hiragino and destroys the pixel aesthetic). Keep the already-wired font_outline_color/outline_size theme entries. Because a pure 8x8 ASCII bitmap cannot render 釣り/港/料理, source a JP pixel font OR fall back to a small-crisp sans (M+ / Noto Sans JP) rendered with filtering OFF + integer snap as a conscious documented decision. Add make_shadow_label() helper in screen_base.gd for a true offset drop-shadow Label on bright backgrounds.
      FILES: src/ui/ui_theme.gd, src/ui/screen_base.gd
      TECH: Pixel/bitmap fonts via FontFile with outline + drop shadow
  [HIGH] Shell backgrounds are dead-flat dark ColorRects (title #07172b, harbor #0b2840) or a single 64x64 2-stop GradientTexture2D stretched full-rect — banding-prone, lifeless, and mood-inconsistent (cool muted navy contradicts the documented bright cheerful sea). No parallax sky/sea, no clouds, no ambient motion, no particles on the shell.
      FIX: In screen_base.gd, add add_pixel_sea_background(top, mid, bottom) that creates a small baked low-res sea+sky texture (or a procedural canvas_item gradient shader with a dither term to kill 8-bit banding) in a TextureRect with texture_filter=NEAREST, expand_mode=IGNORE_SIZE, stretch_mode=SCALE — integer-scaled so it reads as pixel art. Add drifting cloud + sparkle particles via CPUParticles2D (NOT GPUParticles2D — unreliable on macOS gl_compat). Replace title's add_background(#07172b) and harbor's add_background(#0b2840) with the bright-sea gradient (bright cyan #8ec9e8 sky → turquoise #3d8fc4 → deep #1f5e8c sea). Add a reusable ParallaxBackground+ParallaxLayer helper for optional parallax depth.
      FILES: src/ui/screen_base.gd, src/ui/title_screen.gd, src/ui/harbor_screen.gd
      TECH: Enable global snap-to-pixel in project.godot, Per-node texture_filter override (mixed HD + pixel art), CPUParticles2D for bubbles/sparkles/steam/debris (NOT GPUParticles2D on macOS gl_compat), Procedural noise grain / dithering shader to kill banding in the water gradient
  [MED] Pixel pipeline is configured (gl_compatibility + default_texture_filter=0 NEAREST + canvas_items stretch) but snap-to-pixel keys are MISSING (2d/snap/snap_2d_transforms_to_pixel, snap_2d_vertices_to_pixel), causing sub-pixel shimmer on any future moving sprites. Stretch aspect is 'expand' (non-integer-safe) rather than 'keep' with integer scale_mode.
      FIX: Add to project.godot [rendering]: 2d/snap/snap_2d_transforms_to_pixel=true, 2d/snap/snap_2d_vertices_to_pixel=false (enable vertices only if rotating sprites show stair-step tearing). Change [display] window/stretch/aspect from 'expand' to 'keep' and add window/stretch/scale_mode='integer' so the 1280x720 canvas always scales by a whole number (no directional shimmer). This is a 1-line-each retrofit that unblocks all downstream pixel art and is non-destructive to existing vector UI.
      FILES: project.godot
      TECH: Enable global snap-to-pixel in project.godot, Integer scale factor with canvas_items stretch + keep aspect
  [MED] Zero animation/feedback (juice) in the shell: screens pop instantly via main.gd _show_screen (queue_free + add_child, no transition), buttons use only StyleBox hover/pressed swaps with no tween, no scale/pop/flash, no SFX wiring. Static and lifeless vs the mockup's tactile JRPG menus.
      FIX: Add a shared button-feedback script (or logic inside screen_base.make_button): set pivot_offset=size*0.5 in _ready, connect mouse_entered/exited → tween scale 1.0↔1.05 (TRANS_SINE EASE_OUT), button_down → scale 0.92 (quick squash), button_up → TRANS_BACK overshoot back to 1.0; always kill() the previous tween before starting a new one. Add a screen-transition layer in main.gd: wrap _current_screen swap in a fade-to-black ColorRect tween (0.15s out, 0.15s in) instead of instant pop. Add a global ShakeLayer CanvasLayer autoload for trauma-based shake (reused by fight screen later).
      FILES: src/ui/screen_base.gd, src/main.gd
      TECH: Button press / anticipation feedback (press-down, hover breathe, release), Trauma-based screen shake (CanvasLayer.offset + FastNoiseLite), Squash & stretch on impact (fight hits, fish bite, button press)
  [MED] Palette is ad-hoc per-screen (shell is dark navy while theme promises bright parchment/sea) and not governed by a single indexed color set; hex values are scattered as string literals across ui_theme.gd, screen_base.gd, and each screen. Inconsistent palette discipline undermines visual rhythm.
      FIX: Define a single Palette const block (or a res://assets/themes/palette.gd autoload) exposing the governed ~16-32 color set: sky/sea blues, foam white, wood #8a5428, parchment #f3e8cd, dark panel #12283f, gold #e1bd72/#ffe7a8, gauge red #d94b4b, gauge green #3cbf78, bone text #fff1c7. Reference these consts everywhere instead of literal hex. This makes a future palette-swap shader (retro palette quantization pass) a one-file change.
      FILES: src/ui/ui_theme.gd, src/ui/screen_base.gd
      TECH: Retro palette swap via canvas_item shader (post-process), Consistent pixel-density asset/import presets
  [MED] No icons exist anywhere (assets/ = icon.svg only). Title/harbor menus, the header Lv/money/rod line, and footer status are all pure text — no coin icon, no facility icons, no sun/cloud/wind glyphs. The JRPG information density is present in text but not in visuals.
      FIX: Author one 256x256 (or 512x512) icon atlas PNG (32x32 cells) and add a make_icon(atlas, cell, cell_px) helper in screen_base.gd returning a TextureRect with an AtlasTexture region, texture_filtering=NEAREST, expand_mode=IGNORE_SIZE. Wire coin/rod/fish/sun icons into the harbor header and facility-menu buttons. Use TextureRect.modulate for rarity/tint states. Enable filter_clip on AtlasTexture if any icon art switches to LINEAR to avoid neighbor bleed.
      FILES: src/ui/screen_base.gd, src/ui/harbor_screen.gd, src/ui/title_screen.gd
      TECH: Icon set via TextureRect + AtlasTexture (one sheet, many icons), Consistent pixel-density asset/import presets
  [LOW] No asset-production compliance pipeline exists. docs/08 mandates original art only (no pixel-for-pixel copying of existing works), but there is no licensing ledger and no import-preset discipline, so any future .png/.ase risks non-compliance or mipmap-blur.
      FIX: Create res://docs/08_asset_licensing_ledger.csv with columns (asset_path, type, author, created_date, source='original', license='proprietary-original', references_used, ai_assisted, review_status) and an EditorScript scanner that walks res://assets/ for .png/.ase missing a row. Store source .ase under res://assets_src/aseprite/. Apply a 'pixel_art' import preset (Lossless, Mipmaps OFF, Fix Alpha Border ON, Premultiply Alpha OFF) to all sprite/icon folders via bulk Reimport.
      FILES: docs/08_asset_licensing_ledger.csv, assets/ (import presets)
      TECH: Original-art licensing ledger per docs/08 (no copying existing works), Per-texture import presets: disable mipmaps, set lossless VRAM, fix alpha

NEW ASSETS:
  - skin_panel_parchment.png: 9-slice window-skin for the default light JRPGPanel (warm parchment body with beveled gold-on-wood border + corner ornaments, drop shadow baked into transparent outer margin). Replaces the flat #f3e8cd StyleBoxFlat across all menus. | 64x64 px (24px corner margin), ~16-color palette: parchment #f3e8cd, wood #8a5428/#6e5635, gold #e1bd72/#ffe7a8, shadow rgba(0,0,0,0.32). Square/beveled corners (NOT rounded — authentic JRPG lineage). Transparent 10px outer border holding baked shadow. Lossless, no mipmaps.
  - skin_panel_dark.png: 9-slice window-skin for DarkPanel/JRPGHeader (deep navy #12283f with gold #9a7745 bevel + corner studs) — used for headers, HUD strips, footer status, dialogs. | 64x64 px, 20px corner margin. Navy #12283f, gold #ffe7a8/#9a7745, bone text #fff1c7. Baked shadow. Square corners. Lossless.
  - skin_button_*.png (normal/hover/pressed/disabled): Four 9-slice button states replacing the wood-tone StyleBoxFlat swaps. Normal=wood #8a5428/gold border; Hover=brightened #a66831/light gold; Pressed=inset dark #60381f (1px down-shift baked); Disabled=desaturated gray #6c6860. | 48x48 px each, 18px corner margin. Pressed variant authored 1px lower to fake inset. Lossless, no mipmaps.
  - skin_button_gold.png: Variant gold 9-slice for primary CTA buttons (Cast/Reel/Cook) — beveled gold face with white pixel label, for the 'primary vs secondary' hierarchy the mockups show. | 48x48 px, 18px corner margin. Gold #d4af37/#ffe7a8 with bevel highlight + shadow. Lossless.
  - corner_gem_gold.png: Small gold gem/jewel ornament TextureRect placed at the 4 corners (overhanging) of JRPGDialog/header panels for ornate filigree. | 16x16 px pixel gem, gold #ffe7a8 highlight + #8a5428 shadow facet, transparent bg. NEAREST filter.
  - jrpg_pixel_jp.ttf (or .fnt + atlas PNG): License-cleared JP-capable pixel/bitmap font replacing the SystemFont. Must cover kana + the kanji used in screens (釣り/港/料理/市場/ 図鑑). Loaded as FontFile with allow_system_fallback=false. | 8x12 or 16px monospace-ish pixel glyphs, full kana + game-required kanji subset. If a true JP pixel font is out of scope, fall back to M+ / Noto Sans JP rendered with filtering OFF (documented decision). texture_filter=NEAREST, generate_mipmaps=true, antialiasing NONE (or MSDF with #109757 fallback test).
  - bg_sea_bright.png: Baked low-res pixel-art sea+sky backdrop for shell screens (bright cheerful ocean mood), integer-scaled to fill 1280x720. Replaces flat #07172b/#0b2840 ColorRects. | Authored at 320x180 (integer 4x scale to 1280x720) OR 640x360. Stepped palette: sky #8ec9e8, sea-surface #3d8fc4, sea-deep #1f5e8c, foam #eaf6ff. ~24 colors, optional dithered cloud band. NEAREST, no mipmaps.
  - cloud_pixel.png + sparkle_pixel.png: Small pixel sprites for drifting clouds and sun-sparkle particles emitted by CPUParticles2D over the shell sea background. | Cloud 32x16 px (3-4 white/grey tones, transparent). Sparkle 4x4 px (white #eaf6ff + faint cyan). NEAREST, no mipmaps.
  - icon_atlas.png: Single 256x256 (or 512x512) atlas of 32x32 menu/status icons sliced via AtlasTexture: coin, rod, fish, sun, cloud, wind, knife/fork, market-stall, tackle-shop, book/zukan. | 32x32 cells, 8x8 grid = 256x256. Limited per-icon palette (~8 colors), pixel-art, transparent bg. NEAREST, Lossless, no mipmaps.
  - 08_asset_licensing_ledger.csv: Compliance ledger proving every .png/.ase is original art (docs/08 rule). Columns: asset_path, type, author, created_date, source=original, license=proprietary-original, references_used, ai_assisted, review_status. | CSV, machine-readable; paired with an EditorScript scanner that flags any res://assets/ file missing a row.

