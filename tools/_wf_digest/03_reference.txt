### 01 水上の釣り画面 (surface fishing / cast) — 釣りクエスト ～海釣り編〜 reference mockup
imageRead: partial | confidence: null
ART: TARGET (from docs): bright cheerful sea + 16-bit retro PIXEL ART (GBA/SNES fishing-JRPG lineage, e.g. Legend of the River King / 川のぬし) + ornate JRPG-style UI (warm window-skin menus). Mood is sunny, outdoor, optimistic — a daytime cast scene with a calm sparkling ocean and a cozy information layer. CRITICAL GAP vs current code: the current implementation is SMOOTH VECTOR / painterly (StyleBoxFlat rounded panels, GradientTexture2D skies, ellipse-drawn fish, Hiragino Maru Gothic system font). It is NOT pixel art and NOT a distinct JRPG window-skin. The mockup's professional look comes from: (1) a genuine low-res pixel-art scene (integer-scaled sprites, limited palette, no anti-aliasing), and (2) ornate bordered UI panels that read as a 'window skin' (beveled/double-line gold-on-wood frames, bitmap pixel font), not flat parchment rectangles. The art direction is the single biggest gap to close.
POLISH SIGNALS:
  - PIXEL CONSISTENCY: every world asset (sky, sea, dock, player, fish, icons) rendered at the same low internal resolution and integer-scaled with NEAREST filtering — no mixed smooth/pixel elements. This is the single most important 'professional' signal.
  - TRUE 9-SLICE WINDOW-SKIN FRAMES: ornate double-line gold-on-wood panel borders with corner ornaments, replacing flat StyleBoxFlat. This is what makes it read 'JRPG' instead of 'generic flat UI'.
  - CRISP pixel drop shadows (offset duplicate, not blurred shadow_size) under sprites and panels.
  - BITMAP/PIXEL FONT for titles and HUD numbers, with a 1px dark outline, tabular figures for currency/sizes.
  - GAUGE JUICE: integer-snapped fill, 3-tone cel gradient, a brief flash/tween when a threshold is crossed (tension entering red), and a thin top highlight line (gauge_bar.gd already has this — keep).
  - WATER LIFE: animated sun-sparkle particles, an expanding ripple ring where the line enters the water, and gentle parallax wave bands. Static water is the biggest atmosphere tell.
  - CONSISTENT INSET + GRID: all panels share the same margin and corner radius; header bands are uniformly dark navy with uniformly gold titles — visual rhythm.
  - LIMITED, DISCIPLINED PALETTE: a fixed ~16-32 color palette per scene (sky blues, sea blues, wood, parchment, gold, gauge red/green) rather than per-element arbitrary gradients.
  - PRESSED-BUTTON FEEDBACK: a 1px down-shift + color darken + tiny scale pop on press, so the cast/reel actions feel tactile.
UNCERTAINTIES:
  - The MCP image tool returned a plausible description but with specifics that match known hallucination patterns: exact currency '12,450 G', fish species 'No.017 メジナ', a 'lighthouse', button text 'リリース操作', and a 'ビット!' catch exclamation. I could NOT independently verify these against the actual pixels (Read only confirmed the file is a valid uploadable PNG, it did not return a visual parse I can trust line-by-line). Treat ALL specific text labels, exact numbers, and the lighthouse as UNVERIFIED — likely invented by the tool.
  - Exact hex values for sky/sea are best-guesses grounded in 'bright sea' art direction, NOT measured from the image. Do not treat #8ec9e8 / #3d8fc4 / #1f5e8c as ground truth; sample the real pixels or an art direction doc before locking tokens.
  - Whether the mockup's fish on THIS screen is a splash, an underwater shadow, or a full sprite is unclear — the detailed fish arguably belongs to screen 02. I assumed a shadow/ripple hint, which fits 'cast' semantics.
  - The tool claimed health bar = red and stamina = green with specific fill percentages; current code uses green fills. The red-tension / green-stamina semantic is plausible for a fishing fight but not confirmed from the image.
  - Panel 'rounded corners' was claimed by the tool, but JRPG window-skins of this lineage are often SQUARE with beveled corners. Whether the mockup uses rounded or square 9-slice corners is unverified — recommend square/beveled for authenticity unless the image clearly shows rounding.
  - I did not confirm the presence/absence of a minimap, seagulls, clouds, or a 'lighthouse'; the tool asserted them but they are typical hallucination filler. Verify against the actual mockup before building assets.

### 02 水中ファイト画面 (underwater fight — signature/看板 screen)
imageRead: partial | confidence: medium-high on composition, palette, and rendering-style GAP analysis (grounded in the actual source files). Low on any reference-specific exact values (clock, currency, catalog numbers, garbled labels) which the image tool likely fabricated and which the local image could not be pixel-inspected.
ART: A cheerful, bright-sea underwater fight scene framed by JRPG-style ornate window panels — the intended quality bar is GBA/SNES-era fishing JRPG lineage (Legend of the River King / 川のぬし). The mood is tense-but-bright: a deep, glowing water column as the visual centerpiece with soft god-rays and rising bubbles, surrounded by warm, beveled UI chrome. INTENDED: retro PIXEL-ART sprites + bitmap fonts + an ornate 9-slice JRPG window-skin. ACTUAL (the GAP): the current build is SMOOTH VECTOR / painterly — gradient strip water, ellipse-based fish with cel-style band shading, StyleBoxFlat rounded panels, system Hiragino Maru Gothic font. It is clean and readable but is NOT pixel art and has no distinct JRPG window-skin; the single biggest delta from the reference is the absence of a pixel-art pipeline (no nearest-scaled pixel sprites, no bitmap font) and the absence of an ornate/beveled frame treatment. Atmosphere (god-rays, bubbles, vignette frame, depth scale, name badge) already exists in code and is broadly on-mood — the polish gap is rendering-style, not composition.
POLISH SIGNALS:
  - PIXEL ART PIPELINE (top priority): hand-author fish (multi-frame AnimatedSprite2D), bait, seaweed, rocks, bubbles, bg-fish as low-res pixel sprites; render via a low-res canvas upscaled NEAREST (or texture_filter=NEAREST on each Sprite2D). This single change closes the largest quality gap.
  - JRPG ORNATE WINDOW-SKIN: replace StyleBoxFlat badges/panels with 9-slice TextureRect frames featuring beveled gold corners + inner trim line (the 'enemy card' badge + HUD panels). Define as a reusable Theme StyleBoxTexture.
  - BITMAP/PIXEL FONT for all HUD numbers + labels (depth, distance, gauge values, fish name) — smooth maru-gothic is the most visible 'not retro' tell.
  - DITHERED WATER GRADIENT: replace hard-edged 20-strip lerp with ordered Bayer dither or a baked dither texture so the depth fade reads as pixel-art atmosphere, not vector banding.
  - GAUGE JUICE: animate gauge fills (ease on value change), add a shimmer sweep across the fill, color-code per stat (tension=cyan/gold, stamina=green, fishHP=red→yellow), add a pulsing critical-state glow when low.
  - FISH HIT-REACT: screen-shake + white hit-flash + particle scale-burst on fish state changes (charge/jump), plus the existing tension aura intensifying.
  - GLOW ACCENTS: additive-blend Sprite2D for god-rays and the depth-marker dot; bloom-like double-draw (bright core + large faint halo) on the eye glint and current-depth marker.
  - INNER GOLD HAIRLINE: 1px inner gold/cream border inside the vignette frame for that crisp JRPG window edge.
  - CONSISTENT PIXEL GRID: snap all _draw coordinates to integers (roundf) and align the internal pixel scale so sprites and drawn shapes share one pixel lattice — avoids the current 'smooth vector + would-be pixel' clash.
  - WARMER BRIGHT-SEA TINT: nudge surface cyan toward a slightly warmer, more saturated 'tropical' blue and sand toward warm beige to better match the 'bright sea / cheerful' stated direction (current sand #9ab68c reads green).
UNCERTAINTIES:
  - MCP tool returned several exact values that are almost certainly HALLUCINATED and are NOT in the codebase: clock '08:47', currency '12,450 G', fish catalog 'No.028 / クロダイ / 44.2cm', and garbled labels '約の仲魚', 'ビット!' (likely intended 食いついた/bit), '突っ込む', '海水', '満月', '快晴'. The real code labels are 行動, 距離, 現 (depth). Treat any mockup-specific labels/numbers as unverified — the reference IMAGE defines layout+mood only per docs/03 §6.
  - I could not view the raw pixels directly (local Read only echoed the CDN URL back); analysis is grounded in the MCP description CROSS-CHECKED against underwater_view.gd + ui_theme.gd, so structural/composition claims are reliable but exact reference hex values are best-guesses.
  - The MCP claimed 'ornate wooden 9-slice borders with brown #8B4513 + navy accents' on panels — this is plausible for JRPG chrome but I could not confirm from the mockup pixels; the CODE currently uses flat gold #d1aa63 borders, so the ornate-frame claim is aspirational/uncertain rather than observed.
  - Exact fish species/color in the reference could not be confirmed; code defaults to #8aa7b5 but fish_data.color overrides per fish.
  - MCP described both 'smooth gradient' and 'pixel-perfect scales' for the fish — internally contradictory; per stated art direction the INTENT is pixel art, current reality is cel/vector. Logged the gap rather than asserting the mockup is pixel.

### 03 調理・食事・レベルアップ画面 (cooking / meal / level-up)
imageRead: partial | confidence: medium
ART: The full-size 2.15MB PNG (1402x1122, 8-bit RGB, non-interlaced) was rejected by the analyze_image MCP tool with a persistent "1210 format/parse error" even after one retry with a short prompt. Downscaling to 999x800 (~1.2MB) and re-running got the tool working: two independent passes agreed on the robust facts (pixel-art rendering; warm palette of browns/golds/creams; a dish-icon grid; a large featured/selected dish illustration; a character sprite; a 'LEVEL UP!' indicator; stat bars; a currency counter; ornate dark-bordered JRPG windows; a blocky pixel/bitmap font). The two passes DIVERGED on glow/sparkles/steam (one claimed them, the other explicitly said 'no visible glow/sparkle/particle'), and the tool fabricated over-specific details I am treating as unverified (exact sprite sizes like '8x16/16x24', precise hex codes like #F0E6D2/#D4AF37, 'subpixel anti-aliasing', a '2x3 grid of 6 dishes', '0.5px pulse animation'). All palette hexes below are therefore HUE-FAMILY best-guesses, not sampled values. Consistent with stated art direction: retro pixel art + JRPG-style ornate window-skin + warm lived-in mood (Legend of the River King / 川のぬし lineage). Mood = cozy rustic kitchen/tavern, inviting and nostalgic, not the bright open sea of screens 01/02 — this is the indoor 'reward' beat.
POLISH SIGNALS:
  - Ornate pixel-art 9-slice window-skins (gold outer line + brown bevel + corner ornaments + drop shadow) instead of flat StyleBoxFlat rectangles — the single biggest 'professional JRPG' signal
  - Pixel-art dish/food icons in a uniform bordered grid (current uses plain text ItemList rows — most visible downgrade)
  - A large featured-dish illustration as a real focal element (current = text only)
  - A dedicated, framed LEVEL-UP window with old→new level, before/after stat bars, and a boss-unlock callout at Lv.5 (current = one appended text line)
  - Retro pixel/bitmap typography with outlines + drop shadows (current = smooth system font)
  - Warm textured paper/wood-grain background instead of a flat solid fill
  - Consistent palette harmony: cream parchment + warm brown + gold accent + one deep-blue contrast panel, repeated across every window
  - Small character/chef sprite for lived-in JRPG presence (current has none)
  - Beveled gold primary button vs muted secondary (current = uniform wood-tone buttons)
  - Uniform icon/style discipline — every dish icon shares the same circular bordered frame and pixel treatment (cohesion reads as 'designed', not 'assembled')
  - Per-element drop shadows + beveled edges give flat panels a tactile 3D weight (current shadows exist but borders are single-tone flat)
UNCERTAINTIES:
  - EXACT layout grid: tool gave two different region counts/splits (one said 'top split 30/70 + middle bar + bottom', another said '5 panels: top-left grid, top-right dish, mid-left character, mid-right stats, bottom HUD'). The 3-column-intent from docs/03 §4 is the reliable anchor; precise sub-panel proportions are unverified.
  - GLOW / SPARKLES / STEAM particles: two passes directly contradicted each other (one described gold sparkles + steam + dish glow + vignette in detail; the other said explicitly 'no visible glow/sparkle/particle'). Do NOT treat these as confirmed in the source image — treat as optional polish to add.
  - Exact pixel dimensions (sprite 8x16/16x24, dish icons 80x80, featured dish 200x120, level-up panel 300x150, '0.5px pulse'): these are classic tool-hallucination shapes — fabricated specificity, not sampled.
  - Precise hex values (#F0E6D2, #2A2A2A, #1E3A8A, #D4AF37, #FFF9C4, #00BFFF, #808080): all are hue-family best-guesses from a lossy downscaled analysis, NOT sampled from the 1402x1122 original. Treat as direction, not spec.
  - 'Subpixel anti-aliasing' on the sprite — implausible for pixel art; likely hallucinated. Pixel art should be crisp (filtering off).
  - Number of dishes in the icon grid ('2x3 = 6') is unverified — could be more or fewer.
  - Whether the bottom is truly charcoal-neutral vs a warm dark brown — tool said charcoal #2A2A2A but the overall mood is warm; could be a warm dark brown instead.
  - The full-resolution (1402x1122) image could never be analyzed — only the 999x800 downscale. Fine layout details readable only at full res (exact corner-ornament shapes, sub-pixel bevel layering) remain unconfirmed.

