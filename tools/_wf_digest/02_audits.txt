### 水中ファイト (Underwater Fight — signature screen)
WEAKNESSES:
  - NO pixel-art pipeline exists: every visual is antialiased vector (_draw circles/polygons, StyleBoxFlat, GradientTexture2D). There are zero .png sprites — assets/ contains only icon.svg. The fish, seabed, rocks, seaweed, bubbles, background fish, and UI are all smooth painterly shapes, the opposite of the retro pixel-art target.
  - No JRPG window-skin / 9-slice frame texture: windows are plain rounded StyleBoxFlat rectangles. There is no ornament — no scrollwork, no gem/jewel corner accents, no rivets, no beveled border tiles. ui_theme.gd _style() only knows bg_color + 1 border color + corner_radius + shadow.
  - Typography is a smooth anti-aliased SystemFont (Hiragino Maru Gothic ProN) with generate_mipmaps=true, not a bitmap/pixel font; there is no FontFile/.ttf in the project. Text at small sizes is blurry-vector, not crisp pixel glyphs.
  - underwater_view._draw() allocates StyleBoxFlat.new() and PackedVector2Array every frame inside _draw_fight_overlay (badge_style, track_style, fill_style) — per-frame GC churn on the signature screen.
  - The whole scene re-queues redraw every _process frame and recomputes the 20-strip gradient and 26-band fish body each time — wasteful since most of it is static geometry that could be cached or baked to a texture.
  - The fish, while detailed, is a generic banded ellipse — it does not reflect a specific species (kurodai/sea bream) with characteristic proportions, and boss/non-boss only differ by a scale multiplier and a faint aura circle. One procedural fish = one look for the entire game.
  - Color palette skews cold-blue and somewhat muted (top #1a8fd0 → #062847); the mockup is a BRIGHT SEA with vibrant cyan light shafts and warm gold UI — current ocean reads flatter and darker than target.
  - Atmosphere lacks juice: no dithering, no sparkle/particle ambience in the water column, light shafts are a single flat-alpha polygon with no god-ray softness/volumetric banding, the fish has only a static faint aura (no hit-flash, no struggle shake, no scale pulse synced to stamina), and there are no screen-shake/hit-stop/squash feedback events during the fight.
  - No in-canvas pixel SFX/exclamations: the mockup's glowing 'ビット!' callouts and status icons are absent — the fight communicates only via gauges and text labels.
  - The vignette _draw_frame() is 8 nested draw_rect outlines — a cheap darkening, not an ornate frame, and it sits on top of the fish scene rather than being a UI chrome layer.
  - Result overlay is a flat 72% black ColorRect + plain panel — no celebratory/somber framing for catch-vs-escape, no sparkle on success.
GAP VS TARGET:
  [HIGH] Art style & pixel-ness: NOW=Everything is anti-aliased vector: _draw circles/polygons (underwater_view.gd _draw_target_fish/_draw_seabed/_draw_small_fish), StyleBoxFlat (ui_theme.gd), GradientTexture2D fills (gauge_bar.gd _rebuild_gradient). The 26-band fish is smooth-shaded, seabed rocks are draw_circle with offset circles, scales are 2.6px alpha dots. No .png sprites exist (assets/ = icon.svg only).  =>  TARGET=Chunky discrete-pixel sprites with visible pixel grid, hand-placed scale tiles, dithered shading (dot-matrix), limited per-sprite palettes — the mockup's unapologetic retro look.
  [HIGH] JRPG UI grammar / window-skin frames: NOW=Windows are bare rounded StyleBoxFlat rectangles: bg_color + single border_color + corner_radius + optional shadow (ui_theme.gd _style(), screen_base.gd make_panel). The fight badge is one gold-bordered StyleBoxFlat (underwater_view.gd L376-383). No 9-slice textures, no ornaments, no gems/rivets/scrollwork/corner bevels.  =>  TARGET=Ornate 9-slice JRPG window-skin: dark borders with repeating pixel scrollwork, rounded corners with 1px bevel/emboss, gold gem/jewel accents at frame top-center, rivet/claw motifs at corners — 'treasure-chest' fantasy frames.
  [MED] Color & palette: NOW=Ocean is a 20-strip lerp #1a8fd0→#0e5f9c→#062847 (underwater_view.gd L41-43) — competent but skews cold/muted; UI is warm parchment/wood (ui_theme.gd). Palette is internally consistent but not vibrant.  =>  TARGET=BRIGHT SEA: vibrant deep-teal-to-cyan water, bright cyan god-rays, warm gold UI accents and coins, with chunky limited per-element palettes and intentional dithering between steps rather than smooth lerp.
  [HIGH] Typography: NOW=SystemFont chain (Hiragino Maru Gothic ProN…) via ui_theme.gd build_font() with generate_mipmaps=true; no .ttf/.otf in project. In-canvas text uses draw_string/draw_string_outline with get_theme_default_font(). Smooth, anti-aliased, modern-looking.  =>  TARGET=Bitmap/pixel font (chunky 8x8–10x10 style) with crisp jaggies, high-contrast white-on-dark, weight hierarchy for titles vs body — the mockup reads as unmistakably retro text.
  [MED] Atmosphere & lighting: NOW=5 flat-alpha light-shaft polygons with sin sway (underwater_view.gd L52-64), single surface highlight line, 8-rect vignette _draw_frame(), faint static fish aura circle (L256). No dithering, no volumetric banding, no ambient sparkle/particles.  =>  TARGET=Dramatic directional god-rays with soft glow and dithered refraction edges, bright cyan beams, depth-driven ambient darkening, pixelated sparkle/particle ambience, and a cyan glow around the focal fish.
  [HIGH] Animation & feedback (juice): NOW=Continuous procedural motion only (sway/rise/loop via sin+fmod in _process). Fight state shows via gauge values and text; fish only gets a faint aura + stamina scale lerp (underwater_view.gd L242). No hit-flash, shake, hit-stop, squash, exclamations, or success/escape flourish.  =>  TARGET=Per-event juice: hit flashes on the fish, screen-shake/hit-stop on big strikes, stamina-synced scale pulsing, glowing 'ビット!'-style callouts, sparkle bursts on catch, and a distinctly framed escape sequence.
  [HIGH] Asset quality / specificity: NOW=One generic procedural banded-ellipse fish; species differ only by a color hex + scale multiplier + boss aura (underwater_view.gd _draw_target_fish, Color.from_string at L245). Background fish are circle+tail polys. Bait/line/rods have no art.  =>  TARGET=Per-species pixel sprites (e.g. a recognizable sea bream/kurodai) with characteristic proportions, detailed hand-drawn scales/fins/eye, distinct bosses; pixel-art bait/lure/rod icons; background fish as proper small sprites with silhouettes.

### シェル/テーマ (title, harbor, nav, theme)
WEAKNESSES:
  - No pixel-art pipeline exists: assets/ contains only icon.svg; there are zero .png sprites, tilesets, or 9-slice frame textures anywhere. Every shape is a vector StyleBoxFlat or _draw primitive, so the art is smooth/anti-aliased painterly, the opposite of the stated retro pixel-art direction.
  - UI chrome is generic Godot containers, not a JRPG window-skin: there are no ornate 9-slice border textures, no corner ornaments, no beveled gold filigree, no inset content troughs. A PanelContainer with a 3px border + 12px radius (ui_theme.gd _style) reads as a modern flat dashboard, not a warm ornate menu.
  - Typography undermines the genre: a SystemFont (rounded sans Hiragino Maru Gothic, ui_theme.gd build_font lines 80-84) with no bitmap/pixel font and no imported .ttf. JRPG UIs are defined by their stylized type; a system sans makes everything feel like a prototype.
  - No atmospheric layers: backgrounds are a single ColorRect (title #07172b, harbor #0b2840) or a 2-stop vertical gradient (screen_base.gd add_gradient_background). No parallax sky/sea, no clouds, no God-light, no particles, no animated water — the 'bright sea' mood is absent on the shell screens.
  - No juice / feedback animation: screens pop in instantly on navigate (main.gd _show_screen removes + queue_free + add_child with no transition), buttons rely on default hover/pressed StyleBox swaps only, no tween shake/scale/pulse, no SFX hooks visible. Static and lifeless versus mockup polish.
  - Inconsistent palette discipline between shell and content: shell screens are dark navy (#07172b/#0b2840) while the theme promises bright sea + parchment — the title/harbor feel muted/cool rather than bright/cheerful, clashing with the documented art direction.
  - Gradient backgrounds use a 64x64 GradientTexture2D stretched full-rect (screen_base.gd lines 42-49) — banding-prone and visually flat at 1280x720; no dithering, no noise, no layered depth.
  - main.gd hardcodes the screen registry in a match statement (_show_screen lines 28-45) and instantiates by .new() with no preloading/caching beyond const preloads — adequate but offers no room for transition animation layers or shared transition manager.
GAP VS TARGET:
  [HIGH] Art style & pixel-ness: NOW=Entirely smooth vector: StyleBoxFlat solid fills + uniform borders + large anti-aliased corner radii (ui_theme.gd _style lines 89-116), GradientTexture2D backgrounds, and _draw ellipses/polygons in components. default_texture_filter is already NEAREST (project.godot) but feeds zero pixel assets.  =>  TARGET=Retro pixel art: hand-authored low-res sprites/tilesets upscaled crisp via the already-set NEAREST filter, with consistent pixel scale and intentional color-count-limited palettes.
  [HIGH] JRPG UI grammar / frames: NOW=Generic PanelContainer with 3px border + 12px radius and a soft drop shadow; BluePanel/DarkPanel are just recolored flat boxes (ui_theme.gd lines 22-24, 73-76). No 9-slice ornate window-skin, no corner flourishes, no inner troughs, no divider rules, no icon slots.  =>  TARGET=Distinctly JRPG ornate window-skin: gold-beveled 9-slice frames with corner ornaments, inset content panels, decorative dividers and slot frames — the warm-menu lineage of Legend of the River King.
  [MED] Color & palette: NOW=Two disconnected moods: theme defines warm parchment/wood/gold (ui_theme.gd) but shell screens paint dark cool navy backgrounds (title #07172b, harbor #0b2840) that read muted rather than bright. Palette is ad-hoc per-screen, not a governed indexed set.  =>  TARGET=Bright, cheerful, cohesive sea palette — saturated sky/turquoise sea + warm sand/wood + gold, governed by a small indexed color set used consistently across art and UI.
  [HIGH] Typography: NOW=SystemFont fallback chain (Hiragino Maru Gothic ProN, ui_theme.gd lines 6-13, 80-84); no imported .ttf, no bitmap/pixel font. Font sizes are flat numeric (18/28/58) with no type scale or weight system.  =>  TARGET=A stylized JRPG-appropriate typeface (pixel/bitmap or characterful display font) as imported FontFile(s), with a defined type scale and consistent outline treatment matching the window-skin.
  [HIGH] Atmosphere & lighting: NOW=Shell backgrounds are a flat ColorRect (add_background) or a single 64x64 2-stop vertical gradient stretched full-rect (add_gradient_background, screen_base.gd lines 37-55). No parallax, no light shafts, no clouds, no animated water, no particles on title/harbor.  =>  TARGET=Layered bright-sea atmosphere: parallax sky+sea, drifting clouds, god-rays/surface shimmer, ambient particles — the cheerful luminous ocean mood from the mockups.
  [MED] Animation & feedback (juice): NOW=Zero transition between screens (main.gd _show_screen queue_free + add_child, no tween), buttons use only StyleBox hover/pressed swaps, no shake/scale/pulse, no SFX wiring in the shell files.  =>  TARGET=Screen transitions, button press feedback (scale/pop), header/gauge tweens, hover glow, and audio cues — the responsive, juicy feel of polished JRPG menus.
  [HIGH] Asset quality & density: NOW=assets/ holds only icon.svg — no sprites, icons, frame textures, particle textures, or fonts. All visual richness must be re-derived from primitives each frame, capping achievable detail.  =>  TARGET=A curated, original asset library (fish sprites, background art, frame/window-skin textures, icons, font) authored to mockup quality, imported and reused via the existing NEAREST pipeline.

### 調理・図鑑 (cooking_screen.gd + status_screen.gd)
WEAKNESSES:
  - Zero pixel art / retro aesthetic: every visual is a smooth vector StyleBoxFlat (gradients, rounded rects, anti-aliased text) — the antithesis of the stated 'retro pixel art + JRPG UI' direction; nothing is pixel-snapped or low-res
  - No JRPG ornate window-skin: frames are simple single-border StyleBoxFlat rounded rectangles with a drop shadow — no 9-slice ornamental borders, no corner ornaments, no inner bevel/gold trim, no decorative title-bar plates. They look like generic modern flat-design cards, not 川のぬし-style menus
  - No icons anywhere: fish list, recipe list, 図鑑, inventory, equipment, buffs are all pure text. There are no fish sprites/portraits in the encyclopedia, no item icons, no rarity gems/stars drawn as sprites — only the literal characters '★初回' or '[レア]'
  - System font (Hiragino Maru Gothic ProN) is anti-aliased rounded sans — wrong for a retro JRPG; no bitmap/pixel font, font_size overrides are arbitrary (18/19/20/24/26/27) rather than a designed type scale
  - Status 図鑑 and inventory are wall-of-text Labels — no grid, no per-fish cards, no hover/highlight, no sprites, no color-coded rarity. Visually it is a console log, not an encyclopedia screen
  - Cooking screen backgrounds are flat ColorRect (#172235 / #10243a) — no atmosphere, no 'bright sea' mood, no texture, no light; contradicts the BRIGHT SEA art direction more than the gradient screens do
  - Built-in ItemList and ProgressBar are used instead of styled custom controls — the ItemList in particular looks like an OS list widget, breaking immersion; the project's nicer GaugeBar is not leveraged for the EXP bar
  - No animation/feedback: cooking a meal just swaps label text in place (no flash, no particle, no EXP bar tween, no dish-reveal), selecting items has no transition — flat and static compared to mockup 'juice'
  - Color usage is muted/dark and somewhat muddy (#172235, #12283f) rather than the warm bright ocean palette the mockups call for; gold accents exist but are thin single-pixel borders, not bold ornament
GAP VS TARGET:
  [HIGH] Art style & 'pixel-ness': NOW=Entirely smooth vector: StyleBoxFlat rounded panels, anti-aliased SystemFont, gradient ProgressBar fills (ui_theme.gd lines 22-31). No pixel snapping, no low-res rendering, no dithering. cooking_screen.gd uses stock ItemList/ProgressBar; status_screen.gd is all text.  =>  TARGET=Retro pixel-art aesthetic matching GBA/SNES fishing JRPGs (川のぬし lineage) — crisp pixel-snapped rendering, integer-scale pixel art via CanvasTexture + texture_filter NEAREST in gl_compatibility.
  [HIGH] JRPG UI grammar / ornate frames: NOW=Panels are single-border StyleBoxFlat rounded rectangles with a drop shadow (ui_theme.gd _style lines 89-116; make_panel screen_base.gd 108-112). No 9-slice ornamental window-skin, no corner ornaments, no gold inner trim, no decorative title bars — reads as modern flat card UI.  =>  TARGET=Warm ornate JRPG window-skin: 9-slice TextureRect frames with beveled/gold-trimmed borders, decorative corner studs, ornamental header plates behind titles — the signature menu look of the reference mockups.
  [HIGH] Icons & asset quality (sprites): NOW=Zero sprites/icons. Fish list, recipe list, 図鑑 (status_screen.gd _build_fishbook_text 118-144), inventory (_build_collection_text 147-190), equipment and buffs are all plain text Labels; rarity shown as literal '[レア]' text, first-time as '★初回' text. No fish portraits in encyclopedia.  =>  TARGET=Original pixel-art fish sprites/portraits in 図鑑 cells, dish/recipe icons, item icons, rarity stars/gems as drawn sprites — all hand-made pixel art per docs/03 §6 (original, not copied).
  [MED] Color & palette: NOW=Muted dark-navy flat backgrounds (#172235 cooking line 16, #10243a status line 7) with thin gold accents; overall dark and somewhat muddy, contradicts 'bright sea'.  =>  TARGET=Bright cheerful ocean palette — warm sunlit sea blues/teals, bright foam whites, warm wood/parchment, vivid accents — matching the bright-sea + warm-menu mockup mood.
  [HIGH] Typography: NOW=SystemFont Hiragino Maru Gothic ProN (ui_theme.gd 6-13, 80-84), anti-aliased rounded sans; arbitrary font sizes (18/19/20/24/26/27/28) with no designed scale; outline_size used ad hoc for contrast.  =>  TARGET=Pixel/bitmap font (or Godot font with NEAREST + no-antialias) at a designed integer type scale, consistent across headers/body/numbers, evoking GBA-era JRPG text.
  [MED] Atmosphere & lighting: NOW=Flat ColorRect backgrounds — no gradient (add_background used, not add_gradient_background), no texture, no light shafts, no ambient particles on either screen. cooking (#172235) and status (#10243a) are the flattest, darkest screens in the app.  =>  TARGET=Layered atmospheric backdrops: gradient sea/sky, soft light, subtle ambient motion/particles, warm panel materials — 'bright sea' mood even on menu screens.
  [HIGH] Encyclopedia / 図鑑 presentation (status-specific): NOW=Single multi-line make_label inside a ScrollContainer (status_screen.gd 79-85, 118-144) — a wall of text with '？？？未発見' placeholders; no grid, no cells, no sprites, no rarity color coding, no per-entry framing.  =>  TARGET=Grid of illustrated fish-dex cells with sprite + name + rarity badge + size record, locked silhouette for undiscovered, hover/selection states — a proper 図鑑 screen.
  [MED] Animation / feedback / juice: NOW=Purely static state swaps: cooking (_cook_selected 218-242) replaces label text in place with no tween/particle/flash; EXP bar jumps; item selection has no transition; status screen has no motion at all.  =>  TARGET=Juicy feedback: EXP bar tween + sparkle on cook, dish-reveal pop, level-up burst/banner, item-select highlight transitions, panel entrance tweens — matching mockup polish.
  [MED] Control skinning vs stock widgets: NOW=Uses Godot's built-in ItemList (cooking lines 60-64, 76-80) and ProgressBar (lines 35-39) themed only via StyleBox — they render as flat OS-style list rows; the project's nicer custom GaugeBar (components/gauge_bar.gd _draw) is unused here.  =>  TARGET=Custom-styled list/grid rows with JRPG framing and per-item icons, and the custom GaugeBar (or a pixel-styled equivalent) for EXP — consistent bespoke controls, not stock widgets.

### 市場・釣具店 (market, shop)
WEAKNESSES:
  - ItemList is completely unthemed: ui_theme.gd sets styleboxes for PanelContainer/Panel/Button/LineEdit/OptionButton/ProgressBar/AcceptDialog but NOT for ItemList. Both screens lean on a built-in ItemList that falls back to Godot's default gray flat theme, so the central interactive element visually breaks with the parchment/wood panels around it (sharp seam between themed panels and default-grey list).
  - Both screens use add_background() (flat ColorRect) instead of add_gradient_background() — market #163348 and shop #2a2730 are dead flat fills, giving zero depth/atmosphere vs the bright-sea mood target; the shop background #2a2730 is a muddy grey-brown with no sea or warmth.
  - No pixel-art pipeline whatsoever: there are no Sprite2D/TextureRect icons for fish or rods, no .png/.tex art under assets/ (only icon.svg), and no texture_filter = NEAREST / canvas_item snap settings anywhere. Every item row is plain text, so the 'retro pixel art + item icons' of the reference is entirely absent.
  - No JRPG window-skin / 9-slice ornate frame: panels are single StyleBoxFlat rectangles with a plain uniform border. There is no corner ornament, no double-border, no beveled gold trim, no header-tab chrome — the mockup-quality ornate menu frame is not represented at all.
  - Typography is a macOS system font (Hiragino Maru Gothic ProN) rendered smoothly with generate_mipmaps — explicitly NOT a bitmap/pixel font, so headings and rows look like a modern web UI rather than GBA/SNES-era JRPG type. ui_theme.gd:6-13 even notes a future .ttf swap that has not happened.
  - Zero animation/feedback/juice: selection changes are instant text swaps, sales/purchases just rewrite _result_label.text, buttons have no tween/pulse, no coin-fly or item-flash, no tween on panel entry. The result panel is static.
  - Near-duplicated structure between the two files (header/money/body/result scaffolding is ~identical) with no shared ShopLayout component, so visual improvements must be made twice and drift is likely.
  - Flat color choices inside panels: detail panels are solid parchment with a single text block — no rarity color-coding for fish, no stat bars for rods (reel/line/technique are plain '+12%' text), no visual hierarchy beyond font size, wasting the chance to convey equipment quality at a glance.
  - Hard-coded layout numbers (430px list width, 68/82px panel heights, font sizes 19/21/26/28) with no responsiveness to the 1280x720 base or expand-aspect behavior; on other resolutions the list/detail ratio and label sizing are unvalidated.
GAP VS TARGET:
  [HIGH] Art style & pixel-ness: NOW=100% smooth vector: StyleBoxFlat rectangles, system-font text, flat ColorRect backgrounds. No Sprite2D/TextureRect art, assets/ holds only icon.svg, no nearest-neighbor texture filtering or pixel snap configured anywhere in the cluster.  =>  TARGET=Retro pixel-art fish sprites, rod/item icons, pixel window-skin — original art made from scratch at a fixed low internal resolution with NEAREST filtering and integer/consistent pixel grid.
  [HIGH] JRPG UI grammar / window-skin frames: NOW=Single flat StyleBoxFlat panel per region with a plain uniform brown border and rounded corners; no 9-slice ornate frame, no corner ornaments, no double/beveled gold trim, no header tab. The central ItemList is not even themed (falls back to Godot default grey), breaking the window-skin illusion at the most interactive element.  =>  TARGET=Distinct JRPG window-skin: ornate 9-slice border textures with gold corner caps, beveled/embossed edges, ornamental header band, themed list rows with selection highlight matching the skin — the warm ornate menu look of GBA/SNES fishing JRPGs.
  [HIGH] Color & palette: NOW=market flat #163348 and shop flat #2a2730 dead backgrounds; parchment/gold/brown accents are warm but the screen backdrops are muddy and lifeless with no sea or sky gradient. add_background (flat ColorRect) is used instead of add_gradient_background.  =>  TARGET=Bright cheerful sea palette — warm sun-lit horizon, turquoise/blue gradients, sandy golds — evoking a bright ocean fishing town, not a flat dark slab.
  [MED] Typography: NOW=SystemFont Hiragino Maru Gothic ProN with generate_mipmaps = smooth anti-aliased modern sans; sizes 19/21/26/28; outline_size used for contrast. ui_theme.gd:6-13 explicitly flags a future .ttf swap that hasn't happened.  =>  TARGET=Bitmap/pixel font (or pixel-rendered TTF with NEAREST, no mipmap blur) with JRPG-appropriate weighting and consistent pixel grid, especially for headers and money figures.
  [HIGH] Atmosphere & lighting: NOW=No background art, no gradient sky/sea, no decorative shop/market scene (no market stall, no tackle-shop backdrop, no hanging fish, no sparkles). Soft drop shadows on panels are the only depth cue.  =>  TARGET=Scene-setting background (bright market stall / tackle shop interior or sea-view), ambient decoration, light/sparkle accents that sell the cheerful fishing-town mood from the mockups.
  [MED] Animation / feedback / juice: NOW=Instant text swaps on selection, sell, buy; result just rewrites _result_label.text; no tweens, no panel entry animation, no button pulse, no coin-fly or item-flash, no sound (assets/ has no SFX).  =>  TARGET=Snappy JRPG feedback: selection cursor slide/blink, panel open/close tweens, money-count-up tween, item flash on sale, button squash/stretch, audio cues — the juice expected at mockup polish.
  [HIGH] Asset quality (icons / item art): NOW=Item rows are pure text ('魚名 × n （1匹 p G）', '竿名 marker'); no fish thumbnails, no rod icons, no rarity glyphs. Rod stats are presented as '+N%' strings with no stat bars or visual hierarchy.  =>  TARGET=Pixel-art fish thumbnails per row, rod/equipment icons, rarity color-coding and visual stat bars so equipment quality and catch value read at a glance.
  [MED] Theming completeness / consistency: NOW=ui_theme.gd themes Panel/Button/LineEdit/OptionButton/ProgressBar/AcceptDialog but omits ItemList entirely; the two screens duplicate ~identical scaffolding with no shared ShopLayout component, risking visual drift.  =>  TARGET=Every control on the screen (including ItemList rows, scrollbars, selected-state) styled by the single JRPG window-skin theme, with a shared layout component so market and shop stay perfectly consistent.

