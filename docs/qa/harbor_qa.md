# 港画面 QA判断ログ

最終更新: 2026-07-08 / 状態: E5時間帯セレクタ 採用
参照画像: なし（既存港画面への新UIブロック追加）
QA更新コマンド: `./tools/harbor_visual_qa.sh`

## 1. freeze値（正本）

| 項目 | 値 | 場所 | 理由・備考 |
|---|---|---|---|
| 時間帯セレクタ配置 | 「今日の支度」カード下段に `時間帯` ラベル + 3ボタン横並び。上段の支度文とサメ餌魚ボタンは維持 | `src/ui/harbor_screen.gd` | E5追加ブロック。既存サメ餌魚UIと導線を消さず、Lvロック/選択中を同じカードで確認できる |
| ヘッダー状況行 | `時間帯：{選択中}　潮位：満ち始め　風：弱` | `src/ui/harbor_screen.gd` | 潮位/風は飾りのまま、時間帯だけ実値にするE5仕様 |
| 港時間帯グレード | 朝まずめ=暖色薄乗せ、日中=なし、夜釣り=寒色暗め薄乗せ | `src/ui/harbor_screen.gd` / `src/ui/palette.gd` | 新規背景素材は作らず、まず色グレードだけで成立するかを確認 |

## 2. 不採用・再試行禁止リスト

| 案 | 却下理由 | 判断日 |
|---|---|---|
| 時間帯ごとの港背景PNGを先に作る | E5はまずグレーディング検証から始める仕様。現時点では既存背景に薄い朝/夜差を乗せれば導入意図は伝わる | 2026-07-08 |

## 3. 微調整カウンタ

| パラメータ | 回数 | 直近の変更内容 | 状態 |
|---|---|---|---|
| 装飾パス累計 | 1 | 背景上に時間帯グレードColorRectを追加 | 採用 |
| 今日の支度カード配置 | 1 | 支度カードを縦に広げ、上段=支度/サメ餌魚、下段=時間帯に整理 | 採用 |

## 4. 暫定判定・再検証TODO

なし。

## 5. 現在の残ギャップ

- 夜釣りの港背景は専用夜景ではなく寒色暗めグレード。E5 v1では採用し、専用夜背景が必要になった場合は素材ブリーフへ送る。

## 6. フェーズスコープ宣言（作業中のみ）

完了済みのためなし。

## 7. 判断ログ（直近パスのみ）

2026-07-08 E5時間帯セレクタを採用。

変更したもの:
- 「今日の支度」カードに時間帯3ボタンを追加し、未解放Lv/選択中を表示する。
- ヘッダー状況行へ選択中の時間帯を表示する。
- 港背景へ朝まずめ/夜釣りの薄い色グレードを追加する。

変更していないもの:
- 港施設リスト、サメ餌魚の初期選択ボタン、食事効果カード、釣り場導線、潮位/風の実データ化。

判断根拠:
- `docs/qa/evidence/harbor/2026-07-08_e5_time_slot_compare.png`

採用理由:
- 原寸スクショで時間帯ボタン、ヘッダー行、既存サメ餌魚ボタンに見切れ・重なりがない。
- 朝/日中/夜釣りの選択差が港画面内で確認できる。
