# Underwater Fight Design QA

final result: blocked

Date: 2026-06-25

Source visual truth: `reference/02_underwater_fight_mockup.png`
Implementation screenshot: `/tmp/tsuri_fishing_fight.png`
Full-view comparison evidence: `/tmp/tsuri_fight_compare.png`
Static full-view comparison evidence: `/tmp/tsuri_full_static_compare.png`
Focused frame comparison evidence: `/tmp/tsuri_frame_focus_compare.png`
Focused fish/hit comparison evidence: `/tmp/tsuri_fish_hit_focus.png`
Side-by-side page: `/tmp/tsuri_fight_compare.html`
Viewport: 1280x720
State: underwater fight, kurodai hit moment, depth 18.6m, action `突進`

## Patches Made Since Previous QA

- Increased the main kurodai presence without reopening the frozen background. `UnderwaterView` and `/tmp/tsuri_full_static_compare.png` now use `SHOWCASE_FISH_CENTER_OFFSET = Vector2(-0.058, 0.006)`, a 0.525 water-window draw-width ratio, and a slightly wider/lower low-alpha contact shadow, moving the hit cluster closer to the reference's heavier fish/event read while keeping the lure, line, distance chip, and full raster hit badge readable.
- Strengthened the top-status parchment material in `top_status_frame.png` without moving runtime/static text or icons. The first three paper cards now have a stronger browned edge patina, heavier outer/inner paper rules, brighter top highlights, darker bottom edges, and clearer gold corner brackets; `/tmp/tsuri_top_status_static_compare.png` shows less flat white-card read while preserving the current slot geometry.
- Retuned the lower-HUD operation-card proportions in `fight_hud_frame.png`, `FightHud`, and `/tmp/tsuri_hud_static_compare.png`. The frame-backed menu lane now uses 17.5% of the lower row instead of 19.0%, giving the operation hint a wider paper well; A/B caps are slightly narrower, A/B labels rise to 15px, and the static/runtime hint slots start closer to the paper edge. The menu rows shift left so `ポーズメニュー` and `釣りをやめる` remain inside the narrower navy card.
- Recentered the main kurodai/hit cluster slightly in `UnderwaterView` and `/tmp/tsuri_full_static_compare.png`. `SHOWCASE_FISH_CENTER_OFFSET` now moves the fish less aggressively left/up (`Vector2(-0.045, -0.004)`), and `HIT_BURST_CENTER_RATIO` drops the full raster hit badge to 49%/80% with a slightly larger 0.50-0.58 scale range. The focused comparison keeps the line, lure, distance chip, and fish readable while reducing the previous floating/upper-biased event read.
- Re-strengthened the right-panel lower action/tackle card material in `sidebar_frame.png` after the previous pass became too pale. The lower navy frames now have clearer but still restrained borders/rules, the paper bodies regain subtle inner paper-card structure, and the tackle card uses a larger rod/reel cutout plus five compact equipment rows including `リール：スピニング4000番`, making `/tmp/tsuri_sidebar_static_compare.png` read closer to the reference's JRPG information-card stack without returning to the broken gold-grid/debug skin.
- Added `hit_badge_full.png` as a frame-backed hit sign generated by `tools/process_underwater_fish_assets.py`, and changed `UnderwaterView` / `/tmp/tsuri_full_static_compare.png` to prefer it when present. The live fight screen now uses a single raster badge with the reference-derived `ヒット！` lettering composited into the blue burst, while retaining the older `hit_burst.png` + drawn-text path as a fallback.
- Added a restrained water-integration pass to `tools/process_underwater_fish_assets.py` and regenerated `kurodai_showcase_sheet.png` / `kurodai_card_portrait.png`. The fish keeps its reference-like scales, fins, eye, and silhouette, but the belly lift and upper highlight cap are slightly quieter and the transparent-edge/body tint leans a little more into the surrounding water, reducing the pasted white-cutout feel without turning the fish into a dark blue sticker.
- Reduced the live hit-label optical weight in `UnderwaterView` and `/tmp/tsuri_full_static_compare.png`. The `ヒット！` text now uses a smaller 38-52px clamp with 5px warm outline/shadow, letting the blue `hit_burst.png` read as part of the sign instead of being mostly covered by oversized overlay lettering.
- Darkened and shortened the lower-HUD operation-note treatment in `FightHud` and `/tmp/tsuri_hud_static_compare.png`. The A/B notes keep the same single-row printed grammar with darker paper ink, while the narrow L/R slot now uses `テンション` at 10px instead of squeezing `テンション調整` into unreadable text.
- Reduced the underwater distance indicator in `UnderwaterView` and `/tmp/tsuri_full_static_compare.png`. The fight overlay now uses a short, low-alpha auxiliary chip instead of a long horizontal bar, keeping distance readable while avoiding a debug-line crossing the hit badge and seabed art.
- Added a kurodai material-polish pass in `tools/process_underwater_fish_assets.py` and regenerated `kurodai_showcase_sheet.png` / `kurodai_card_portrait.png`. The current fish cutout keeps the reference-derived scales and belly shine, but the upper body, fin edges, and high white patches are slightly cooled so the main fish and card portrait read less like flat pasted art on the water/paper surfaces.
- Darkened the right-panel tackle-card runtime/static body copy in `FightSidebar` and `/tmp/tsuri_sidebar_static_compare.png`. The frame-backed tackle rows now use a warm paper-ink color instead of the global blue-gray text color, giving the equipment card closer printed-card presence without changing the layout or icon size.
- Raised the lower-HUD A/B operation-note size in `FightHud` and `/tmp/tsuri_hud_static_compare.png`. The A/B parenthetical notes now start at 11px while L/R remains 9px, improving same-baseline readability without returning to stacked form-like notes or overflowing the narrow L/R slot.
- Retuned the top-status location card baselines in `FightStatusBar` and `/tmp/tsuri_top_status_static_compare.png`. The location title now sits slightly higher and the depth row slightly lower, giving the navy card more of the reference's two-line status-card rhythm without changing slot ratios or reopening frame material work.
- Reduced the lower-HUD bait-card value typography in `FightHud` and `/tmp/tsuri_hud_static_compare.png`. `オキアミ` now uses 19px and `× 17` uses 17px in the frame-backed path, so the bait card reads more like printed information beside the shrimp icon instead of a large button-like label.
- Reduced the lower-HUD top title outlines in `FightHud` and `/tmp/tsuri_hud_static_compare.png`. `テンション` and `魚の体力` now use a 1px frame-backed outline instead of 2px, keeping them readable on the navy board while reducing the heavy debug-label feel in the full-screen composition.
- Lightened the right-panel lower-card material in `sidebar_frame.png`, `FightSidebar`, and `/tmp/tsuri_sidebar_static_compare.png`. The action/tackle navy frames now use weaker outer/border/detail alpha, the paper bodies are a touch warmer with less form-like inner framing, and the lower-card title outlines drop to 1px so the bottom stack reads more like printed JRPG information cards than boxed debug panels.
- Raised the lower-HUD operation hint optical size in `FightHud` and `/tmp/tsuri_hud_static_compare.png`. A/B/LR still use the same one-baseline printed grammar, but the action labels are now 14px/13px and the regular-font notes are 10px/9px with darker ink, making the supplemental text readable without returning to a stacked form layout.
- Enlarged the right-panel tackle cutout in `FightSidebar` and `/tmp/tsuri_sidebar_static_compare.png`. The rod/reel now uses a 106x82px draw box with a 108px reserved text lane, giving the bottom card more reference-like equipment presence without dropping the four compact tackle rows.
- Lifted the right fish-card portrait presence in `FightSidebar` and `/tmp/tsuri_sidebar_static_compare.png`. The dedicated `kurodai_card_portrait.png` now draws at 88% contain scale with a -9px/-15px offset, reducing the empty upper card area while keeping the estimate and ecology text clear.
- Strengthened the right fish-card lower information typography in `FightSidebar` and `/tmp/tsuri_sidebar_static_compare.png`. The estimate row now uses 15px regular label/unit plus a 24px value, and the ecology/detail copy is slightly darker with stronger green dots, making the printed card information closer to the reference without bolding the small paragraph into a dark band.
- Opened up the right-panel lower-card paper bodies in `sidebar_frame.png`, `FightSidebar`, and `/tmp/tsuri_sidebar_static_compare.png`. The action/tackle bodies now start slightly higher, fill more width/height, and use weaker navy/gold frame alphas, so the cards read more like printed information cards and less like inset form wells.
- Re-strengthened the top-status runtime/static typography in `FightStatusBar` and `/tmp/tsuri_top_status_static_compare.png`. Time/money now use 23px, weather/wind use 20px/18px, and the location card uses a 14px title plus 16px/19px depth text, restoring a little of the reference's printed-card weight without returning to oversized labels.
- Reworked the lower-HUD operation hints in `FightHud` and `/tmp/tsuri_hud_static_compare.png` back toward the reference's single printed-row grammar. A/B/LR keep their small physical caps, but `巻く（リールを巻く）`, `緩める（ラインを出す）`, and `調整 テンション調整` now sit on one baseline, reducing the form-like two-line note stack in the operation card.
- Rebalanced the right fish-card portrait scale in `FightSidebar` and `/tmp/tsuri_sidebar_static_compare.png`. The compact card now draws `kurodai_card_portrait.png` at 88% contain scale with a stronger -9px/-15px left/up offset, restoring the reference-like fish presence while keeping the paper-card text stack clear.
- Retuned the lower-HUD operation hint text in `FightHud`, `fight_hud_frame.png`, and `/tmp/tsuri_hud_static_compare.png`. The A/B/LR hints now use a clearer two-line printed structure inside each paper slot instead of forcing the secondary note into tiny inline text, and the operation paper well is slightly taller with weaker vertical rules.
- Softened the right-panel lower-card and outer-frame material in `sidebar_frame.png`, `FightSidebar`, and `/tmp/tsuri_sidebar_static_compare.png`. The action/tackle paper bodies are taller and wider, the navy card borders/gold rules are lower alpha, the full sidebar outer frame has weaker black fill/shadow, and the runtime/static text/icon coordinates now sit on the larger paper bodies instead of inside smaller form-like wells.
- Retuned the lower-HUD paper/console pass in `fight_hud_frame.png`, `FightHud`, and `/tmp/tsuri_hud_static_compare.png`. The newest pass softens gold divider/trim alpha, expands the bait and operation paper wells slightly, and lets compact key notes wrap inside their operation slot when needed instead of forcing unreadably tiny inline text. The lower board keeps the reference-like card grammar without going back to the broken gold grid skin.
- Restored a little kurodai material sharpness and screen presence without reopening background work. `tools/process_underwater_fish_assets.py` now applies a modest contrast/sharpness/unsharp pass after despill cleanup, the underwater edge underlay is still thin but slightly more legible, and `UnderwaterView` plus `/tmp/tsuri_full_static_compare.png` raise the showcase fish draw width from 0.49 to 0.515 of the water window with a broader, still-low-alpha contact shadow.
- Retuned the top-status frame material in `top_status_frame.png`. `tools/generate_underwater_ui_frame_assets.py` now uses a lighter uniform dark outline, stronger brown/gold inner rules, brighter paper edge highlights, and more visible parchment corner brackets. `/tmp/tsuri_top_status_static_compare.png` keeps the text/icon geometry stable while the cards read less like plain rectangular white UI boxes.
- Raised the right fish-card optical weight in `FightSidebar` and `/tmp/tsuri_sidebar_static_compare.png`. The compact card drew `kurodai_card_portrait.png` larger during that pass, and the ecology paragraph uses 14px regular text. The current follow-up restores more card margin at 84% contain scale after the full side-by-side showed the fish was becoming too dominant inside the paper card.
- Softened the main kurodai compositing pass. `tools/process_underwater_fish_assets.py` now generates a thinner, lower-alpha dark-blue runtime edge underlay for `kurodai_showcase_sheet.png`, and both `UnderwaterView` and `/tmp/tsuri_full_static_compare.png` reduce the hit-time white fish flash to `0.10`. The fish keeps its scale texture and dorsal outline during the hit moment while reading less like a pasted sticker on the water.
- Reduced the water-window frame to one low-alpha outer stroke plus one extremely subtle inner highlight in both `UnderwaterView` and `/tmp/tsuri_full_static_compare.png`. This removes the repeated cyan/dark inset lines that made the water area read like a monitor/debug viewport, without touching the frozen background art.
- Expanded and softened the right-sidebar frame again. `sidebar_frame.png`, `FightSidebar`, and `/tmp/tsuri_sidebar_static_compare.png` now use wider frame-backed card ratios, a shallower top inset, lighter outer shadow/black-gold trim, quieter header/fish-card borders, weaker lower-card separator lines, and smaller corner rivets. The right panel reads more like stacked JRPG information cards and less like content trapped inside a heavy machine frame.
- Retuned the top-status runtime/static typography. `FightStatusBar` now uses the regular font for small labels like `AM` and the `水深` prefix, reduces the time/money values to 23px, slightly tightens the wind glyph group, and splits the navy location card into a small `水深` label plus `18.6m` value. `/tmp/tsuri_top_status_static_compare.png` now reads less like oversized overlay text while preserving the reference's compact status-card scan.
- Retuned the lower-HUD segment gauges. `FightHud` and `/tmp/tsuri_hud_static_compare.png` now draw a darker meter bed, make unfilled segments opaque dark slots instead of pale grid cells, reduce segment highlights, and soften the tension marker/safe ticks. The top HUD meters read closer to recessed reference gauges and less like a spreadsheet-like debug meter.
- Removed the left-side depth-scale strip from the showcase underwater window. `UnderwaterView` now keeps `_draw_depth_scale()` only for the procedural fallback background, and `/tmp/tsuri_full_static_compare.png` no longer paints the 44px dark vertical band over the reference-derived water art. Depth remains available through the top status and lower HUD, while the water window reads more like the reference's full illustrated scene.
- Added `hud_tension_icon.png`, extracted from the reference lower HUD heart. `FightHud` and `/tmp/tsuri_hud_static_compare.png` now use this small transparent heart for the tension title instead of tinting the ornate shared icon sheet red, so the icon reads closer to the reference and less like a red blob.
- Matched the right-sidebar fish count label to the reference copy. `FightSidebar` and `/tmp/tsuri_sidebar_static_compare.png` now draw `1/1 匹` with a space before `匹`, and right-align the text by measured width so the wider label stays inside the green header.
- Matched the lower-HUD menu copy in `FightHud` and `/tmp/tsuri_hud_static_compare.png` to the reference: the frame-backed right menu now shows `ポーズメニュー` and `釣りをやめる` instead of the shorter `ポーズ` / `港へ戻る`. Long menu labels use a 14px printed size so the reference copy fits inside the navy menu rows without clipping.
- Split the right fish-card estimate typography in `FightSidebar` and `/tmp/tsuri_sidebar_static_compare.png`. Compact frame-backed cards now draw `推定` and `cm` with the regular font at 14px while keeping the `44.2` value as a 23px bold number, so the size row reads more like printed card hierarchy instead of one heavy debug label.
- Lightened the lower-HUD center depth plate in `fight_hud_frame.png`, `FightHud`, and `/tmp/tsuri_hud_static_compare.png`. The baked value plaque is less black, the outline is thinner, and the runtime/static `18.6m` text drops from 31px with a 2px outline to 30px with a 1px outline so the center module reads less like a heavy debug label while staying legible.
- Regenerated `sidebar_frame.png` with a softer right-panel frame. `tools/generate_underwater_ui_frame_assets.py` now lets navy cards choose lower outline/detail alpha, and the sidebar path reduces the outer frame shadow, black/gold trim, header bands, lower-card title separators, and corner rivets while keeping the same card ratios and runtime text coordinates.
- Raised and rebalanced the hit-badge group in `UnderwaterView` and `/tmp/tsuri_full_static_compare.png`. `HIT_BURST_CENTER_RATIO` now places the group at 49%/76.5% of the water window, the blue burst scales larger, and the `ヒット！` baseline sits closer to the burst center so the badge reads as one visible sign instead of yellow text floating over a mostly hidden splash.
- Softened the top-status parchment card outer shadow and border alpha in `top_status_frame.png` via `tools/generate_underwater_ui_frame_assets.py`. `/tmp/tsuri_top_status_static_compare.png` keeps the existing slot geometry, text positions, icon positions, and location-card treatment, but the first three paper cards no longer carry quite as much black/brown frame weight at final scale.
- Nudged the main kurodai placement left in `UnderwaterView` and `/tmp/tsuri_full_static_compare.png` by changing `SHOWCASE_FISH_CENTER_OFFSET` to `Vector2(-0.082, -0.018)`. The fish sits closer to the reference's left-weighted subject placement while the line, lure, hit text, and distance cue remain readable.
- Lightened the lower-HUD top-row bar wells in `fight_hud_frame.png`, `FightHud`, and `/tmp/tsuri_hud_static_compare.png`. The baked black wells and center depth plate are slightly less heavy, the runtime meter bed is quieter, and the filled segments draw a little taller so the gauges read less like hard black debug slots while keeping the 18-step meter grammar.
- Retuned the right fish-card lower information area in `FightSidebar` and `/tmp/tsuri_sidebar_static_compare.png`. The card portrait window now uses a shorter 42.5% compact height and draws the portrait at 90% contain scale, while the ecology copy becomes a two-line reference-like note plus stronger 13px green-dot bait/habitat rows.
- Added deterministic paper-edge scuffs to `top_status_frame.png` parchment cards in `tools/generate_underwater_ui_frame_assets.py`. `/tmp/tsuri_top_status_static_compare.png` keeps the same slot geometry and readable text/icon placement, but the top cards now have a little more authored paper wear instead of a fully clean procedural panel surface.
- Reworked the lower-HUD operation hints in `FightHud` and `/tmp/tsuri_hud_static_compare.png` from a two-line label/note stack into a reference-like single printed row: key cap + action label + compact parenthetical note. A/B/LR keep the physical button treatment, but the operation well now reads less like three form cells and more like one printed instruction card.
- Tuned the right fish-card portrait placement and action-card typography in `FightSidebar` and `/tmp/tsuri_sidebar_static_compare.png`. The card portrait now draws at 90% of the previous contain scale and shifts slightly left/up, restoring more paper margin; the action title/message drop to 21px/14px so the lower card reads more like printed information instead of a large overlay caption.
- Softened the water-window inner frame in `UnderwaterView` and `/tmp/tsuri_full_static_compare.png`. The showcase path now uses a 1px low-alpha outer line and fewer/lighter inner inset strokes, reducing the hard debug-monitor feel while preserving fish, line, lure, hit text, and distance-meter readability.
- Reduced the live `ヒット！` text outline weight in `UnderwaterView` and synced the static full-screen comparison. The text keeps the reference-like yellow/orange punch, but the previous heavy black outline is now a smaller dark shadow plus warmer brown edge, so the hit badge reads more like artwork in the water window instead of an oversized overlay label.
- Shortened and quieted the underwater distance meter in `UnderwaterView` and `/tmp/tsuri_full_static_compare.png`. The meter now reads as a left-bottom auxiliary chip capped at about 28% of the water-window width / 244px, uses a 4px low-alpha track, and draws a smaller 9px distance label, so it remains readable without cutting across the hit badge like a debug overlay.
- Retuned the top-status typography in `FightStatusBar` and `/tmp/tsuri_top_status_static_compare.png`: time/money now use 24px instead of the heavier 25px, weather/wind use 19px/18px, and the dark location card keeps a lighter 1px outline with a smaller 12px/18px title-depth pair. The top row still reads clearly but no longer shouts over the water window as much.
- Reduced lower-HUD operation typography and key-cap scale in `FightHud` and `/tmp/tsuri_hud_static_compare.png`. A/B now use smaller 22x19px caps, L/R uses a 38x19px pill, operation labels drop to 14px, and the bait/menu labels are slightly quieter, moving the lower board away from oversized button widgets and closer to printed reference cards.
- Lightened the lower-HUD top-row meter overlay in `FightHud` and `/tmp/tsuri_hud_static_compare.png`. The frame-backed gauges now use a quieter black bed, softer inactive segments, lighter segment highlights, and a 2px depth-value outline; the static comparison also draws the runtime depth up/down arrows so the QA board reflects the actual HUD state.
- Regenerated `sidebar_frame.png` with quieter action/tackle body cards. `tools/generate_underwater_ui_frame_assets.py` now lets clean cards use lower border/detail alpha, and the right-panel lower paper bodies drop the heavy body shadow, dark outer stroke, and corner-bracket strength so `/tmp/tsuri_sidebar_static_compare.png` reads a little less like nested form boxes while keeping the card boundaries readable.
- Regenerated `top_status_frame.png` with wider inter-card gutters. The top row still uses the same runtime slot/text coordinates, but the frame material now leaves more dark separation between the parchment/location cards so `/tmp/tsuri_top_status_static_compare.png` reads less like one continuous strip and closer to the reference's independent status cards.
- Enlarged the right-panel action-card body icon by drawing only the useful alpha crop of `fight_action_card_icon.png` into a 72px frame. `FightSidebar` and `/tmp/tsuri_sidebar_static_compare.png` now match this crop behavior, so the action visual reads as a real card illustration instead of a tiny transparent-padding thumbnail while the two-line message still fits.
- Added a small real icon-sheet glyph to the right-panel `魚の行動` title band in `FightSidebar` and the sidebar static comparison. The action card header now has reference-like information density instead of a text-only navy strip, without adding code-drawn placeholder art.
- Rebuilt `kurodai_card_portrait.png` with a tighter 560x310 transparent canvas and larger useful alpha bounds. The right fish card now gives the kurodai more presence in `/tmp/tsuri_sidebar_static_compare.png` without adding a framed portrait window, blue halo, or extra runtime drawing.
- Rebuilt `top_status_frame.png` with stronger parchment-edge patina and quieter highlights, then reduced the top-status runtime/static value typography slightly. `/tmp/tsuri_top_status_static_compare.png` now reads less like flat white UI cards with oversized overlay text and closer to the reference's printed paper status windows.
- Rebuilt `fight_hud_frame.png` without the extra pale circular bait placeholder and reduced the frame-backed HUD title/edge label outlines. The bait card now relies on the extracted `hud_bait_icon.png` instead of a generated-looking icon well, and `/tmp/tsuri_hud_static_compare.png` reads less like a form-grid panel while preserving the reference-style operation card layout.
- Rebuilt `sidebar_frame.png` with a thinner outer frame, quieter gold lines, smaller corner accents, and wider card insets. `FightSidebar` and `tools/build_fight_sidebar_static_compare.py` now share the updated right-panel ratios, so `/tmp/tsuri_sidebar_static_compare.png` reads less like a heavy green/gold debug skin and closer to the reference's JRPG information-card stack.
- Added `fight_lure.png`, extracted from the reference mockup and masked to a transparent shrimp/lure asset. `UnderwaterView` and the full static comparison now use it instead of the previous code-drawn orange circles and hook arc, so the fish/line/lure/hit cluster no longer reads as a debug placeholder.
- Added `hud_bait_icon.png`, extracted from the reference lower HUD bait card and masked as a transparent okiai icon. `FightHud` and the HUD static comparison now use it for `使用中のエサ`, replacing the smaller generic sheet icon in the showcase path.
- Tuned right-panel runtime/static typography for the frame-backed path: header and lower-card title outlines are lighter, the main action label is slightly smaller, and tackle rows are a little more compact. `/tmp/tsuri_sidebar_static_compare.png` now reads less like bold debug overlay text and closer to printed card typography, while keeping the same card/asset structure.
- Added a subtle deterministic paper-edge patina to `top_status_frame.png` parchment cards. The current top-status comparison keeps text readability while reducing the flat white-card read without changing the top-row slot geometry.
- Nudged the showcase kurodai center offset from `Vector2(-0.072, -0.014)` to `Vector2(-0.082, -0.018)` and synced the full static comparison. The fish remains the water-window subject while sitting slightly closer to the reference's left/up placement and giving the hit text a little more breathing room.
- Reduced the lower-HUD operation key caps and labels in `FightHud` and the static HUD comparison. A/B/LR now read more like printed controls inside the reference-style operation card instead of oversized button widgets, while the short secondary notes remain legible.
- Synced the runtime kurodai showcase scale with the full static comparison: boss fish now uses a 0.49 water-window width ratio and `SHOWCASE_FISH_CENTER_OFFSET = Vector2(-0.067, -0.018)`. The fish regains main-subject presence while keeping the hit/lure cluster readable, and the QA board no longer overstates the runtime fish size.
- Background is accepted for this pass and frozen unless a full side-by-side QA reintroduces a P1 black band, hard mask boundary, ghosting, or unreadable fish/line/distance treatment. Remaining background differences are tracked as lower-priority polish while fish, panels, HUD, and top status carry the next quality push.
- Added dedicated `fight_action_card_icon.png` and `fight_tackle_card_icon.png` assets for the right-panel lower cards so the large ornate shared icon sheet no longer dominates those small cards.
- Rebuilt `sidebar_frame.png` with calmer lower-card icon wells and kept the dedicated `kurodai_card_portrait.png` path for the fish card.
- Added opaque paper insets to the lower HUD bait/hint cards while keeping the menu card navy; a translucent inset attempt was rejected because it made text sink into the dark board.
- Rebuilt the lower HUD operation-hint slots to align the authored paper wells with the Godot-drawn A/B/LR key blocks, reducing the broken grid/debug-panel read.
- Shortened and re-spaced the right-panel action/tackle lower-card text so the dedicated card icons no longer fight as much with dense body copy.
- Reworked the right fish-card title row into an opaque paper plaque and regenerated `sidebar_frame.png`, so `No.028 / クロダイ / レア` no longer sits on a dark, stale-looking strip.
- Rebuilt the right-panel action/tackle card interiors with separate icon wells and text plaques, reducing the mechanical guide-rule feel in the lower cards.
- Added `underwater_foreground_ambience.png`, a transparent foreground ambience asset with bubble columns, caustic strokes, and distant fish silhouettes. `UnderwaterView` now uses it over `underwater_battle_bg.png` and keeps only light specks as a runtime shimmer.
- Rebuilt `top_status_frame.png` with stronger paper-card inner frames, corner brackets, a more authored navy location card, and quieter top-status icon rendering.
- Refined HUD segment gauges with visible inactive segments, subtle upper highlights, bottom shadows, and a stronger tension marker shadow so the meters feel more embedded in the authored console.
- Rebuilt the lower HUD frame with stronger paper title bands, recessed bait/hint/menu wells, and darker menu row slots. Realigned the runtime bait text and A/B/LR key caps to the baked paper slots.
- Added `top_status_icon_sheet.png` as a top-bar-specific icon sheet extracted and cleaned from the reference. `FightStatusBar` now uses those larger clock/weather/wind/coin icons and draws the wind icon as its own inline glyph instead of a tiny overlay on the sun.
- Blended blurred, low-alpha reference-paper luminance from `reference/02_underwater_fight_mockup.png` into the parchment slots of `top_status_frame.png`, so the top cards read less like flat procedural grain. Asset-level comparison: `/tmp/tsuri_top_status_frame_texture_compare.png`.
- Moved `FightStatusBar` from the full-width root row into the left fight column so the right sidebar header starts at the same top height as the status cards, matching the reference's top-level composition more closely.
- Rebuilt `top_status_frame.png` and `FightStatusBar` slot ratios so the weather/money cards are wider and the location card is tighter instead of one oversized rightmost blue card.
- Regenerated `kurodai_card_portrait.png` with the fish occupying more of the paper portrait window, reducing empty parchment around the sidebar fish card.
- Added `underwater_color_grade.png`, a transparent background color-grade/depth overlay. `UnderwaterView` now draws it over `underwater_battle_bg.png` before ambience/fish so the clean generated background gets darker edges, stronger seabed depth, and subtler surface light structure.
- Added `underwater_seabed_detail.png`, a transparent seabed/edge-density layer with extra rock silhouettes, seaweed/coral clusters, and low caustic contour lines. `UnderwaterView` draws it after the color grade and before bubble/fish ambience.
- Rebuilt `kurodai_card_portrait.png` with a 620x330 card-window ratio instead of the previous 720x330 source ratio, so the runtime sidebar slot no longer scales the portrait down like a wide document thumbnail.
- Tuned `FightStatusBar` typography and icon layout: larger top icons, stronger AM/time/weather text, a single strong money value instead of a tiny stacked money label, and a larger centered location-depth value.
- Tuned right-panel lower card runtime layout: larger action/tackle text, reduced lower-card icon footprint, condensed the tackle card into two readable lines, and adjusted body baselines so the action and tackle notes read less like shrunken debug copy.
- Added `tools/source_assets/underwater_battle_bg_source.png` plus `tools/enhance_underwater_battle_bg.py` so `underwater_battle_bg.png` is now regenerated deterministically from a preserved source asset. The first pass adds a subtle paint glaze, darker edge/depth enclosure, far-depth haze, extra seabed contour lines, edge plants, and small foreground rocks directly into the base background while keeping the main fish zone clear.
- Set `UnderwaterView.texture_filter` to linear so the showcase background/fish/hit textures downscale more like the smoother reference illustration instead of inheriting the project's global nearest-neighbor UI texture setting.
- Added a half-resolution bilateral smoothing stage to `enhance_underwater_battle_bg.py`, blending it back into the source before depth/detail layers so the base background keeps rock/plant information while reducing the hard pixel-art surface.
- Added `tools/build_reference_underwater_background.py`, which builds `underwater_battle_bg.png` from the reference mockup's water window instead of continuing to polish the generated background as the primary source. The current pass uses the full authored water window rather than only the upper-left crop, masks the main fish, hit burst, line, and lure zones, fills those runtime-subject areas with a clean blue water field, expands the crop to the existing 1672x941 texture slot, and harmonizes the result for use under the runtime fish/HUD.
- Reduced `underwater_color_grade.png`, `underwater_seabed_detail.png`, and `underwater_foreground_ambience.png` draw opacity in `UnderwaterView` so the old generated-background helper layers no longer create a gray film over the reference-derived base.
- Replaced the earlier procedural lower/right detail and right-reef patch approach with the full-window extraction path. This preserves the reference's actual left rock pile, right reef, seabed caustics, bubble rhythm, and distant fish, while broad masks prevent the original fish/hit/line/lure from doubling behind the runtime art.
- Added a reference-derived center texture pass inside `tools/build_reference_underwater_background.py`. The pass reuses side seabed/water pixels and subtle caustic strokes inside the masked fish/hit zone, reducing the smooth circular clean-water patch without reintroducing the original reference fish, hit text, lure, or line.
- Reduced `hit_burst.png` ray/glint density in `tools/process_underwater_fish_assets.py`, so the hit badge throws fewer bright blue-white lines across the center water and competes less with the live `ヒット！` text.
- Rebuilt `sidebar_frame.png` with a shallower navy title band and taller paper body wells for the right-panel action/tackle cards. `FightSidebar` now aligns the action title/message and tackle text to those larger paper wells, with slightly smaller card icons and less assertive internal guide lines.
- Added sparse central midwater bubble columns to `underwater_foreground_ambience.png` via `tools/generate_underwater_foreground_assets.py`. A side QA check found no P2+ regressions; the bubbles do not interfere with the fish, line, or `ヒット！` text, and they slightly reduce the empty central-water read.
- Enlarged and lowered the lower-HUD operation-hint key caps and labels so the runtime `A/B/LR` controls sit more deliberately inside the baked paper slots instead of reading like small floating text.
- Strengthened the reference-derived center replacement inside `tools/build_reference_underwater_background.py` with a softer subject mask, a more visible midwater texture pass, stronger seabed caustic strokes, and small low-contrast bubbles. The updated `/tmp/tsuri_fish_hit_focus.png` restores some water/seabed density behind the runtime fish without covering the fish, line, lure, or `ヒット！` text.
- Removed the extra runtime body-panel redraw from the right-panel action/tackle cards when `sidebar_frame.png` is active. The lower cards now rely on the baked paper wells, with smaller action message text, cleaner vertical spacing, and shorter tackle copy so the text reads more like card printing than an overlaid debug panel.
- Increased the right-panel action-message text from 12px to 13px with a slightly wider line gap so the action card reads less like a miniaturized table while still fitting inside the baked paper well.
- Reduced the fight-screen root margin and container gaps in `FishingScreen`, so the top status cards, water window, lower HUD, and right sidebar use more of the 1280x720 viewport like the reference instead of floating inside a dark outer gutter.
- Tightened the `top_status_frame.png` slot ratios and matched `FightStatusBar` runtime slots to the regenerated frame. The weather card now pulls the wind glyph/text closer to `快晴`, reducing the stretched-card feel visible in `/tmp/tsuri_top_status_focus.png`.
- Increased the lower-HUD runtime gauge segmentation from broad blocks to finer 18-step meters. The tension and fish-stamina bars now read closer to the reference's detailed console meters without returning to the earlier broken grid-skin look.
- Removed the large runtime cyan fight aura behind the showcase kurodai and reduced the fish shadow to a small, low-alpha ellipse. A side test with the intact reference water window showed the broad oval artifact was coming mainly from Godot's fish layer, not only from `underwater_battle_bg.png`.
- Added a softer, lower seabed-fragment pass to `tools/build_reference_underwater_background.py`, reusing reference floor pixels only near the bottom of the masked center so it does not strengthen the midwater replacement boundary.
- Added a reference-derived surface-light patch and subtle raster light shafts inside the masked center, then lowered the seabed replacement start/fade so the center reads less like a single horizontal fill band.
- Strengthened the lower-HUD operation key caps in `FightHud` with larger A/B/LR plaques, dark outer strokes, gold inner strokes, and a subtle top highlight so the controls read less like tiny overlay text.
- Rebuilt the right-panel action/tackle lower-card wells with textured paper recesses, softer icon pockets, inner shadows, and shorter low-alpha guide lines so the lower cards read less like white ruled form fields.
- Rebuilt `underwater_battle_bg.png` with waved vertical masks, soft blob masks, additional low-alpha reef fragments, more central bubbles, and top-to-mid light shafts inside the removed fish/hit area so the center replacement reads less like a rectangular clean-water patch.
- Raised the right-panel action-card title/body text block and tightened the action-message line gap so the second line no longer collides with the baked lower guide line.
- Tightened the fight-screen root margin, body separation, left-column separation, and sidebar internal gap so the top status, water window, HUD, and right panel sit with less dark gutter between them.
- Tightened the `top_status_frame.png` slot boundaries together with `FightStatusBar._slot_rects()` and strengthened the weather-card wind text with a darker green, slightly larger label.
- Added low-alpha central caustic strokes and small bubbles to `underwater_foreground_ambience.png`, breaking up the flat masked-water band without drawing over the runtime fish or hit text.
- Retuned the runtime fish/lure/hit cluster in `UnderwaterView`: the showcase kurodai is slightly smaller and shifted left/up, the lure uses a wider forward offset so it does not collapse into the nose, and the hit badge/text are smaller and raised. The line, lure, fish, and hit badge now read as a tighter event cluster without overfilling the water window.
- Added a background-builder-only central distant-fish and midwater-bubble pass, then softened the dark midwater texture while strengthening the reference-derived light shafts. The center replacement is less like a plain blue band in `/tmp/tsuri_fish_hit_focus.png` without covering the fish, line, lure, or `ヒット！` text.
- Rebuilt `sidebar_frame.png` so the fish-card lower guide lines no longer cut through the runtime `推定 44.2 cm` and ecology notes. `FightSidebar` also reduces the compact fish portrait height slightly and uses smaller ecology text, giving the fish card a cleaner printed-information area.
- Repositioned the lower-HUD operation key caps/labels deeper inside the baked paper slots and added small paper label plates, so the slot guide lines no longer cut through `巻く / 緩める / 調整`.
- Switched the right fish-card ecology notes to regular text and shortened the bait label to `エサ：オキアミ・カニ`, removing the dark band caused by overly dense bold small text.
- Tuned `FightStatusBar` optical sizes: larger clock/time, weather, money, and location-depth values, slightly larger top icons, and a tighter AM/time grouping so the top strip reads closer to the reference's strong card numerals.
- Added a stronger reference-derived center background pass in `tools/build_reference_underwater_background.py`: a midwater school texture plus a raised seabed-shelf patch reuse the reference's own fish-school/water/seabed pixels inside the subject mask, reducing the flat blue band behind the runtime kurodai without covering the line, lure, or `ヒット！` text.
- Rebalanced the lower-HUD bait/hint/menu widths so the operation hint card gets more room, regenerated `fight_hud_frame.png` with matching baked slots, and added tiny secondary operation notes under `巻く / 緩める / 調整` to move the HUD closer to the reference's printed control-card information density.
- Added a reference-derived center-floor glint pass in `tools/build_reference_underwater_background.py`, reusing left/right seabed pixels as a low-alpha lower-center patch so the area under the fish and behind the hit text has more seabed reflection without covering the runtime fish or `ヒット！` text.
- Increased the runtime `hit_burst.png` draw scale and moved the fight distance overlay behind the hit treatment with lower opacity, so the blue splash reads more like a real badge behind `ヒット！` instead of being cut by the meter line.
- Tightened right-panel lower-card runtime density: the action-card icon is smaller, action title/message text is larger, and the tackle card now uses a shorter two-line copy with a smaller icon reserve so the lower cards read less like tiny form fields.
- Added a reference-derived midwater-depth patch and safer center sand-channel pass to `tools/build_reference_underwater_background.py`, using only edge water/seabed crops so the center masked zone gains more depth and floor texture without retaining fish, line, lure, or `ヒット！` remnants.
- Rebalanced the lower-HUD bait/hint/menu widths again so the operation-hint card gets more horizontal room, then enlarged the A/B/LR key caps, main action labels, and secondary notes to reduce the shrunken-table read.
- Added a reference-derived center band-breakup haze pass from far-left/far-right water crops, darkening and varying the cyan horizontal fill band without reintroducing fish, line, lure, or `ヒット！` remnants.
- Softened the right-panel lower-card baked text/icon wells in `sidebar_frame.png` and retuned `FightSidebar` action/tackle typography so the small cards read more like printed paper cards and less like nested form controls.
- Added a lower-center floor micro-detail pass to `tools/build_reference_underwater_background.py`, using only left/right safe seabed crops, and reduced the dark midwater/band-breakup opacity so the masked center reads slightly less like a flat dark fill while preserving the runtime fish, line, lure, and `ヒット！` text.
- Rebuilt the lower-HUD operation slots with taller baked paper wells and retuned `FightHud` key-cap/label/note sizes so the A/B/LR controls align with the art slots instead of floating as tiny overlaid text.
- Added a center midwater light-glaze pass in `tools/build_reference_underwater_background.py`, reusing only safe upper-left/upper-right water pixels to bring more reference-like surface-light variation behind the runtime fish without reintroducing the original fish, line, lure, or hit text.
- Rebuilt `top_status_frame.png` and matched `FightStatusBar._slot_rects()` with wider time/weather/money cards, a tighter navy location card, removed baked pale icon wells from the parchment cards, and realigned the top icon draw offset so the top row reads less like separate parts dropped into boxes.
- Rebalanced the center background builder after side-agent review: the dark midwater-depth and band-breakup passes are now lighter/lower-alpha, while the safe upper-water light glaze, edge-sourced sand channel, floor glints, and lower micro-detail read slightly stronger without using risky central hit/fish crops.
- Tightened the lower-HUD operation hint runtime typography: the A/B/LR key plaques are slightly shorter, the main labels sit 1px higher, the secondary notes are smaller and raised, and `_hint_key_slots()` is shortened so the text no longer presses as hard into the baked paper-slot lower rules.
- Reworked the lower-HUD operation hint grammar from stacked key/label/note rows into a more reference-like horizontal `A 巻く（リールを巻く）` composition, removing the small label plates so the three operation slots read less like nested form fields.
- Raised and strengthened the safe upper-water light glaze and edge-sourced floor-glint pass in `tools/build_reference_underwater_background.py`, bringing a little more surface-light and floor reflection into the masked center without increasing the dark band passes or using risky central hit/fish crops.
- Rebuilt `top_status_frame.png` with lower paper texture strength, quieter inner outlines/highlights, and softer weather/money separator rules. `FightStatusBar` now draws slightly larger top icons, tighter icon-to-text spacing, stronger `AM`/weather text, and a readable but closer `AM 08:47` grouping.
- Lightened the center midwater-depth and band-breakup patches in `tools/build_reference_underwater_background.py`, reduced their alpha, lowered the showcase color-grade/seabed-detail overlay opacity in `UnderwaterView`, and reduced the runtime kurodai ground shadow so the fish/hit area reads less like a dark patched band.
- Rebuilt the right-panel lower-card assets so `fight_action_card_icon.png` and `fight_tackle_card_icon.png` are transparent cutout icons instead of tiny paper cards. `sidebar_frame.png` now uses one continuous paper body for the action/tackle cards with only faint inner rules, and `FightSidebar` realigns the larger cutout icons and text to that single-card surface.
- Added a center floor-caustic mesh pass in `tools/build_reference_underwater_background.py`, using only safe left/right seabed crops plus low-alpha floor-light strokes and bubbles inside the subject mask so the fish/hit area gains more seabed reflection density without reintroducing the original fish, lure, line, or hit text.
- Raised the runtime lower-HUD display height from 178px to 198px in both `FishingScreen` and `FightHud`, so the 2048x456 `fight_hud_frame.png` is no longer visibly compressed into a thin strip and the lower operation board has more reference-like presence.
- Rebalanced the center background replacement again: the dark midwater and reef-fragment patches are lighter/lower-alpha, a subject-masked clear-water glaze adds safer blue depth and lower floor light, and `UnderwaterView` reduces the color-grade/seabed-detail overlay opacity to keep old helper layers from forming a dark film over the reference-derived center.
- Tightened the background-builder source hygiene: retained a broad subject mask to prevent fish/hit remnants, moved unsafe reference crops away from the original fish/line zones, and added `_stitch_reference_patches()` so left/right edge crops blend through a gradient instead of creating a hard vertical seam in the center replacement.
- Rebuilt the lower-HUD operation hint body from three separate paper slots into one continuous paper well with subtle dividers, then retuned the runtime key caps, main labels, and smaller secondary notes so the operation area reads less like a broken grid of mini forms.
- Raised the right-panel lower-card action icon/title/message and tackle copy slightly while reducing the action message size back to a safer fit, so the lower cards gain presence without clipping into the baked paper guide lines.
- Added a final center paintover cleanup pass to `tools/build_reference_underwater_background.py`, using only safe edge floor crops, a subject-masked midwater brightness lift, extra low-alpha floor caustics, and small bubbles to reduce the dark flat center band without reintroducing fish, line, lure, or hit remnants.
- Added `tools/source_assets/underwater_center_paintover_candidate.png`, an ImageGen-produced underwater background candidate used only as a center paintover source. `tools/build_reference_underwater_background.py` now blends it through a broad softened center mask plus a stronger subject-region mask after the reference extraction pass, preserving the reference-derived left/right reefs while lifting the center floor caustics, midwater brightness, and water density.
- Retuned the lower-HUD runtime typography again: bait count is lifted away from the lower edge, A/B/LR plaques and main labels are larger, and the secondary operation notes are shortened to inline words so they no longer clip below the paper well.
- Retuned the right-panel runtime copy: fish ecology notes are larger, action/tackle icons reserve less width, and the tackle text gains a little more size/width so the lower cards read less like shrunken debug labels.
- Added a second canvas-scale center floor-lift pass in `tools/build_reference_underwater_background.py`, using the existing `underwater_center_paintover_candidate.png` through stronger central and subject-region masks so the fish/hit region gets brighter floor caustics and midwater density without replacing the reference-derived side reefs.
- Lowered `UnderwaterView` showcase helper overlays again, from color grade 0.24/seabed detail 0.44 to 0.18/0.36, so the old helper layers no longer re-darken the newly lifted central background.
- Lowered `UnderwaterView` showcase helper overlays once more, from color grade 0.18/seabed detail 0.36 to 0.14/0.30, so the reference-derived center light and floor texture are less covered by the old color-grade/detail film.
- Raised the runtime lower-HUD display height from 198px to 206px in both `FishingScreen` and `FightHud`. A 214px trial made the water window too vertically cramped, so 206px is the current balance between reference-like operation-board presence and preserving the main underwater showcase area.
- Retuned the right-panel lower-card body usage again: the action/tackle paper bodies now start slightly higher and use more vertical space, the transparent icons reserve less width, action message text is 15px, and tackle copy is 19px over two lines. In `/tmp/tsuri_frame_focus_compare.png`, the lower cards read less like tiny form fields without new clipping.
- Enlarged the right-panel lower-card cutout icons in `FightSidebar`: the action icon now uses a 50px footprint, and the tackle icon uses a 64x58px right-side footprint with reserved text width. In `/tmp/tsuri_frame_focus_compare.png`, the tackle card reads more like a gear card with a visible rod/reel illustration instead of a tiny hook marker.
- Added a canvas-scale reference-light polish pass to `tools/build_reference_underwater_background.py`. It reuses safe upper-left/upper-right water crops for central surface light, safe left/right floor crops for lower-center seabed reflection, and low-alpha caustic strokes under the subject mask, giving `underwater_battle_bg.png` more continuous center light/floor density without reintroducing the original fish, line, lure, or hit text.
- Strengthened the same reference-light polish with a broader generated central ray gate, subtle top-surface shimmer strokes, and denser low-alpha floor caustic strokes/arcs. These are rasterized into `underwater_battle_bg.png` and do not sample from fish/line/hit regions, so they add surface light and seabed detail without reintroducing subject remnants.
- Strengthened `underwater_color_grade.png`'s surface glow, light shafts, and top shimmer strokes while reducing the very top dark veil, then raised `UnderwaterView` color-grade opacity to 0.18 with seabed detail kept at 0.30. This gives the upper water more reference-like light without changing the fish/hit floor balance.
- Retuned the lower-HUD operation hints again: A/B/LR key plaques and main labels are slightly smaller, and the control notes now sit as a second printed line inside each paper slot (`リールを巻く`, `ラインを出す`, `テンション調整`) instead of disappearing or clipping at the bottom edge.
- Added broad upper-water light shafts and top shimmer strokes to `underwater_foreground_ambience.png`. Because this layer is drawn before the runtime fish, line, and hit badge, it lifts the muted upper water toward the reference without putting a white film over the main subject.
- Strengthened `underwater_battle_bg.png` itself with a canvas-level surface-bloom pass, brighter safe upper-water reference-light polish, reduced top dark harmonize veil, and slightly stronger lower floor-caustic strokes. This moves the water window toward the reference's bright top-to-floor light without reintroducing fish, line, lure, or hit remnants.
- Lowered `UnderwaterView` showcase helper overlays from color grade 0.18/seabed detail 0.30 to 0.12/0.26. A stronger candidate-detail background pass was rejected because it darkened the center water; this runtime balance keeps the reference-derived background less covered while preserving enough edge/depth structure.
- Expanded the lower-HUD operation paper well in `fight_hud_frame.png` and enlarged the runtime A/B key caps, main labels, and secondary notes. The L/R slot uses slightly smaller text and key width so `調整 / テンション調整` fits without pressing into the divider.
- Changed the showcase background/grade/detail/ambience texture placement from centered cover to a slightly top-biased cover alignment in `UnderwaterView`, then lowered helper overlays to color grade 0.10 and seabed detail 0.22. The latest `/tmp/tsuri_fish_hit_focus.png` brings more reference-like surface light into the visible water window and reduces the dark midwater-band read without regenerating the base background asset.
- Raised the runtime lower-HUD minimum height from 206px to 224px in both `FishingScreen` and `FightHud`. The latest full and frame comparisons show the lower board reading less like a thin strip and closer to the reference's thick operation console, while the fish/hit cluster remains readable in the slightly shorter water window.
- Softened the right-panel action/tackle body wells in `sidebar_frame.png` by lowering inner-outline, guide-line, and corner-bracket alpha. `FightSidebar` now uses a 56px action cutout, a slightly larger 16px action message, 20px tackle copy, and a 68x60px tackle cutout so the lower cards read more like printed information cards and less like small form controls.
- Added `_add_canvas_visible_floor_density()` to `tools/build_reference_underwater_background.py`. It lifts safe left/right edge-floor pixels, small bubbles, and low-alpha caustic strokes into the mid-lower part of `underwater_battle_bg.png`, because the runtime water window crops the lower texture area. The latest `/tmp/tsuri_fish_hit_focus.png` shows slightly more floor-light density under the fish and around the hit badge without visible fish/line/lure remnants.
- Background freeze decision: the current background pass is accepted for this phase. It still trails the reference's fully authored center art, but the latest side-by-side has no clear black band, hard mask edge, broken cutout, or obvious fish/line/lure ghost; the fish, line, target/depth marks, distance bar, and hit treatment remain readable; and the water light, side rocks/seaweed, bubbles, and seabed density now read as a high-quality underwater base. Do not keep iterating background brightness, bubbles, floor light, or center density unless full-screen side-by-side QA shows a new P1 regression.
- Tightened the kurodai cutout pipeline in `tools/process_underwater_fish_assets.py`: semi-transparent magenta edge pixels are neutralized after the main despill, very low-alpha fringe pixels are reduced, and the runtime `kurodai_showcase_sheet.png` gets a thin dark-blue underwater edge underlay. The paper-card portrait is generated from the clean cutout before that runtime underlay, so the sidebar fish does not inherit a blue halo on parchment. Purple-edge pixels in the runtime sheet dropped from 4388 to 264, and the latest `/tmp/tsuri_fish_hit_focus.png` keeps the fish readable without adding a debug-like oval or reopening background work.
- Reworked the right-panel lower-card paper wells: `sidebar_frame.png` no longer bakes the upper body guide line that was aligning with the `突進！` and tackle text baselines, the remaining lower guide/corner brackets are quieter, and `FightSidebar` lowers the action/tackle text and icons a few pixels so they sit more like printed card content. The updated `/tmp/tsuri_frame_focus_compare.png` reduces the form-field/ruled-paper conflict in the action and tackle cards without changing the frozen background.
- Softened the lower-HUD operation paper-well dividers and enlarged the runtime A/B/LR plaques, labels, and secondary notes. The updated `/tmp/tsuri_frame_focus_compare.png` reads less like a table/grid of form fields and more like a printed operation console.
- Rebuilt `fight_action_card_icon.png` and `fight_tackle_card_icon.png` from reference lower-card crops with paper-background flood removal instead of deriving them from the generic icon sheet. `FightSidebar` now draws the tackle cutout larger, so the bottom card reads more like the reference's rod/reel information card without dropping the `港の入門竿` text.
- Reduced the showcase kurodai draw width, moved its runtime center slightly farther left/up, and softened the hit treatment scale/text size. `/tmp/tsuri_fish_hit_focus.png` keeps the fish readable while restoring more water-window breathing room around the main event.
- Retuned `FightStatusBar` runtime drawing: slightly smaller/repositioned top icons, stronger time/weather/money values, tighter wind-glyph spacing, and a regenerated `/tmp/tsuri_top_status_focus.png` check against the reference.
- Regenerated `kurodai_card_portrait.png` with a slightly smaller fish inside the 620x330 paper window, restoring more card-window margin while keeping the dedicated clean-cutout portrait path.
- Polished the right fish-card title plaque at runtime: `クロダイ` now centers between `No.028` and the rarity tag, and the `レア` tag uses a small authored plate treatment instead of a flat magenta rectangle.
- Reworked the fish-card portrait path so `kurodai_card_portrait.png` is a transparent left-facing cutout with only a soft contact shadow, while `sidebar_frame.png` carries the faint paper grid/rules. This removes the old nested rounded portrait panel and matches the reference fish-card direction.
- Restored the fish-card lower information hierarchy: compact cards now draw a plain ecology sentence followed by two small green-dot rows for bait and habitat, instead of treating all lower text as identical bullet rows.
- Retuned the lower-HUD operation key caps: A/B now render as round button caps and L/R as a compact pill, reducing the rectangular form-field read while preserving the one-piece operation paper well.
- Softened the right fish-card title treatment: `sidebar_frame.png` no longer bakes a filled name plaque, and `FightSidebar` draws the compact title row directly on the paper surface with only a thin divider, reducing the nested-plaque/form-card read.
- Raised and enlarged the right fish-card slot in `sidebar_frame.png` and matched `FightSidebar` runtime ratios, reducing the oversized dark gap under the header and giving the fish portrait/estimate/lower notes more vertical room. The compact fish-card description separator now sits below the estimate baseline instead of pressing into it. Asset-level comparison: `/tmp/tsuri_sidebar_fishcard_layout_compare.png`.
- Tightened the compact fish-card lower stack in `FightSidebar`: the portrait window is slightly shorter, the estimate type is 20px, and the ecology block starts lower so `推定 44.2 cm`, the separator rule, and the lower notes do not visually merge in runtime-size sidebar comparisons.
- Added `tools/build_fight_sidebar_static_compare.py` as a right-sidebar-only fallback comparison while Godot SubViewport capture is unavailable. It composites the real `sidebar_frame.png`, card icons, `kurodai_card_portrait.png`, and runtime-equivalent coordinates against the reference crop at `/tmp/tsuri_sidebar_static_compare.png`.
- Enlarged the card-only kurodai portrait inside `kurodai_card_portrait.png` by reducing transparent padding, and compacted the action-card message copy to a readable 15px two-line treatment. The static sidebar comparison now shows the fish card and action card as separate readable blocks without text clipping.
- Rebalanced the right sidebar vertical ratios in both `sidebar_frame.png` and `FightSidebar`: fish card height is now 46.6%, while action/tackle cards are 19.5%/17.8%. This keeps the fish card prominent but gives the lower paper cards more breathing room in `/tmp/tsuri_sidebar_static_compare.png`.
- Changed the right tackle card from oversized 20px two-line copy to four compact 13px regular rows for rod, line, leader, and hook. The card now reads closer to the reference's equipment-info card while keeping the reference-derived rod/reel cutout on the right.
- Current environment note: after the sandbox changed, non-headless Godot preview crashes before writing logs, while headless project initialization passes and headless `SubViewport` capture cannot render. Re-run `/tmp/tsuri_fight_compare.png`, `/tmp/tsuri_frame_focus_compare.png`, and `/tmp/tsuri_sidebar_fish_card_focus.png` once normal preview capture is available.
- Added a top-status-specific paper-card renderer in `tools/generate_underwater_ui_frame_assets.py` and regenerated `top_status_frame.png`. `/tmp/tsuri_top_status_frame_asset_compare.png` shows the paper cards now have lighter outer strokes, quieter corner brackets, and thinner weather/money separator rules than the previous frame asset.
- Re-extracted `top_status_icon_sheet.png` at 128px cells with tighter alpha cleanup and a light unsharp mask. `/tmp/tsuri_top_status_icons_compare.png` shows larger source cells and crisper clock/sun/wind/coin edges before runtime downscaling.
- Retuned `FightStatusBar` text baselines and optical sizes so `08:47`, `12,450 G`, and `水深 18.6m` sit inside the parchment/navy cards instead of pressing into the lower edge. Runtime-size local comparison: `/tmp/tsuri_top_status_textfit_compare.png`.
- Added `tools/build_fight_top_status_static_compare.py` as a top-status-only fallback comparison while full Godot capture is blocked. It composites `top_status_frame.png`, `top_status_icon_sheet.png`, and Godot-equivalent text baselines against the reference top-status crop at `/tmp/tsuri_top_status_static_compare.png`.
- Retuned the top-status slot ratios so the location/depth card is wider, reduced the location/depth typography to avoid a cramped right card, slightly strengthened the paper texture, and moved the weather-card separator from after the sun icon to between `快晴` and the wind glyph.
- Tightened the lower-HUD operation hint runtime layout and regenerated `fight_hud_frame.png` with weaker baked vertical separators. A/B now use slightly smaller round caps, L/R uses a shorter pill, the label/note typography is lower weight visually, and the hint title no longer presses into the key row. Runtime-size comparison: `/tmp/tsuri_hud_keyhint_compare.png`.
- Added `tools/build_fight_hud_static_compare.py` as a lower-HUD-only fallback comparison while full Godot capture is blocked. It renders the real `fight_hud_frame.png` with Godot-equivalent baselines and HUD coordinates against the reference lower-HUD crop at `/tmp/tsuri_hud_static_compare.png`.
- Reworked the lower-HUD hint card away from a beige form panel: `fight_hud_frame.png` now gives the operation hint a navy title band with a single paper body, `FightHud` centers the light title on that band, the bait title uses a darker printed tab, and the menu rows use cream circular +/- caps instead of rectangular key boxes.
- Added `tools/build_fight_full_static_compare.py` as a full-screen fallback board while runtime capture is unavailable. It composites top status, water window, lower HUD, and right sidebar into `/tmp/tsuri_fishing_fight_static.png`, then compares it against the reference at `/tmp/tsuri_full_static_compare.png`.
- Reduced the runtime showcase-fish white hit flash from `0.52` to `0.10` alpha. The full static comparison showed the previous flash washed out the kurodai's scales, dorsal spines, eye, and dark outline, making the main fish read weaker than the reference during the hit moment.
- Rebalanced the right sidebar header/fish-card ratios again in `sidebar_frame.png`, `FightSidebar`, and `/tmp/tsuri_sidebar_static_compare.png`. The green header is shorter and closer to the reference, the fish card starts higher and has more vertical room, the header title/count outlines are lighter, and the later 84% portrait draw scale keeps the kurodai present without crowding the paper card margin.
- Retuned the lower-HUD bait and operation-control runtime typography in `FightHud` and `/tmp/tsuri_hud_static_compare.png`. The bait count is lifted away from the paper-card lower trim, A/B caps are now 25x21px round controls, L/R is a 42x21px pill, and pressed A/B states brighten the cap fill/border so the operation HUD has visible interaction state without returning to oversized button widgets.
- Warmed the right-panel and lower-HUD paper material in `sidebar_frame.png` / `fight_hud_frame.png`, then added subtle paper-slot edge patina in `tools/generate_underwater_ui_frame_assets.py`. `/tmp/tsuri_full_static_compare.png`, `/tmp/tsuri_sidebar_static_compare.png`, and `/tmp/tsuri_hud_static_compare.png` show the paper cards are less flat/white without reintroducing the broken gold grid skin.
- Converted `kurodai_showcase_sheet.png` from four cloned cells into a subtle four-frame fish sheet in `tools/process_underwater_fish_assets.py`. The head/body and scale detail stay stable, while tail-side pose changes provide idle/rush/rest variation without adding a new fish style, halo, or low-quality generated art.
- Retuned the top-status runtime typography and icon scale in `FightStatusBar` and `tools/build_fight_top_status_static_compare.py`. Top icons now render at 36-42px instead of 40-46px, time/money settle at 23px, weather/wind at 20px/18px, and the inline wind icon stays at 21px, making `/tmp/tsuri_top_status_static_compare.png` read more like printed status cards and less like oversized UI labels.
- Regenerated `/tmp/tsuri_fight_compare.png`, `/tmp/tsuri_frame_focus_compare.png`, and `/tmp/tsuri_fish_hit_focus.png`.

## Findings

- [P2] Right panel is structurally better but still below reference card quality.
  Location: `assets/showcase/underwater/sidebar_frame.png`, `src/ui/components/fight_sidebar.gd`, `src/ui/fishing_screen.gd`.
  Evidence: the panel is now wider, starts at the top of the screen beside the status cards, uses a dedicated fish-card kurodai portrait instead of the runtime swimming sheet, and the previous dark portrait rectangle regression is gone. The fish-card title row now avoids the earlier opaque nested plaque: `sidebar_frame.png` owns a thin paper divider, and runtime draws `No.028 / クロダイ / レア` directly on the card surface so the header is closer to the reference's printed title row. The runtime title pass centers `クロダイ` in the available space between the number and the tag, and the `レア` tag has an inset highlight/shadow plate treatment instead of the earlier flat magenta rectangle. The newest fish-card pass makes `kurodai_card_portrait.png` a transparent left-facing cutout with a soft contact shadow, matching the reference card's fish direction and removing the earlier nested rounded portrait panel. The card paper, faint grid, and divider rules now live in `sidebar_frame.png`, so the fish reads more like it is printed on one card surface rather than pasted into a second UI panel. Moving the status bar into the left column gives the right panel more vertical room, and the 560x310 `kurodai_card_portrait.png` ratio fits the runtime fish window. The current display pass scales that portrait draw to 88% and shifts it -9px/-15px left/up, while the compact portrait window uses 42.5% of the card height. `/tmp/tsuri_sidebar_static_compare.png` and `/tmp/tsuri_full_static_compare.png` show this reduces the empty upper fish-card area without letting the kurodai collide with the estimate or ecology text. The latest ratio pass shortens the green header to 7.71% of sidebar height, raises the fish-card top from 10.55% to 9.18%, expands the fish card to 48.83%, and keeps action/tackle at 59.18%/80.08% with 19.53%/18.75% heights in both `sidebar_frame.png` and `FightSidebar`. This better matches the reference's shallow header and keeps the fish card as the main sidebar feature without shrinking the lower cards. Header title/count outlines are now 1px in the frame-backed path, so the green band reads more like printed UI and less like heavy debug text. The compact fish-card description separator sits below the estimate baseline instead of crowding the estimate row. The estimate row splits hierarchy instead of using one heavy string: `推定` and `cm` use the regular font at 15px, while `44.2` is now a 24px bold value. This reads closer to the reference's printed size callout while keeping the lower information hierarchy: a two-line ecology note appears above two green-dot rows for bait and habitat. The ecology paragraph remains 14px regular text, while the lower bait/habitat rows stay compact 13px rows with slightly stronger green dots and darker ink, so the lower block reads less faint without colliding with the card bottom. Recent lower-card passes make the navy title bands shallower, enlarge the paper wells, reduce the icon-well footprint, soften internal guide lines, and realign the action/tackle text onto the larger paper areas. The newest lower-card pass opens those paper bodies again: the action body now uses roughly 8.5px side insets plus 17.0%/3.5% vertical insets inside its card, the tackle body uses roughly 8.5px side insets plus 16.7%/4.2% vertical insets, and the navy/card border alphas are weaker. The lower-card path uses dedicated transparent cutout icons from the reference mockup, places them onto one continuous paper body, and avoids baking strong upper guide lines that collide with text. The action message draws at 14px and compresses `潜ろうとしている` to `潜る`; the main action title is 20px in the frame-backed path so the title no longer overpowers the body. The tackle card uses regular 13px rows for `ロッド / ライン / ハリス / 針`, reserves 108px for the right-side art, and draws the rod/reel cutout in a 106x82px box. The latest material pass warms the fish/action/tackle paper fills and adds slightly more texture, so `/tmp/tsuri_sidebar_static_compare.png` reads less like clean form paper while staying below the broken gold-grid/noisy-frame threshold. `/tmp/tsuri_sidebar_static_compare.png` and `/tmp/tsuri_full_static_compare.png` show the lower cards now read less like small form fields inside dark frames and more like reference-style information cards. They still feel slightly more mechanically framed than the reference, especially around final authored card assets and typography.
  Impact: the right panel now matches the reference's top-level composition better, the fish card no longer has visible rule/text collisions, and the title/fish/lower-information areas read more like printed card UI instead of a set of flat labels and nested panels. The latest pass should reduce nested-plaque noise in the fish-card header, but the full runtime comparison must be rerun once preview capture is available.
  Fix: keep the dedicated transparent left-facing fish-card portrait, 88% runtime/static portrait draw scale with -9px/-15px left/up offset, soft contact shadow, 560x310 portrait ratio, wider panel, direct-on-paper fish-card title row, authored rarity tag plate, short 7.71% header with 1px title/count outline, fish card at y=9.18%/h=48.83%, action/tackle at y=59.18%/80.08% with h=19.53%/18.75%, warmed/textured paper fills, softened outer/sidebar/header/lower-card frame alpha, reduced corner rivet strength, 42.5% compact portrait window height, split estimate typography with 15px regular label/unit and 24px bold value, cleaned fish-card lower information area, 14px regular two-line ecology note plus 13px green-dot bait/habitat rows with darker ink, reference-derived transparent lower-card icons, one-piece lower-card paper bodies, action body x=8.5px/y=17.0%/bottom=3.5% and tackle body x=8.5px/y=16.7%/bottom=4.2%, removed upper body guide line, subdued lower-card paper body shadow/borders, softened lower guide/corner brackets, alpha-cropped 72px action cutout, 106x82px right-side tackle cutout draw box with 108px reserved text lane, 20px action title, 14px compact action message, and regular 13px four-row tackle copy. Next right-panel work should focus on true final authored card/text assets rather than reintroducing a framed portrait window, a filled fish-title plaque, heavy black/gold outer trim, tall green header, oversized portrait placement, narrow inset paper wells, or internal guide rules.

- [P2] HUD top row and board proportions are closer, but still below the reference's authored console quality.
  Location: `assets/showcase/underwater/fight_hud_frame.png`, `src/ui/components/fight_hud.gd`.
  Evidence: the upper board is now darker, the central depth module is less bright, the title/icon scale is more compact, and the row icons are visually quieter after opacity modulation. The runtime meter now uses finer 18-step segmentation, inactive segments remain faintly visible, and filled segments retain small highlight/shadow treatment instead of flat rectangles. This brings the tension and fish-stamina bars closer to the reference's detailed console meters while avoiding the earlier broken grid-skin look. The latest top-row HUD pass softens the baked black bar wells and the runtime black meter bed, reduces inactive segment opacity, draws the filled segments slightly taller, and lightens the center depth-plate fill/shadow. It now also lightens the baked center value plaque from a near-black fill to a softer blue, reduces its outline width, and drops the runtime/static `18.6m` from 31px with a 2px outline to 30px with a 1px outline; `/tmp/tsuri_hud_static_compare.png` shows the value still reads but no longer dominates the depth module as a heavy black-outlined label. It keeps the reduced depth-title outline and narrow right-side depth-arrow lane so the arrows do not collide with `18.6m`; `/tmp/tsuri_hud_static_compare.png` includes those runtime arrows. The lower bait/hint/menu area now has stronger baked paper title bands, a one-piece operation paper well with subtle separators, darker menu rows, and runtime A/B/LR key caps aligned to the authored paper area instead of floating inside a generic grid. Recent operation-hint passes keep the hint card wide by preserving the reduced bait/menu proportions. The newest HUD pass adds `/tmp/tsuri_hud_static_compare.png` so the lower board can be judged while full Godot capture is unavailable, then uses that comparison to move the operation hint away from a beige form card: the hint panel now has a navy title band, centered light title, one paper body, weaker separators, and printed key rows inside the paper area. The bait title now sits on a darker printed tab, and the menu commands use cream circular +/- caps closer to the reference's icon grammar. The current operation row uses 25x21px A/B caps, a 42x21px L/R pill, 14px main A/B labels, a 13px L/R label, 11px A/B notes, and a 10px shortened `テンション` L/R note in darker paper ink on the same baseline as the label. This brings the operation hint closer to the reference's `キー + 操作（補足）` printed-row grammar and reduces the form-like stacked-note read while keeping the supplemental text legible. Pressed A/B states brighten the key cap fill and gold border so interaction state is visible without turning the controls into large UI buttons. The bait count baseline is lifted from the lower paper trim, so `/tmp/tsuri_hud_static_compare.png` no longer shows `× 17` touching the bottom edge. The latest paper-material pass warms the bait card fill and adds low-alpha edge patina to the bait/hint paper slots, reducing the clean form-card whiteness while preserving readability. The right menu still matches the reference copy with `ポーズメニュー` and `釣りをやめる`; those longer labels use 14px so they remain printed inside the navy menu rows instead of clipping at the right edge. The displayed HUD minimum height is still 224px. In `/tmp/tsuri_hud_static_compare.png`, the lower board no longer reads like a table of form cells; the operation hint reads more like the reference's blue-header operation card, and the upper gauges read less like hard black debug boxes, though the reference still has more custom typography, tighter black-panel spacing, and a more bespoke meter construction.
  Impact: the HUD reads more like an authored operation board and less like a debug grid. The wider hint card, taller baked slots, horizontal operation grammar, round A/B caps, compact L/R pill, and thicker display height improve scan quality, but the overall construction still does not fully reach the reference's premium console quality.
  Fix: keep the darker frame, label padding, quieter icon opacity, 18-step segment grid, slightly taller filled segments, visible but subdued inactive segments, softened baked/runtime meter bed, lighter blue center depth plate, 30px/1px-outline depth value, narrow right-side depth-arrow lane, navy menu card with reference copy `ポーズメニュー` / `釣りをやめる`, darker bait title tab, warmed paper fills, subtle paper-slot edge patina, lifted bait name/count baselines, wider operation-hint card, navy operation title band, centered light hint title, softened one-piece operation paper well, very low-alpha hint separators, 224px runtime display height, 25x21px round A/B caps with subtle active-state brightening, 42x21px compact L/R pill, 14px A/B labels, 13px L/R label, 11px A/B notes, 10px shortened `テンション` L/R note, cream circular menu caps, and the current single-row printed operation grammar. Next HUD work should focus on bespoke typography/assets rather than restoring rectangular A/B boxes, three separate paper slots, strong vertical rules, beige-only hint panels, heavy black depth plaques, oversized controls, stacked form-like notes, or unreadably tiny inline notes.

- [P2] Top status bar has stronger material quality, but still trails the reference's compact icon grammar.
  Location: `assets/showcase/underwater/top_status_frame.png`, `assets/showcase/underwater/top_status_icon_sheet.png`, `src/ui/components/fight_status_bar.gd`.
  Evidence: the top status frame now has stronger paper-card inner borders, corner brackets, and a more authored navy location card. The old common icon sheet made the first three top cards read as small noisy ornaments; the new top-specific icon sheet gives the clock, sun, wind, and coin clearer silhouettes closer to the reference, and the wind glyph is now placed inline before `風 弱`. This icon pass regenerates `top_status_icon_sheet.png` at 128px cells instead of 96px, tightens low-alpha cleanup, and applies a light unsharp mask after scaling from the reference crops. `/tmp/tsuri_top_status_icons_compare.png` shows the icon source has more usable pixel area and crisper edges before Godot downsizes it into the status cards. The status bar now lives only over the left fight column, and the latest slot pass widens the time/weather/money cards while tightening the navy location card, so `/tmp/tsuri_top_status_compare.png` no longer has the same stretched location-card feel. The baked pale icon wells were removed from the parchment cards, and the runtime top-icon offset was aligned closer to the generated frame, reducing the sense that icons and text are separate dropped-in parts. The newest top-status pass adds `/tmp/tsuri_top_status_static_compare.png` as a direct reference comparison, then retunes the slot ratios to 23.5%/30.0%/25.0%/21.5%, giving the location/depth card more room while keeping the first three parchment cards close to the reference widths. The weather-card separator now sits between `快晴` and the wind glyph instead of immediately after the sun icon. The parchment texture strength is slightly higher so the cards read less like flat white panels. Recent `top_status_frame.png` regeneration widens the inter-card gutters so the top row reads less like a continuous connected strip and more like four independent reference-style status cards. A small deterministic scuff layer adds fine edge wear, short paper scratches, and corner stains to the parchment cards while keeping `/tmp/tsuri_top_status_static_compare.png` readable at final scale. The latest material/typography pass reduces the hard vertical black gutter, expands the parchment/navy cards vertically inside the 76px top row, softens the baked shadow, and keeps the runtime/static top icon and type scale printed rather than oversized: top icons are 36-42px, time/money are 23px, weather/wind are 20px/18px, and the inline wind icon is 21px. The navy location card keeps 14px title plus 16px/19px depth text. `/tmp/tsuri_full_static_compare.png` now shows the top row as part of the screen edge rather than small boxes floating on black. The reference still has more tailored glyph metrics, stronger icon/text alignment, and more authored card distress.
  Impact: the top row now reads more like the reference's authored status strip, no longer pushes the right panel down, and feels less isolated from the water window. The lighter frame should reduce generated-frame noise, but a fresh full runtime comparison is still required once normal preview capture is available.
  Fix: keep the 128px `top_status_icon_sheet.png`, left-column placement, the 23.5%/30.0%/25.0%/21.5% slot ratios, wider location/depth card, removed baked icon wells, widened-but-controlled top-card gutters, vertically expanded top cards, top-status-specific lighter parchment renderer, softened uniform dark outline and baked shadow, stronger brown/gold inner rules, brighter paper edge highlights, visible parchment corner brackets, reference-paper luminance blend with slightly stronger texture, deterministic low-alpha parchment scuffs, weather separator between `快晴` and wind, current top icons at 36-42px, 23px time/money, 20px/18px weather/wind, 21px inline wind icon, 14px title plus 16px/19px location-depth type with a 1px outline, and the current baselines. Next top-row work should focus on final authored icons/card assets, not on returning to heavier black paper outlines, a narrow right card, small floating cards with large vertical black gutters, a separator directly after the sun icon, or oversized 24px+ top-row labels.

- [P3] Main fish is now close, with minor animation/contact polish remaining.
  Location: `assets/showcase/underwater/kurodai_showcase_sheet.png`, `assets/showcase/underwater/kurodai_card_portrait.png`, `tools/process_underwater_fish_assets.py`, `src/ui/components/underwater_view.gd`.
  Evidence: the fish source now preserves the reference-like black seabream outline, scale texture, dorsal spines, eye, and gray banding. The implementation fish is no longer a generic generated cutout, and the previous oversized cyan fight aura is gone. The newest cutout pass restores a small amount of contrast, sharpness, and unsharp detail after despill cleanup, so scales and fin rays survive the transparent-edge processing better. The dark fish shadow is still small, but it is slightly wider so the fish no longer feels pasted onto the water while avoiding a visible debug-like oval that the reference does not have. The runtime path now uses `SHOWCASE_FISH_CENTER_OFFSET = Vector2(-0.058, 0.006)`, uses a boss showcase fish width ratio of 0.525 of the water-window width, keeps the lure forward offset at about 0.44 of the fish width, and reduces the hit badge/text scale. This latest placement/scale pass gives the main fish stronger subject weight in `/tmp/tsuri_full_static_compare.png` without making the line, lure, hit text, or distance cue unreadable. The cutout pipeline also cleans the transparent fish edge after despill and adds only a thin dark-blue runtime edge underlay to `kurodai_showcase_sheet.png`; the paper `kurodai_card_portrait.png` still uses the clean cutout before that underlay. The newest fish-sheet pass keeps the same high-quality source art but adds subtle tail-side pose changes for the four runtime cells, so `UnderwaterView._showcase_fish_frame_index()` no longer cycles visually identical fish frames. The head, eye, scale field, and body silhouette remain stable; the motion reads as tail/caudal movement instead of a newly generated fish. The full-screen static board at `/tmp/tsuri_full_static_compare.png` exposed that the previous runtime white fish flash (`_fish_flash * 0.52`) washed out the kurodai during the exact hit moment, so the showcase-fish flash overlay is now reduced to `0.10` and the dark-blue edge underlay remains restrained. The latest fish-material pass lowers the pearly belly lift, slightly strengthens the upper highlight cap, and adds a very low water-colored mix to opaque edge/upper-fin areas. The fish still gets a subtle hit response, but the scales, dorsal spines, eye, and dark body outline remain visible. In `/tmp/tsuri_full_static_compare.png` and `/tmp/tsuri_fish_hit_focus.png`, the purple/magenta fringe is no longer visible, the fish reads crisply against the frozen water background without quite as much white cutout glare, the lure sits forward of the nose instead of merging into it, and the smaller hit treatment leaves more water-window breathing room around the event.
  Impact: the fish is no longer a blocking quality mismatch; it now sells the screen's main subject with a cleaner cutout edge and less sticker-like compositing against the water.
  Fix: keep this asset, cleaned transparent edge, restrained runtime underwater edge underlay, restored detail pass, slight water-integration material tint, subtle four-frame tail-side variation, `Vector2(-0.058, 0.006)` fish center offset, 0.525 boss draw-width ratio, lure offset, reduced 0.10 white fish-flash overlay, and smaller raised hit treatment as the current kurodai baseline. With the background frozen, remaining fish work should be a true final-art replacement only if it improves the reference comparison without adding a visible oval, halo, whitewashed body, or mismatched fish style.

- [P3] Background passes this phase; final ideal gap remains, but it is frozen unless a P1 regression appears.
  Location: `assets/showcase/underwater/underwater_battle_bg.png`, `tools/build_reference_underwater_background.py`, `assets/showcase/underwater/underwater_color_grade.png`, `assets/showcase/underwater/underwater_seabed_detail.png`, `assets/showcase/underwater/underwater_foreground_ambience.png`, `src/ui/components/underwater_view.gd`.
  Evidence: the primary background is no longer the generated canyon scene. `tools/build_reference_underwater_background.py` now extracts the full authored water window from the reference mockup, preserving the real left rock pile, right reef, seabed caustics, bubbles, distant fish, surface light, and darker blue side depth. The main fish, hit burst, line, and lure zones are broadly masked so the runtime kurodai, hit treatment, and line can sit on top without obvious duplicates. The retained reference-derived passes soften the subject mask, strengthen the midwater/seabed texture, keep tiny bubbles in the central replacement zone, add very low-alpha reference floor and reef fragments near the lower masked center, reuse the reference's surface-light pixels as a faint central light patch, and apply waved/blobbed alpha masks so the inserted patches are less rectangular. Recent builder passes add a reference-derived fish-school/water texture, lighter and lower-alpha midwater-depth and band-breakup patches from safe edge water crops, a stronger safe upper-water light glaze, a raised seabed-shelf patch, a brighter center sand channel from far-left/far-right seabed crops, brighter/raised center-floor glints, slightly stronger lower-center floor micro-detail, and a low-alpha center floor-caustic mesh directly inside the subject-limited background mask. This pass keeps the broad subject mask after a tighter-mask attempt visibly reintroduced fish remnants, moves unsafe source crops away from the original fish/line zones, blends left/right source crops with `_stitch_reference_patches()`, blends the generated `underwater_center_paintover_candidate.png` through both a broad center-water mask and a stronger subject-region mask, then adds a second canvas-scale center floor-lift mask so the center floor and hit region receive more of the candidate's bright caustics. `_add_canvas_reference_light_polish()` reuses only safe top and side-floor reference crops after the canvas-scale paintover, strengthens the generated broad central ray gate, adds a separate top surface-bloom/shimmer layer, and slightly increases lower floor-caustic stroke strength. The newest `_add_canvas_visible_floor_density()` pass accounts for the runtime water window's top-biased, wide crop by lifting safe edge-floor pixels, bubbles, and thin caustic strokes into the visible mid-lower band of the texture. This adds a little more floor-light density under the fish and around the hit badge without sampling from the original fish, line, lure, or hit zones. `underwater_color_grade.png` now has stronger upper surface glow/light shafts and a weaker top dark veil. The runtime draws the showcase background, color grade, seabed detail, and foreground ambience with a top-biased cover alignment (`Vector2(0.5, 0.24)`) instead of a centered crop, then uses color grade/seabed helper opacity at 0.10/0.22 so the reference-derived center is less covered. `underwater_foreground_ambience.png` still carries broad upper-water shafts and top shimmer behind the runtime fish/hit layers. In `/tmp/tsuri_fish_hit_focus.png`, the visible water window now keeps more surface light from the source asset, the midwater band reads less dark, and the fish/hit floor region keeps cleaner floor-light density while the runtime fish, line, lure, and `ヒット！` text remain clean. The center is still not as fully authored as the reference's hand-painted water window.
  Impact: this is a visible move toward the target image because the water panel carries more of the reference's real edge density while the generated center source gives the masked area a more finished floor-light read. It reduces the flat replacement-band feeling more than deterministic crop reuse alone. The remaining gap is that the center is still not as hand-authored as the reference, but that is now a P2/P3 ideal-quality gap rather than a P1 blocker for the whole screen.
  Fix: freeze this background for the current pass. Keep the full-window extraction, broad no-remnant subject mask, stronger center texture pass, waved/blobbed masks, central light shafts, edge-only safe crops, blended left/right crop stitching, lighter edge-sourced midwater-depth patch, lower-opacity brighter center band-breakup haze, raised safe upper-water light glaze, clear-water glaze, reduced dark reef-fragment alpha, edge-sourced center sand channel, center-floor glint patch, floor-caustic mesh, lower-center micro-detail patch, sparse central background fish/bubbles, final center paintover cleanup, generated center paintover source, second canvas center floor-lift pass, canvas reference-light polish, visible-floor-density polish, broad generated central ray gate, stronger color-grade surface glow, foreground-ambience upper shafts, top-surface bloom/shimmer strokes, denser floor caustic strokes/arcs, reduced runtime fish shadow, top-biased showcase texture alignment, and the current 0.10/0.22 showcase overlay balance as the deterministic background build path. Do not adjust background brightness, bubbles, floor light, or center density again unless `/tmp/tsuri_fight_compare.png` or `/tmp/tsuri_fish_hit_focus.png` shows a clear P1 defect such as a black band, hard mask edge, broken cutout, obvious ghost, or unreadable fish/line/distance elements.

- [P3] Hit treatment is closer, with only final context polish remaining.
  Location: `assets/showcase/underwater/hit_badge_full.png`, `assets/showcase/underwater/hit_burst.png`, `src/ui/components/underwater_view.gd`.
  Evidence: the badge now reads as a single raster sign instead of a blue splash with separately drawn overlay text. `tools/process_underwater_fish_assets.py` generates `hit_badge_full.png` by compositing the reference-derived `ヒット！` lettering onto the blue burst asset, and `UnderwaterView` prefers that full badge when it exists. The older `hit_burst.png` + 38-52px / 5px drawn-text path remains as a fallback only. This keeps the text and burst optically locked together, so `/tmp/tsuri_full_static_compare.png` shows a more authored hit sign and less of a floating UI label. The latest distance-meter pass still shortens the track to about 28% of the water-window width with a 244px cap, lowers the track/fill opacity, and reduces the distance label to 9px, so it reads as a subtle left-bottom fight cue rather than a long cyan debug bar cutting through the hit area. The full badge is slightly smaller than the reference crop, but it no longer lets oversized live lettering cover most of the blue sign.
  Impact: the hit moment has more of the reference's central badge presence while keeping the fish and HUD readable. It is no longer a major art-style mismatch, but it is still not a perfect match to the reference badge.
  Fix: keep `hit_badge_full.png` as the preferred runtime/static hit treatment, retain `hit_burst.png` plus the 38-52px / 5px drawn-text path only as fallback, keep the shorter low-alpha distance overlay behind the hit treatment, 49%/76.5% hit-badge center, and 0.47-0.56 badge scale clamp. Revisit only after the final HUD/font pass or if the frozen background comparison exposes a clear read issue.

- [P2] Typography is improved but still not at the reference's custom UI quality.
  Location: all fight UI overlay text.
  Evidence: the fight UI now uses `MPLUS1p-Bold.ttf` for the main overlay text, the top-status numbers are stronger, and the location/depth card now matches the reference's iconless centered layout. The parchment-card icons are smaller and lower opacity, but still more ornate than the simpler reference glyphs. The reference still has more tailored optical weights plus tighter small-text rendering.
  Impact: the screen now reads more like a game UI, but typography still does not fully sell the premium mockup quality.
  Fix: keep the bold/regular split, then tune per-component font sizes and replace/simplify the top-status icon sheet if those icons still read too noisy in the final full-screen pass.

## Open Questions

- None blocking. The background is accepted and frozen for this phase: final ideal differences remain, but they should not drive another brightness/bubble/floor-light/center-density loop. The main fish source/placement/frame variation, top status, right-panel macro layout, lower-HUD runtime typography/states, and current paper/header material pass now have acceptable subject/readability weight for this pass. The next highest-value pass is a true final-art fish replacement or bespoke UI typography/assets, not more frame-material micro-polish.

## Implementation Checklist

1. Keep comparing `/tmp/tsuri_frame_focus_compare.png` against the reference after any HUD/sidebar frame change.
2. Treat `underwater_battle_bg.png` as frozen for this phase. Only reopen `tools/build_reference_underwater_background.py` if full-screen side-by-side QA shows a P1 background defect, not for normal brightness, bubble, floor-light, or center-density polish.
3. Keep the dedicated fish-card portrait; do not return to drawing the swimming sprite sheet directly in the sidebar.
4. Re-run `/tmp/tsuri_fight_compare.png`, `/tmp/tsuri_frame_focus_compare.png`, and `/tmp/tsuri_fish_hit_focus.png` after each pass.
5. Re-run the normal Godot preview capture after the current sandbox restriction is gone; headless project initialization passes, but headless `SubViewport` capture cannot replace visual side-by-side QA.

## Follow-up Polish

- Prioritize a deliberate final-art fish replacement or bespoke UI typography/assets next. Top status, right-panel macro ratios, lower-HUD runtime typography/states, current fish-frame variation, and the current paper/header material pass are acceptable for this pass and should only be reopened for clear optical/readability defects. Background-only polish is out of scope unless it fixes a P1 regression; main-fish work should be a true final-art replacement, not another scale/brightness loop.
- Recheck lower HUD text sizes after any layout/proportion change; the current paper slots and key-cap positions are intentionally paired.
- Recheck the hit splash after the final HUD/font pass, but do not keep iterating it unless the comparison regresses.
- Add small sparkle/bubble particles only after the main frame and typography mismatches are solved.
- Keep the current subtle tail-side variants; do not return `kurodai_showcase_sheet.png` to four identical cloned frames unless a future final-art sheet replaces it.
