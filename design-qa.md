# Underwater Fight Design QA

final result: blocked

Date: 2026-06-25

Source visual truth: `reference/02_underwater_fight_mockup.png`
Implementation screenshot: `/tmp/tsuri_fishing_fight.png`
Full-view comparison evidence: `/tmp/tsuri_fight_compare.png`
Focused frame comparison evidence: `/tmp/tsuri_frame_focus_compare.png`
Focused fish/hit comparison evidence: `/tmp/tsuri_fish_hit_focus.png`
Side-by-side page: `/tmp/tsuri_fight_compare.html`
Viewport: 1280x720
State: underwater fight, kurodai hit moment, depth 18.6m, action `突進`

## Patches Made Since Previous QA

- Added dedicated `fight_action_card_icon.png` and `fight_tackle_card_icon.png` assets for the right-panel lower cards so the large ornate shared icon sheet no longer dominates those small cards.
- Rebuilt `sidebar_frame.png` with calmer lower-card icon wells and kept the dedicated `kurodai_card_portrait.png` path for the fish card.
- Added opaque paper insets to the lower HUD bait/hint cards while keeping the menu card navy; a translucent inset attempt was rejected because it made text sink into the dark board.
- Rebuilt the lower HUD operation-hint slots to align the authored paper wells with the Godot-drawn A/B/LR key blocks, reducing the broken grid/debug-panel read.
- Shortened and re-spaced the right-panel action/tackle lower-card text so the dedicated card icons no longer fight as much with dense body copy.
- Reworked the right fish-card title row into an opaque paper plaque and regenerated `sidebar_frame.png`, so `No.028 / クロダイ / レア` no longer sits on a dark, stale-looking strip.
- Rebuilt the right-panel action/tackle card interiors with separate icon wells and text plaques, reducing the mechanical guide-rule feel in the lower cards.
- Added `underwater_foreground_ambience.png`, a transparent foreground ambience asset with bubble columns, caustic strokes, and distant fish silhouettes. `UnderwaterView` now uses it over `underwater_battle_bg.png` and keeps only light specks as a runtime shimmer.
- Rebuilt `top_status_frame.png` with stronger paper-card inner frames, corner brackets, a more authored navy location card, and quieter top-status icon rendering.
- Refined HUD segment gauges with visible inactive segments, subtle upper highlights, bottom shadows, and a stronger tension marker shadow so the meters feel more embedded in the authored console.
- Rebuilt the lower HUD frame with stronger paper title bands, recessed bait/hint/menu wells, and darker menu row slots. Realigned the runtime bait text and A/B/LR key caps to the baked paper slots.
- Added `top_status_icon_sheet.png` as a top-bar-specific icon sheet extracted and cleaned from the reference. `FightStatusBar` now uses those larger clock/weather/wind/coin icons and draws the wind icon as its own inline glyph instead of a tiny overlay on the sun.
- Moved `FightStatusBar` from the full-width root row into the left fight column so the right sidebar header starts at the same top height as the status cards, matching the reference's top-level composition more closely.
- Rebuilt `top_status_frame.png` and `FightStatusBar` slot ratios so the weather/money cards are wider and the location card is tighter instead of one oversized rightmost blue card.
- Regenerated `kurodai_card_portrait.png` with the fish occupying more of the paper portrait window, reducing empty parchment around the sidebar fish card.
- Added `underwater_color_grade.png`, a transparent background color-grade/depth overlay. `UnderwaterView` now draws it over `underwater_battle_bg.png` before ambience/fish so the clean generated background gets darker edges, stronger seabed depth, and subtler surface light structure.
- Added `underwater_seabed_detail.png`, a transparent seabed/edge-density layer with extra rock silhouettes, seaweed/coral clusters, and low caustic contour lines. `UnderwaterView` draws it after the color grade and before bubble/fish ambience.
- Rebuilt `kurodai_card_portrait.png` with a 620x330 card-window ratio instead of the previous 720x330 source ratio, so the runtime sidebar slot no longer scales the portrait down like a wide document thumbnail.
- Tuned `FightStatusBar` typography and icon layout: larger top icons, stronger AM/time/weather text, a single strong money value instead of a tiny stacked money label, and a larger centered location-depth value.
- Tuned right-panel lower card runtime layout: larger action/tackle text, reduced lower-card icon footprint, condensed the tackle card into two readable lines, and adjusted body baselines so the action and tackle notes read less like shrunken debug copy.
- Added `tools/source_assets/underwater_battle_bg_source.png` plus `tools/enhance_underwater_battle_bg.py` so `underwater_battle_bg.png` is now regenerated deterministically from a preserved source asset. The first pass adds a subtle paint glaze, darker edge/depth enclosure, far-depth haze, extra seabed contour lines, edge plants, and small foreground rocks directly into the base background while keeping the main fish zone clear.
- Set `UnderwaterView.texture_filter` to linear so the showcase background/fish/hit textures downscale more like the smoother reference illustration instead of inheriting the project's global nearest-neighbor UI texture setting.
- Added a half-resolution bilateral smoothing stage to `enhance_underwater_battle_bg.py`, blending it back into the source before depth/detail layers so the base background keeps rock/plant information while reducing the hard pixel-art surface.
- Added `tools/build_reference_underwater_background.py`, which builds `underwater_battle_bg.png` from the reference mockup's water window instead of continuing to polish the generated background as the primary source. The current pass uses the full authored water window rather than only the upper-left crop, masks the main fish, hit burst, line, and lure zones, fills those runtime-subject areas with a clean blue water field, expands the crop to the existing 1672x941 texture slot, and harmonizes the result for use under the runtime fish/HUD.
- Reduced `underwater_color_grade.png`, `underwater_seabed_detail.png`, and `underwater_foreground_ambience.png` draw opacity in `UnderwaterView` so the old generated-background helper layers no longer create a gray film over the reference-derived base.
- Replaced the earlier procedural lower/right detail and right-reef patch approach with the full-window extraction path. This preserves the reference's actual left rock pile, right reef, seabed caustics, bubble rhythm, and distant fish, while broad masks prevent the original fish/hit/line/lure from doubling behind the runtime art.
- Added a reference-derived center texture pass inside `tools/build_reference_underwater_background.py`. The pass reuses side seabed/water pixels and subtle caustic strokes inside the masked fish/hit zone, reducing the smooth circular clean-water patch without reintroducing the original reference fish, hit text, lure, or line.
- Reduced `hit_burst.png` ray/glint density in `tools/process_underwater_fish_assets.py`, so the hit badge throws fewer bright blue-white lines across the center water and competes less with the live `ヒット！` text.
- Rebuilt `sidebar_frame.png` with a shallower navy title band and taller paper body wells for the right-panel action/tackle cards. `FightSidebar` now aligns the action title/message and tackle text to those larger paper wells, with slightly smaller card icons and less assertive internal guide lines.
- Added sparse central midwater bubble columns to `underwater_foreground_ambience.png` via `tools/generate_underwater_foreground_assets.py`. A side QA check found no P2+ regressions; the bubbles do not interfere with the fish, line, or `ヒット！` text, and they slightly reduce the empty central-water read.
- Enlarged and lowered the lower-HUD operation-hint key caps and labels so the runtime `A/B/LR` controls sit more deliberately inside the baked paper slots instead of reading like small floating text.
- Strengthened the reference-derived center replacement inside `tools/build_reference_underwater_background.py` with a softer subject mask, a more visible midwater texture pass, stronger seabed caustic strokes, and small low-contrast bubbles. The updated `/tmp/tsuri_fish_hit_focus.png` restores some water/seabed density behind the runtime fish without covering the fish, line, lure, or `ヒット！` text.
- Removed the extra runtime body-panel redraw from the right-panel action/tackle cards when `sidebar_frame.png` is active. The lower cards now rely on the baked paper wells, with smaller action message text, cleaner vertical spacing, and shorter tackle copy so the text reads more like card printing than an overlaid debug panel.
- Increased the right-panel action-message text from 12px to 13px with a slightly wider line gap so the action card reads less like a miniaturized table while still fitting inside the baked paper well.
- Reduced the fight-screen root margin and container gaps in `FishingScreen`, so the top status cards, water window, lower HUD, and right sidebar use more of the 1280x720 viewport like the reference instead of floating inside a dark outer gutter.
- Tightened the `top_status_frame.png` slot ratios and matched `FightStatusBar` runtime slots to the regenerated frame. The weather card now pulls the wind glyph/text closer to `快晴`, reducing the stretched-card feel visible in `/tmp/tsuri_top_status_focus.png`.
- Increased the lower-HUD runtime gauge segmentation from broad blocks to finer 18-step meters. The tension and fish-stamina bars now read closer to the reference's detailed console meters without returning to the earlier broken grid-skin look.
- Removed the large runtime cyan fight aura behind the showcase kurodai and reduced the fish shadow to a small, low-alpha ellipse. A side test with the intact reference water window showed the broad oval artifact was coming mainly from Godot's fish layer, not only from `underwater_battle_bg.png`.
- Added a softer, lower seabed-fragment pass to `tools/build_reference_underwater_background.py`, reusing reference floor pixels only near the bottom of the masked center so it does not strengthen the midwater replacement boundary.
- Added a reference-derived surface-light patch and subtle raster light shafts inside the masked center, then lowered the seabed replacement start/fade so the center reads less like a single horizontal fill band.
- Strengthened the lower-HUD operation key caps in `FightHud` with larger A/B/LR plaques, dark outer strokes, gold inner strokes, and a subtle top highlight so the controls read less like tiny overlay text.
- Rebuilt the right-panel action/tackle lower-card wells with textured paper recesses, softer icon pockets, inner shadows, and shorter low-alpha guide lines so the lower cards read less like white ruled form fields.
- Rebuilt `underwater_battle_bg.png` with waved vertical masks, soft blob masks, additional low-alpha reef fragments, more central bubbles, and top-to-mid light shafts inside the removed fish/hit area so the center replacement reads less like a rectangular clean-water patch.
- Raised the right-panel action-card title/body text block and tightened the action-message line gap so the second line no longer collides with the baked lower guide line.
- Tightened the fight-screen root margin, body separation, left-column separation, and sidebar internal gap so the top status, water window, HUD, and right panel sit with less dark gutter between them.
- Tightened the `top_status_frame.png` slot boundaries together with `FightStatusBar._slot_rects()` and strengthened the weather-card wind text with a darker green, slightly larger label.
- Added low-alpha central caustic strokes and small bubbles to `underwater_foreground_ambience.png`, breaking up the flat masked-water band without drawing over the runtime fish or hit text.
- Retuned the runtime fish/lure/hit cluster in `UnderwaterView`: the showcase kurodai is slightly smaller and shifted left/up, the lure uses a wider forward offset so it does not collapse into the nose, and the hit badge/text are smaller and raised. The line, lure, fish, and hit badge now read as a tighter event cluster without overfilling the water window.
- Added a background-builder-only central distant-fish and midwater-bubble pass, then softened the dark midwater texture while strengthening the reference-derived light shafts. The center replacement is less like a plain blue band in `/tmp/tsuri_fish_hit_focus.png` without covering the fish, line, lure, or `ヒット！` text.
- Rebuilt `sidebar_frame.png` so the fish-card lower guide lines no longer cut through the runtime `推定 44.2 cm` and ecology notes. `FightSidebar` also reduces the compact fish portrait height slightly and uses smaller ecology text, giving the fish card a cleaner printed-information area.
- Repositioned the lower-HUD operation key caps/labels deeper inside the baked paper slots and added small paper label plates, so the slot guide lines no longer cut through `巻く / 緩める / 調整`.
- Switched the right fish-card ecology notes to regular text and shortened the bait label to `エサ：オキアミ・カニ`, removing the dark band caused by overly dense bold small text.
- Tuned `FightStatusBar` optical sizes: larger clock/time, weather, money, and location-depth values, slightly larger top icons, and a tighter AM/time grouping so the top strip reads closer to the reference's strong card numerals.
- Added a stronger reference-derived center background pass in `tools/build_reference_underwater_background.py`: a midwater school texture plus a raised seabed-shelf patch reuse the reference's own fish-school/water/seabed pixels inside the subject mask, reducing the flat blue band behind the runtime kurodai without covering the line, lure, or `ヒット！` text.
- Rebalanced the lower-HUD bait/hint/menu widths so the operation hint card gets more room, regenerated `fight_hud_frame.png` with matching baked slots, and added tiny secondary operation notes under `巻く / 緩める / 調整` to move the HUD closer to the reference's printed control-card information density.
- Added a reference-derived center-floor glint pass in `tools/build_reference_underwater_background.py`, reusing left/right seabed pixels as a low-alpha lower-center patch so the area under the fish and behind the hit text has more seabed reflection without covering the runtime fish or `ヒット！` text.
- Increased the runtime `hit_burst.png` draw scale and moved the fight distance overlay behind the hit treatment with lower opacity, so the blue splash reads more like a real badge behind `ヒット！` instead of being cut by the meter line.
- Tightened right-panel lower-card runtime density: the action-card icon is smaller, action title/message text is larger, and the tackle card now uses a shorter two-line copy with a smaller icon reserve so the lower cards read less like tiny form fields.
- Added a reference-derived midwater-depth patch and safer center sand-channel pass to `tools/build_reference_underwater_background.py`, using only edge water/seabed crops so the center masked zone gains more depth and floor texture without retaining fish, line, lure, or `ヒット！` remnants.
- Rebalanced the lower-HUD bait/hint/menu widths again so the operation-hint card gets more horizontal room, then enlarged the A/B/LR key caps, main action labels, and secondary notes to reduce the shrunken-table read.
- Added a reference-derived center band-breakup haze pass from far-left/far-right water crops, darkening and varying the cyan horizontal fill band without reintroducing fish, line, lure, or `ヒット！` remnants.
- Softened the right-panel lower-card baked text/icon wells in `sidebar_frame.png` and retuned `FightSidebar` action/tackle typography so the small cards read more like printed paper cards and less like nested form controls.
- Added a lower-center floor micro-detail pass to `tools/build_reference_underwater_background.py`, using only left/right safe seabed crops, and reduced the dark midwater/band-breakup opacity so the masked center reads slightly less like a flat dark fill while preserving the runtime fish, line, lure, and `ヒット！` text.
- Rebuilt the lower-HUD operation slots with taller baked paper wells and retuned `FightHud` key-cap/label/note sizes so the A/B/LR controls align with the art slots instead of floating as tiny overlaid text.
- Added a center midwater light-glaze pass in `tools/build_reference_underwater_background.py`, reusing only safe upper-left/upper-right water pixels to bring more reference-like surface-light variation behind the runtime fish without reintroducing the original fish, line, lure, or hit text.
- Rebuilt `top_status_frame.png` and matched `FightStatusBar._slot_rects()` with wider time/weather/money cards, a tighter navy location card, removed baked pale icon wells from the parchment cards, and realigned the top icon draw offset so the top row reads less like separate parts dropped into boxes.
- Rebalanced the center background builder after side-agent review: the dark midwater-depth and band-breakup passes are now lighter/lower-alpha, while the safe upper-water light glaze, edge-sourced sand channel, floor glints, and lower micro-detail read slightly stronger without using risky central hit/fish crops.
- Tightened the lower-HUD operation hint runtime typography: the A/B/LR key plaques are slightly shorter, the main labels sit 1px higher, the secondary notes are smaller and raised, and `_hint_key_slots()` is shortened so the text no longer presses as hard into the baked paper-slot lower rules.
- Reworked the lower-HUD operation hint grammar from stacked key/label/note rows into a more reference-like horizontal `A 巻く（リールを巻く）` composition, removing the small label plates so the three operation slots read less like nested form fields.
- Raised and strengthened the safe upper-water light glaze and edge-sourced floor-glint pass in `tools/build_reference_underwater_background.py`, bringing a little more surface-light and floor reflection into the masked center without increasing the dark band passes or using risky central hit/fish crops.
- Rebuilt `top_status_frame.png` with lower paper texture strength, quieter inner outlines/highlights, and softer weather/money separator rules. `FightStatusBar` now draws slightly larger top icons, tighter icon-to-text spacing, stronger `AM`/weather text, and a readable but closer `AM 08:47` grouping.
- Lightened the center midwater-depth and band-breakup patches in `tools/build_reference_underwater_background.py`, reduced their alpha, lowered the showcase color-grade/seabed-detail overlay opacity in `UnderwaterView`, and reduced the runtime kurodai ground shadow so the fish/hit area reads less like a dark patched band.
- Rebuilt the right-panel lower-card assets so `fight_action_card_icon.png` and `fight_tackle_card_icon.png` are transparent cutout icons instead of tiny paper cards. `sidebar_frame.png` now uses one continuous paper body for the action/tackle cards with only faint inner rules, and `FightSidebar` realigns the larger cutout icons and text to that single-card surface.
- Added a center floor-caustic mesh pass in `tools/build_reference_underwater_background.py`, using only safe left/right seabed crops plus low-alpha floor-light strokes and bubbles inside the subject mask so the fish/hit area gains more seabed reflection density without reintroducing the original fish, lure, line, or hit text.
- Raised the runtime lower-HUD display height from 178px to 198px in both `FishingScreen` and `FightHud`, so the 2048x456 `fight_hud_frame.png` is no longer visibly compressed into a thin strip and the lower operation board has more reference-like presence.
- Rebalanced the center background replacement again: the dark midwater and reef-fragment patches are lighter/lower-alpha, a subject-masked clear-water glaze adds safer blue depth and lower floor light, and `UnderwaterView` reduces the color-grade/seabed-detail overlay opacity to keep old helper layers from forming a dark film over the reference-derived center.
- Tightened the background-builder source hygiene: retained a broad subject mask to prevent fish/hit remnants, moved unsafe reference crops away from the original fish/line zones, and added `_stitch_reference_patches()` so left/right edge crops blend through a gradient instead of creating a hard vertical seam in the center replacement.
- Rebuilt the lower-HUD operation hint body from three separate paper slots into one continuous paper well with subtle dividers, then retuned the runtime key caps, main labels, and smaller secondary notes so the operation area reads less like a broken grid of mini forms.
- Raised the right-panel lower-card action icon/title/message and tackle copy slightly while reducing the action message size back to a safer fit, so the lower cards gain presence without clipping into the baked paper guide lines.
- Added a final center paintover cleanup pass to `tools/build_reference_underwater_background.py`, using only safe edge floor crops, a subject-masked midwater brightness lift, extra low-alpha floor caustics, and small bubbles to reduce the dark flat center band without reintroducing fish, line, lure, or hit remnants.
- Added `tools/source_assets/underwater_center_paintover_candidate.png`, an ImageGen-produced underwater background candidate used only as a center paintover source. `tools/build_reference_underwater_background.py` now blends it through a broad softened center mask plus a stronger subject-region mask after the reference extraction pass, preserving the reference-derived left/right reefs while lifting the center floor caustics, midwater brightness, and water density.
- Retuned the lower-HUD runtime typography again: bait count is lifted away from the lower edge, A/B/LR plaques and main labels are larger, and the secondary operation notes are shortened to inline words so they no longer clip below the paper well.
- Retuned the right-panel runtime copy: fish ecology notes are larger, action/tackle icons reserve less width, and the tackle text gains a little more size/width so the lower cards read less like shrunken debug labels.
- Added a second canvas-scale center floor-lift pass in `tools/build_reference_underwater_background.py`, using the existing `underwater_center_paintover_candidate.png` through stronger central and subject-region masks so the fish/hit region gets brighter floor caustics and midwater density without replacing the reference-derived side reefs.
- Lowered `UnderwaterView` showcase helper overlays again, from color grade 0.24/seabed detail 0.44 to 0.18/0.36, so the old helper layers no longer re-darken the newly lifted central background.
- Lowered `UnderwaterView` showcase helper overlays once more, from color grade 0.18/seabed detail 0.36 to 0.14/0.30, so the reference-derived center light and floor texture are less covered by the old color-grade/detail film.
- Raised the runtime lower-HUD display height from 198px to 206px in both `FishingScreen` and `FightHud`. A 214px trial made the water window too vertically cramped, so 206px is the current balance between reference-like operation-board presence and preserving the main underwater showcase area.
- Retuned the right-panel lower-card body usage again: the action/tackle paper bodies now start slightly higher and use more vertical space, the transparent icons reserve less width, action message text is 15px, and tackle copy is 19px over two lines. In `/tmp/tsuri_frame_focus_compare.png`, the lower cards read less like tiny form fields without new clipping.
- Enlarged the right-panel lower-card cutout icons in `FightSidebar`: the action icon now uses a 50px footprint, and the tackle icon uses a 64x58px right-side footprint with reserved text width. In `/tmp/tsuri_frame_focus_compare.png`, the tackle card reads more like a gear card with a visible rod/reel illustration instead of a tiny hook marker.
- Added a canvas-scale reference-light polish pass to `tools/build_reference_underwater_background.py`. It reuses safe upper-left/upper-right water crops for central surface light, safe left/right floor crops for lower-center seabed reflection, and low-alpha caustic strokes under the subject mask, giving `underwater_battle_bg.png` more continuous center light/floor density without reintroducing the original fish, line, lure, or hit text.
- Strengthened the same reference-light polish with a broader generated central ray gate, subtle top-surface shimmer strokes, and denser low-alpha floor caustic strokes/arcs. These are rasterized into `underwater_battle_bg.png` and do not sample from fish/line/hit regions, so they add surface light and seabed detail without reintroducing subject remnants.
- Strengthened `underwater_color_grade.png`'s surface glow, light shafts, and top shimmer strokes while reducing the very top dark veil, then raised `UnderwaterView` color-grade opacity to 0.18 with seabed detail kept at 0.30. This gives the upper water more reference-like light without changing the fish/hit floor balance.
- Retuned the lower-HUD operation hints again: A/B/LR key plaques and main labels are slightly smaller, and the control notes now sit as a second printed line inside each paper slot (`リールを巻く`, `ラインを出す`, `テンション調整`) instead of disappearing or clipping at the bottom edge.
- Added broad upper-water light shafts and top shimmer strokes to `underwater_foreground_ambience.png`. Because this layer is drawn before the runtime fish, line, and hit badge, it lifts the muted upper water toward the reference without putting a white film over the main subject.
- Strengthened `underwater_battle_bg.png` itself with a canvas-level surface-bloom pass, brighter safe upper-water reference-light polish, reduced top dark harmonize veil, and slightly stronger lower floor-caustic strokes. This moves the water window toward the reference's bright top-to-floor light without reintroducing fish, line, lure, or hit remnants.
- Lowered `UnderwaterView` showcase helper overlays from color grade 0.18/seabed detail 0.30 to 0.12/0.26. A stronger candidate-detail background pass was rejected because it darkened the center water; this runtime balance keeps the reference-derived background less covered while preserving enough edge/depth structure.
- Expanded the lower-HUD operation paper well in `fight_hud_frame.png` and enlarged the runtime A/B key caps, main labels, and secondary notes. The L/R slot uses slightly smaller text and key width so `調整 / テンション調整` fits without pressing into the divider.
- Changed the showcase background/grade/detail/ambience texture placement from centered cover to a slightly top-biased cover alignment in `UnderwaterView`, then lowered helper overlays to color grade 0.10 and seabed detail 0.22. The latest `/tmp/tsuri_fish_hit_focus.png` brings more reference-like surface light into the visible water window and reduces the dark midwater-band read without regenerating the base background asset.
- Raised the runtime lower-HUD minimum height from 206px to 224px in both `FishingScreen` and `FightHud`. The latest full and frame comparisons show the lower board reading less like a thin strip and closer to the reference's thick operation console, while the fish/hit cluster remains readable in the slightly shorter water window.
- Softened the right-panel action/tackle body wells in `sidebar_frame.png` by lowering inner-outline, guide-line, and corner-bracket alpha. `FightSidebar` now uses a 56px action cutout, a slightly larger 16px action message, 20px tackle copy, and a 68x60px tackle cutout so the lower cards read more like printed information cards and less like small form controls.
- Added `_add_canvas_visible_floor_density()` to `tools/build_reference_underwater_background.py`. It lifts safe left/right edge-floor pixels, small bubbles, and low-alpha caustic strokes into the mid-lower part of `underwater_battle_bg.png`, because the runtime water window crops the lower texture area. The latest `/tmp/tsuri_fish_hit_focus.png` shows slightly more floor-light density under the fish and around the hit badge without visible fish/line/lure remnants.
- Background freeze decision: the current background pass is accepted for this phase. It still trails the reference's fully authored center art, but the latest side-by-side has no clear black band, hard mask edge, broken cutout, or obvious fish/line/lure ghost; the fish, line, target/depth marks, distance bar, and hit treatment remain readable; and the water light, side rocks/seaweed, bubbles, and seabed density now read as a high-quality underwater base. Do not keep iterating background brightness, bubbles, floor light, or center density unless full-screen side-by-side QA shows a new P1 regression.
- Tightened the kurodai cutout pipeline in `tools/process_underwater_fish_assets.py`: semi-transparent magenta edge pixels are neutralized after the main despill, very low-alpha fringe pixels are reduced, and the runtime `kurodai_showcase_sheet.png` gets a thin dark-blue underwater edge underlay. The paper-card portrait is generated from the clean cutout before that runtime underlay, so the sidebar fish does not inherit a blue halo on parchment. Purple-edge pixels in the runtime sheet dropped from 4388 to 264, and the latest `/tmp/tsuri_fish_hit_focus.png` keeps the fish readable without adding a debug-like oval or reopening background work.
- Reworked the right-panel lower-card paper wells: `sidebar_frame.png` no longer bakes the upper body guide line that was aligning with the `突進！` and tackle text baselines, the remaining lower guide/corner brackets are quieter, and `FightSidebar` lowers the action/tackle text and icons a few pixels so they sit more like printed card content. The updated `/tmp/tsuri_frame_focus_compare.png` reduces the form-field/ruled-paper conflict in the action and tackle cards without changing the frozen background.
- Softened the lower-HUD operation paper-well dividers and enlarged the runtime A/B/LR plaques, labels, and secondary notes. The updated `/tmp/tsuri_frame_focus_compare.png` reads less like a table/grid of form fields and more like a printed operation console.
- Rebuilt `fight_action_card_icon.png` and `fight_tackle_card_icon.png` from reference lower-card crops with paper-background flood removal instead of deriving them from the generic icon sheet. `FightSidebar` now draws the tackle cutout larger, so the bottom card reads more like the reference's rod/reel information card without dropping the `港の入門竿` text.
- Reduced the showcase kurodai draw width, moved its runtime center slightly farther left/up, and softened the hit treatment scale/text size. `/tmp/tsuri_fish_hit_focus.png` keeps the fish readable while restoring more water-window breathing room around the main event.
- Retuned `FightStatusBar` runtime drawing: slightly smaller/repositioned top icons, stronger time/weather/money values, tighter wind-glyph spacing, and a regenerated `/tmp/tsuri_top_status_focus.png` check against the reference.
- Regenerated `kurodai_card_portrait.png` with a slightly smaller fish inside the 620x330 paper window, restoring more card-window margin while keeping the dedicated clean-cutout portrait path.
- Polished the right fish-card title plaque at runtime: `クロダイ` now centers between `No.028` and the rarity tag, and the `レア` tag uses a small authored plate treatment instead of a flat magenta rectangle.
- Reworked the fish-card portrait path so `kurodai_card_portrait.png` is a transparent left-facing cutout with only a soft contact shadow, while `sidebar_frame.png` carries the faint paper grid/rules. This removes the old nested rounded portrait panel and matches the reference fish-card direction.
- Restored the fish-card lower information hierarchy: compact cards now draw a plain ecology sentence followed by two small green-dot rows for bait and habitat, instead of treating all lower text as identical bullet rows.
- Retuned the lower-HUD operation key caps: A/B now render as round button caps and L/R as a compact pill, reducing the rectangular form-field read while preserving the one-piece operation paper well.
- Softened the right fish-card title treatment: `sidebar_frame.png` no longer bakes a filled name plaque, and `FightSidebar` draws the compact title row directly on the paper surface with only a thin divider, reducing the nested-plaque/form-card read.
- Current environment note: after the sandbox changed, non-headless Godot preview crashes before writing logs, while headless project initialization passes and headless `SubViewport` capture cannot render. Re-run `/tmp/tsuri_fight_compare.png`, `/tmp/tsuri_frame_focus_compare.png`, and `/tmp/tsuri_sidebar_fish_card_focus.png` once normal preview capture is available.
- Regenerated `/tmp/tsuri_fight_compare.png`, `/tmp/tsuri_frame_focus_compare.png`, and `/tmp/tsuri_fish_hit_focus.png`.

## Findings

- [P2] Right panel is structurally better but still below reference card quality.
  Location: `assets/showcase/underwater/sidebar_frame.png`, `src/ui/components/fight_sidebar.gd`, `src/ui/fishing_screen.gd`.
  Evidence: the panel is now wider, starts at the top of the screen beside the status cards, uses a dedicated fish-card kurodai portrait instead of the runtime swimming sheet, and the previous dark portrait rectangle regression is gone. The fish-card title row now avoids the earlier opaque nested plaque: `sidebar_frame.png` owns a thin paper divider, and runtime draws `No.028 / クロダイ / レア` directly on the card surface so the header is closer to the reference's printed title row. The runtime title pass centers `クロダイ` in the available space between the number and the tag, and the `レア` tag has an inset highlight/shadow plate treatment instead of the earlier flat magenta rectangle. The newest fish-card pass makes `kurodai_card_portrait.png` a transparent left-facing cutout with a soft contact shadow, matching the reference card's fish direction and removing the earlier nested rounded portrait panel. The card paper, faint grid, and divider rules now live in `sidebar_frame.png`, so the fish reads more like it is printed on one card surface rather than pasted into a second UI panel. Moving the status bar into the left column gives the right panel more vertical room, and the 620x330 `kurodai_card_portrait.png` ratio still fits the runtime fish window better than the previous 720x330 source. The latest runtime pass restores the reference-like lower information hierarchy: a plain ecology sentence appears above two green-dot rows for bait and habitat. It uses smaller 12px regular text in the compact card so the lower rows fit without colliding with the card bottom, while avoiding the older state where every lower line looked like the same bullet row. Recent lower-card passes make the navy title bands shallower, enlarge the paper wells, reduce the icon-well footprint, soften internal guide lines, and realign the action/tackle text onto the larger paper areas. The lower-card path now stops baking separate icon wells and text wells into the action/tackle cards, regenerates the dedicated lower-card icons as transparent cutouts instead of tiny paper-card thumbnails, and places the larger cutout icons/text onto one continuous paper body. This pass derives those lower-card cutouts from the reference mockup itself, removing the connected parchment background from the crop and falling back to the generic icon sheet only if the reference file is missing. The runtime path also stops drawing an extra body panel over the baked action/tackle wells, raises the action-card title/body block, shortens the tackle second line to `糸3号・チヌ針`, and gives the tackle text enough width to keep `港の入門竿` visible. This pass keeps the action cutout at 56px, keeps the action message at 16px, keeps tackle copy at 20px, and enlarges the reference-derived tackle cutout draw box to 88x70px. In the current sandbox, full runtime recapture is blocked by a non-headless Godot crash, so the latest title-row change is verified against `sidebar_frame.png` directly plus headless project initialization, not against a fresh `/tmp/tsuri_sidebar_fish_card_focus.png`. They still feel more mechanically framed than the reference, especially around small-text optical quality and the authored paper-card finish.
  Impact: the right panel now matches the reference's top-level composition better, the fish card no longer has visible rule/text collisions, and the title/fish/lower-information areas read more like printed card UI instead of a set of flat labels and nested panels. The latest pass should reduce nested-plaque noise in the fish-card header, but the full runtime comparison must be rerun once preview capture is available.
  Fix: keep the dedicated transparent left-facing fish-card portrait, soft contact shadow, 620x330 portrait ratio, wider panel, direct-on-paper fish-card title row, authored rarity tag plate, paper/grid/rules owned by `sidebar_frame.png`, cleaned fish-card lower information area, 12px compact-card paragraph plus green-dot bait/habitat rows, reference-derived transparent lower-card icons, one-piece lower-card paper bodies, removed upper body guide line, softened lower guide/corner brackets, 56px action cutout, 88x70px right-side tackle cutout draw box, 16px action message, 20px tackle copy, and the shorter two-line tackle copy. Next right-panel work should focus on final card typography and lower information spacing rather than reintroducing a framed portrait window, a filled fish-title plaque, or internal guide rules.

- [P2] HUD top row and board proportions are closer, but still below the reference's authored console quality.
  Location: `assets/showcase/underwater/fight_hud_frame.png`, `src/ui/components/fight_hud.gd`.
  Evidence: the upper board is now darker, the central depth module is less bright, the title/icon scale is more compact, and the row icons are visually quieter after opacity modulation. The runtime meter now uses finer 18-step segmentation, inactive segments remain faintly visible, and filled segments retain small highlight/shadow treatment instead of flat rectangles. This brings the tension and fish-stamina bars closer to the reference's detailed console meters while avoiding the earlier broken grid-skin look. The lower bait/hint/menu area now has stronger baked paper title bands, a one-piece operation paper well with subtle separators, darker menu rows, and runtime A/B/LR key caps aligned to the authored paper area instead of floating inside a generic grid. Recent operation-hint passes keep the hint card wide by preserving the reduced bait/menu proportions. This pass keeps A/B as small round physical button caps, uses a compact L/R pill for the longer key label, and keeps the main labels/secondary notes inside the baked operation well. The displayed HUD minimum height is now 224px. In `/tmp/tsuri_frame_focus_compare.png`, the lower board reads less like a thin compressed strip or table of form fields and more like the reference's thick operation console; `/tmp/tsuri_fish_hit_focus.png` confirms the slightly shorter water window does not break the fish/hit composition. The operation hint no longer reads as three nested form boxes or rectangular input fields, though its small text still lacks the reference's custom-rendered console typography. The reference still has more deliberate black-panel spacing, larger authored operation-card proportions, stronger custom text rendering, and a more bespoke meter construction.
  Impact: the HUD reads more like an authored operation board and less like a debug grid. The wider hint card, taller baked slots, horizontal operation grammar, round A/B caps, compact L/R pill, and thicker display height improve scan quality, but the overall construction still does not fully reach the reference's premium console quality.
  Fix: keep the darker frame, label padding, quieter icon opacity, reduced segment grid, visible inactive segments, navy menu card, wider operation-hint card, softened one-piece operation paper well, 224px runtime display height, round A/B caps, compact L/R pill, and the compact two-line `key + label + note` grammar with slightly reduced L/R typography. Next HUD work should focus on final typography and frame proportions rather than restoring rectangular A/B boxes, three separate paper slots, strong vertical rules, or small label plates.

- [P2] Top status bar has stronger material quality, but still trails the reference's compact icon grammar.
  Location: `assets/showcase/underwater/top_status_frame.png`, `assets/showcase/underwater/top_status_icon_sheet.png`, `src/ui/components/fight_status_bar.gd`.
  Evidence: the top status frame now has stronger paper-card inner borders, corner brackets, and a more authored navy location card. The old common icon sheet made the first three top cards read as small noisy ornaments; the new top-specific icon sheet gives the clock, sun, wind, and coin clearer silhouettes closer to the reference, and the wind glyph is now placed inline before `風 弱`. The status bar now lives only over the left fight column, and the latest slot pass widens the time/weather/money cards while tightening the navy location card, so `/tmp/tsuri_top_status_compare.png` no longer has the same stretched location-card feel. The baked pale icon wells were removed from the parchment cards, and the runtime top-icon offset was aligned closer to the generated frame, reducing the sense that icons and text are separate dropped-in parts. The newest runtime pass draws the top icons slightly smaller and 2px farther right, strengthens `08:47`, `快晴`, `風 弱`, and `12,450 G`, and tightens the wind glyph/text spacing. `AM 08:47` is now closer to the reference's strong numeral card without collapsing into `AM08`, and `/tmp/tsuri_top_status_focus.png` shows the weather card reading more like one compact `快晴 | 風 弱` strip. Screen-level margin passes reduce the dark outer gutter, narrow the body/left-column/sidebar gaps, and make the top row, water window, HUD, and right panel sit closer to the reference's edge-to-edge density. The reference still has more tailored glyph metrics, tighter card-to-card spacing, better extracted icons, and stronger icon/text alignment.
  Impact: the top row now reads more like the reference's authored status strip, no longer pushes the right panel down, and feels less isolated from the water window. It still does not fully match the compact, high-contrast reference top bar.
  Fix: keep `top_status_icon_sheet.png`, left-column placement, the wider time/weather/money ratios, tighter location card, removed baked icon wells, quieter parchment inner rules, current smaller/right-shifted top icons, and the stronger money/time/weather typography. Next top-row work should focus on replacing or cleaning the top icon sheet if those icons still feel less crisp in the final full-screen pass.

- [P3] Main fish is now close, with minor animation/contact polish remaining.
  Location: `assets/showcase/underwater/kurodai_showcase_sheet.png`, `assets/showcase/underwater/kurodai_card_portrait.png`, `tools/process_underwater_fish_assets.py`, `src/ui/components/underwater_view.gd`.
  Evidence: the fish source now preserves the reference-like black seabream outline, scale texture, dorsal spines, eye, and gray banding. The implementation fish is no longer a generic generated cutout, and the previous oversized cyan fight aura is gone. The dark fish shadow is small, so the fish no longer sits inside a visible debug-like oval that the reference does not have. The runtime path now uses `SHOWCASE_FISH_CENTER_OFFSET = Vector2(-0.072, -0.014)`, reduces the boss showcase fish width ratio to 0.465 of the water-window width, keeps the lure forward offset at about 0.44 of the fish width, and reduces the hit badge/text scale. The cutout pipeline also cleans the transparent fish edge after despill and adds only a thin dark-blue runtime edge underlay to `kurodai_showcase_sheet.png`; the paper `kurodai_card_portrait.png` still uses the clean cutout before that underlay. In `/tmp/tsuri_fish_hit_focus.png`, the purple/magenta fringe is no longer visible, the fish still reads crisply against the frozen water background, the lure sits forward of the nose instead of merging into it, and the smaller hit treatment leaves more water-window breathing room around the event.
  Impact: the fish is no longer a blocking quality mismatch; it now sells the screen's main subject with a cleaner cutout edge and less sticker-like compositing against the water.
  Fix: keep this asset, cleaned transparent edge, runtime underwater edge underlay, `Vector2(-0.072, -0.014)` fish center offset, 0.465 boss draw-width ratio, lure offset, and smaller raised hit treatment as the current kurodai baseline. With the background frozen, remaining fish work should focus on subtle frame/state variation and contact/shadow polish only if it improves the reference comparison without adding a visible oval or halo.

- [P3] Background passes this phase; final ideal gap remains, but it is frozen unless a P1 regression appears.
  Location: `assets/showcase/underwater/underwater_battle_bg.png`, `tools/build_reference_underwater_background.py`, `assets/showcase/underwater/underwater_color_grade.png`, `assets/showcase/underwater/underwater_seabed_detail.png`, `assets/showcase/underwater/underwater_foreground_ambience.png`, `src/ui/components/underwater_view.gd`.
  Evidence: the primary background is no longer the generated canyon scene. `tools/build_reference_underwater_background.py` now extracts the full authored water window from the reference mockup, preserving the real left rock pile, right reef, seabed caustics, bubbles, distant fish, surface light, and darker blue side depth. The main fish, hit burst, line, and lure zones are broadly masked so the runtime kurodai, hit treatment, and line can sit on top without obvious duplicates. The retained reference-derived passes soften the subject mask, strengthen the midwater/seabed texture, keep tiny bubbles in the central replacement zone, add very low-alpha reference floor and reef fragments near the lower masked center, reuse the reference's surface-light pixels as a faint central light patch, and apply waved/blobbed alpha masks so the inserted patches are less rectangular. Recent builder passes add a reference-derived fish-school/water texture, lighter and lower-alpha midwater-depth and band-breakup patches from safe edge water crops, a stronger safe upper-water light glaze, a raised seabed-shelf patch, a brighter center sand channel from far-left/far-right seabed crops, brighter/raised center-floor glints, slightly stronger lower-center floor micro-detail, and a low-alpha center floor-caustic mesh directly inside the subject-limited background mask. This pass keeps the broad subject mask after a tighter-mask attempt visibly reintroduced fish remnants, moves unsafe source crops away from the original fish/line zones, blends left/right source crops with `_stitch_reference_patches()`, blends the generated `underwater_center_paintover_candidate.png` through both a broad center-water mask and a stronger subject-region mask, then adds a second canvas-scale center floor-lift mask so the center floor and hit region receive more of the candidate's bright caustics. `_add_canvas_reference_light_polish()` reuses only safe top and side-floor reference crops after the canvas-scale paintover, strengthens the generated broad central ray gate, adds a separate top surface-bloom/shimmer layer, and slightly increases lower floor-caustic stroke strength. The newest `_add_canvas_visible_floor_density()` pass accounts for the runtime water window's top-biased, wide crop by lifting safe edge-floor pixels, bubbles, and thin caustic strokes into the visible mid-lower band of the texture. This adds a little more floor-light density under the fish and around the hit badge without sampling from the original fish, line, lure, or hit zones. `underwater_color_grade.png` now has stronger upper surface glow/light shafts and a weaker top dark veil. The runtime draws the showcase background, color grade, seabed detail, and foreground ambience with a top-biased cover alignment (`Vector2(0.5, 0.24)`) instead of a centered crop, then uses color grade/seabed helper opacity at 0.10/0.22 so the reference-derived center is less covered. `underwater_foreground_ambience.png` still carries broad upper-water shafts and top shimmer behind the runtime fish/hit layers. In `/tmp/tsuri_fish_hit_focus.png`, the visible water window now keeps more surface light from the source asset, the midwater band reads less dark, and the fish/hit floor region keeps cleaner floor-light density while the runtime fish, line, lure, and `ヒット！` text remain clean. The center is still not as fully authored as the reference's hand-painted water window.
  Impact: this is a visible move toward the target image because the water panel carries more of the reference's real edge density while the generated center source gives the masked area a more finished floor-light read. It reduces the flat replacement-band feeling more than deterministic crop reuse alone. The remaining gap is that the center is still not as hand-authored as the reference, but that is now a P2/P3 ideal-quality gap rather than a P1 blocker for the whole screen.
  Fix: freeze this background for the current pass. Keep the full-window extraction, broad no-remnant subject mask, stronger center texture pass, waved/blobbed masks, central light shafts, edge-only safe crops, blended left/right crop stitching, lighter edge-sourced midwater-depth patch, lower-opacity brighter center band-breakup haze, raised safe upper-water light glaze, clear-water glaze, reduced dark reef-fragment alpha, edge-sourced center sand channel, center-floor glint patch, floor-caustic mesh, lower-center micro-detail patch, sparse central background fish/bubbles, final center paintover cleanup, generated center paintover source, second canvas center floor-lift pass, canvas reference-light polish, visible-floor-density polish, broad generated central ray gate, stronger color-grade surface glow, foreground-ambience upper shafts, top-surface bloom/shimmer strokes, denser floor caustic strokes/arcs, reduced runtime fish shadow, top-biased showcase texture alignment, and the current 0.10/0.22 showcase overlay balance as the deterministic background build path. Do not adjust background brightness, bubbles, floor light, or center density again unless `/tmp/tsuri_fight_compare.png` or `/tmp/tsuri_fish_hit_focus.png` shows a clear P1 defect such as a black band, hard mask edge, broken cutout, obvious ghost, or unreadable fish/line/distance elements.

- [P3] Hit treatment is closer, with only final context polish remaining.
  Location: `assets/showcase/underwater/hit_burst.png`, `src/ui/components/underwater_view.gd`.
  Evidence: the badge now reads as a darker blue splash with fewer white rays/flecks behind the yellow/orange live text, and its lower edge sits closer to the top of the operation board. The runtime pass draws the fight distance meter before the hit treatment, lowers the distance meter opacity, then places a smaller raised `hit_burst.png` and slightly smaller `ヒット！` text over it. This pass clamps the hit-burst texture scale to 0.42-0.50 and the hit text to 42-58px so the splash still reads behind the text but no longer dominates the fish/lure cluster or pushes as hard into the lower HUD. It still differs slightly from the reference starburst silhouette and text optical weight.
  Impact: the hit moment has more of the reference's central badge presence while keeping the fish and HUD readable. It is no longer a major art-style mismatch, but it is still not a perfect match to the reference badge.
  Fix: keep the quieter ray/glint density, distance overlay behind the hit treatment, smaller raised runtime splash, 0.42-0.50 burst scale clamp, and 42-58px hit-text clamp. Revisit only after the final HUD/font pass or if the frozen background comparison exposes a clear read issue.

- [P2] Typography is improved but still not at the reference's custom UI quality.
  Location: all fight UI overlay text.
  Evidence: the fight UI now uses `MPLUS1p-Bold.ttf` for the main overlay text, the top-status numbers are stronger, and the location/depth card now matches the reference's iconless centered layout. The parchment-card icons are smaller and lower opacity, but still more ornate than the simpler reference glyphs. The reference still has more tailored optical weights plus tighter small-text rendering.
  Impact: the screen now reads more like a game UI, but typography still does not fully sell the premium mockup quality.
  Fix: keep the bold/regular split, then tune per-component font sizes and replace/simplify the top-status icon sheet if those icons still read too noisy in the final full-screen pass.

## Open Questions

- None blocking. The background is accepted and frozen for this phase: final ideal differences remain, but they should not drive another brightness/bubble/floor-light/center-density loop. The next highest-value pass is the screen's foreground quality: main fish final art/size/contact/outline first, then right-panel cards/tags/rules/spacing/information density, then bottom HUD/buttons/typography/states, then top status icons/typography.

## Implementation Checklist

1. Keep comparing `/tmp/tsuri_frame_focus_compare.png` against the reference after any HUD/sidebar frame change.
2. Treat `underwater_battle_bg.png` as frozen for this phase. Only reopen `tools/build_reference_underwater_background.py` if full-screen side-by-side QA shows a P1 background defect, not for normal brightness, bubble, floor-light, or center-density polish.
3. Keep the dedicated fish-card portrait; do not return to drawing the swimming sprite sheet directly in the sidebar.
4. Re-run `/tmp/tsuri_fight_compare.png`, `/tmp/tsuri_frame_focus_compare.png`, and `/tmp/tsuri_fish_hit_focus.png` after each pass.
5. Re-run the normal Godot preview capture after the current sandbox restriction is gone; headless project initialization passes, but headless `SubViewport` capture cannot replace visual side-by-side QA.

## Follow-up Polish

- Prioritize the main fish final art/size/contact/outline, then the right panel, bottom HUD, and top status polish. Background-only polish is out of scope unless it fixes a P1 regression.
- Recheck lower HUD text sizes after any layout/proportion change; the current paper slots and key-cap positions are intentionally paired.
- Recheck the hit splash after the final HUD/font pass, but do not keep iterating it unless the comparison regresses.
- Add small sparkle/bubble particles only after the main frame and typography mismatches are solved.
- Add subtle tail/body variants to the kurodai sheet later if animation quality becomes noticeable; keep the current static extracted art for visual fidelity.
